# PLAN MAESTRO — Salón y Más

**Creado:** 9 de agosto de 2026 · **Última revisión:** 23 de septiembre de 2026 (revisión integral, D-254; hallazgos cerrados por la tarde, D-255 a D-259)
**Estado:** vigente · **Manda sobre:** todo lo demás en materia de qué falta y en qué orden

> **Este documento reemplaza y jubila a siete:** `PLAN_DE_LANZAMIENTO_2026-08-06`,
> `BARRIDO_Y_PLAN_MAESTRO_2026-08-08`, `PLAN_DE_TRABAJO_A_PRODUCCION.xlsx`,
> `RUTA_GENERAL_2026-07-25`, `RUTA_A_PRODUCCION_2026-07-25`,
> `BEAUTYOS_EXPEDIENTE_TECNICO_Y_PLAN_MAESTRO` y `PROMPT_MAESTRO_IA`.
> Todos están en `docs/_archivo/` — no se borraron, porque guardan el porqué.
>
> **Se creó porque siete documentos opinando sobre lo mismo ya se contradijeron
> dos veces** (D-063 el 25-jul, D-118 el 09-ago) y en la segunda se trabajó una
> etapa entera fuera de orden sin que nadie lo notara.

---

## 1. Qué es y para quién

**Salón y Más es una aplicación por suscripción para dueños de salones de
estética:** spas de uñas, barberías, peluquerías, estéticas caninas.

**A quién se le vende:** al dueño o dueña del salón. Persona que no es técnica,
que trabaja dentro de su propio negocio, y que hoy lleva su agenda en un
cuaderno o en el WhatsApp.

**Qué le resuelve, en una frase por plan:**

- **Básico** — *"que no se me pierda ninguna cita ni ningún cobro"*
- **Business** — *"que sepa si estoy ganando plata de verdad"*
- **Profesional** — *"que me vean y me busquen"*

> ⚠️ *Desde el 01-sep hay **un solo plan**, Todo Incluido (D-188). Las tres
> frases se conservan a propósito: D-219 las cita porque eran **el orden en que
> la dueña conocía el producto**, y al fundir los planes nada lo sustituyó.*

**Cómo se vende, al principio:** salón por salón, de forma personalizada. Por
eso existe el filtro de aceptación de la fase 3: **nadie entra solo**.

**El mercado:** Colombia. La competencia principal es chilena y argentina, con
soporte en otra zona horaria y sin factura colombiana. **No hay hoy una
aplicación de gestión de salones viralizada en Colombia.**

---

## 2. Cómo se usa este documento

| Marca | Significa |
|---|---|
| 👤 **TÚ** | Solo lo puede hacer el propietario (comprar, registrar cuentas, decidir) |
| 🤖 **YO** | Lo hace el asistente en una sesión de trabajo |
| 👥 **JUNTOS** | El asistente guía paso a paso mientras el propietario ejecuta |

| Estado | Significa |
|---|---|
| ✅ | Cerrado y verificado en producción |
| 🔄 | En curso |
| ⬜ | Pendiente |
| ⛔ | Bloqueado por otra cosa |

**Regla de oro:** no se salta de fase sin cerrar la anterior. Si hay que
saltarse algo, **se anota aquí por qué**.

**Las tres reglas que sostienen la continuidad del proyecto:**

1. **Este documento manda** sobre qué falta y en qué orden.
2. **`REGISTRO_DE_DECISIONES.md` manda sobre el porqué.** Solo crece, nunca se
   resume, nunca se borra una fila. Es lo que permitió el 09-ago descubrir tres
   errores propios que ninguna prueba automática vio.
3. **El HANDOFF más reciente manda sobre dónde quedamos hoy.** Se reemplaza
   cada sesión.

**No hay un cuarto documento con opinión sobre el plan. Si aparece, está mal.**

---

## 3. El plan, el precio y la sede

**Decidido el 01-sep (D-188). Corrige a D-004, D-124, D-136 y D-140, y resuelve
la Idea I-14.**

### Un solo plan

Se retiró la escalera de tres (Básico / Business / Profesional). Queda **un solo
plan, "Todo Incluido"**, y el eje de cobro deja de ser *qué módulos te dejo
usar* para pasar a ser **cuántas sedes tienes activas**.

**Por qué:** enamorar al salón desde el día cero con el producto entero, quitar
la sensación de mezquindad de los candados por módulo, y que el ingreso crezca
con el negocio del cliente en vez de con lo que se le esconde.

**Qué trae, que es todo:** agenda y citas, caja y cobros, comisiones, clientes
con historial, inventario, compras y gastos, reportes financieros, fotos de
trabajos, reseñas y la vitrina web pública. Sin topes de estilistas, de cuentas
de equipo ni de fotos.

**Lo único que no está incluido es lo que no existe:** WhatsApp con agente e
Instagram automático son Fase 6, y la regla de esa fase sigue en pie — *nada de
la fase 6 se vende como disponible hasta que exista*.

### Precios

**Ajustado el 02-sep (D-189).**

| | Por sede al mes |
|---|---:|
| **Precio de lista** | **$150.000** |
| | *$5.000 al día* |

**El descuento de lanzamiento no se publica.** Ni cifra ni cupo. En la web hay
un *"Consulta la tarifa especial de lanzamiento"* con enlace a WhatsApp, y el
precio de cada salón se fija **uno a uno desde el panel**, al aprobarlo. Así no
se atan las manos en la negociación ni se promete en público algo que luego
haya que sostener con todo el mundo.

> *Nota del 23-sep: **desde D-252 (22-sep) ese precio se fija en cada sede**,
> no en el negocio. Al aprobar un salón se escribe en su sede principal; las
> demás se pactan con el botón **Pago** de cada sede.*

- **El descuento se fija como precio pactado, nunca como porcentaje.** Un
  33,33% de 150.000 tampoco cae redondo, y `beautyos_procesar_evento_epayco`
  compara el monto pagado contra el calculado (D-159): un peso de diferencia se
  ve en el checkout.
- Los pioneros que ya tienen $80.000 pactados **se quedan con su precio**.

### Cuánta gente cabe

**10 personas por salón: el dueño más 9 cuentas de equipo** entre
administradores, recepcionistas y estilistas.

- ⚠️ **El tope guardado es 9, no 10**, porque **el propietario no cuenta contra
  el límite** (D-136: *"su cuenta no es de equipo, es el negocio"*). Poner 10
  daría once personas.
- ⚠️ **Y el tope es por NEGOCIO, no por sede.** `create_team_invitation` cuenta
  el tenant entero. Que sea "10 por sede" solo será cierto cuando la Etapa 2
  haga la suscripción por sede — **hay que arreglarlo ahí, o la promesa
  comercial y el código dejan de coincidir**. ✅ *Resuelto el 02-sep: el tope
  se multiplica por las sedes activas (D-196, paso 8.18).*
- **Al salón grande se le concede la excepción desde el panel**, con
  `tenant_feature_overrides`. Es mejor tener algo que conceder que no tener
  nada.

**Clientas, citas, tickets y fotos no tienen tope ninguno.**

### Qué cambia de precio para quién

| | Antes | Ahora | |
|---|---:|---:|---|
| Salón de **1 sede** | $160.000 (Básico, sin inventario ni reportes ni fotos) | **$150.000 con todo** | algo más barato, y completo |
| Cadena de **3 sedes** | $240.000 (Profesional, sedes ilimitadas) | **$450.000** | casi el doble |

Es coherente —el valor escala con el negocio— pero **cambia para quién es barato
el producto**: el salón de barrio, que es el cliente objetivo escrito en el
apartado 1, sale ganando; la cadena paga más.

**Frente a AgendaPro** ($99.000 el capado, $510.000 el completo, precios reales
de una cuenta de prueba, ver el benchmarking del 28-jul): su equivalente a *todo
incluido* cuesta **más de cuatro veces**. Ese es el argumento de la primera
visita, no el precio de entrada.

### Cómo se cobra la sede — en tres etapas

El cobro por sede rehace el ciclo de facturación anclado (D-160), el webhook y
las intenciones de pago (D-182), que es el código de dinero recién blindado. Por
eso va por partes:

| Etapa | Qué | Estado |
|---|---|---|
| 1 | Catálogo de un solo plan, capacidades abiertas y pantalla pública | ✅ **CERRADA 02-sep (D-188, D-189).** Migración aplicada, Control 201 en verde (8 de 8) y `create-epayco-session` desplegada con el plan por defecto `pro` |
| 2 | **Suscripción por sede:** tabla propia, la sede nace inactiva y se activa al pagarse | ✅ **CERRADA 02-sep (D-190), paso 8.15.** *(Esta fila siguió marcada como pendiente hasta el 23-sep.)* |
| 3a | **La base:** `branch_id` en la intención de pago y el cálculo del cargo por sede con prorrateo | ✅ **CERRADA 02-sep (D-191).** Control 203 en verde |
| 3b | **Lo que cobra:** checkout y webhook por sede, y la pantalla de pago del salón | ✅ **CERRADA 02-sep (D-192, D-193).** Faltan las alertas por sede y el tope de equipo por sede, anotados aparte |

> 🔴 **Hasta que la Etapa 3 esté hecha no se puede vender la segunda sede.** Hoy
> un negocio paga una suscripción y puede crear las sedes que quiera sin que se
> le cobren. Con cero clientes pagando no cuesta dinero, pero es un agujero de
> ingreso en cuanto entre el primer salón con dos locales.
>
> ✅ **Ya no aplica desde el 02-sep:** 3a y 3b cerraron (D-191, D-192, D-193)
> y la segunda sede se cobra. Desde D-239 (16-sep) **cobra la sede**, y desde
> D-252 (22-sep) el precio solo existe en la sede.

---

## 4. Estado real, módulo a módulo

### Hoy (23-sep)

**19 módulos en el menú:** 15 de la dueña y el administrador, 4 de la estilista
(los *"Mi…"*), y la recepción ve 3 — Agenda, Tickets & Caja y Clientes.
Sacado de `main.dart` el 23-sep, no contado a ojo: este documento llegó a decir
14, 15, 16 y 19 en sitios distintos. Más dos aplicaciones aparte: el panel de
plataforma y lo que ve la clienta.

| Módulo | Qué hace hoy | Qué le falta | Dónde |
|---|---|---|---|
| **Agenda** | Tablero día / semana / mes, cita express, cobrar desde la tarjeta (D-148, D-195) | Abrir por hora y recordar la elección | I-18 |
| **Tickets & Caja** | Niveles 2 y 3, número de venta, abonos, **atender servicios desde aquí** (D-150, D-152, D-163, D-250) | La lógica de dinero vive dentro de la pantalla; cuatro diálogos que borran lo escrito al fallar | 9.13, AK |
| **Clientes** | Valor y retorno (RFM), **el celular es la llave única** (D-153, D-249) | — | — |
| **Dashboard** | Indicadores, *Tu negocio en palabras*, **cinco** Primeros pasos (D-168, D-186, D-251) | — | — |
| **Reportes** | Períodos, métodos de pago, consolidado multisede (D-154, D-194) | El desglose por servicio puede cambiar de día | I-15 |
| **Inventario / Compras / Gastos** | Funcionan, pulidos (D-155) | — | — |
| **Fotos de trabajos** | Privadas hasta aprobar, filtros, estudio de publicación (D-119, D-156, D-169) | **Que autorice la clienta**; *Visible al cliente* no funciona; el flujo de captura | 9.48, AQ, AU, Ñ |
| **Reseñas** | Moderación y respuestas (D-170) | **Que la clienta autorice su nombre**; el formulario público sin tildes | 9.48, AV |
| **Blog** | Por salón (D-171) | — | — |
| **Servicios / Estilistas** | Catálogo y precio por sede (D-156) | El estilista tiene dos nombres que nadie concilia | AN |
| **Usuarios** | Invitar, sedes por persona (D-179) | Al invitado le llega el correo del dueño | AL |
| **Configuración** | Completa: sedes con su pago, **comisión confirmable** (D-193, D-242, D-251) | — | — |
| ***Mi agenda / Mis fotos / Mis reseñas / Mi panel*** *(estilista)* | Funcionan; el panel avisa si nadie confirmó la comisión (D-251) | Pasar a una aplicación propia | 9.25 (D-219) |
| **Panel de plataforma** | Maestro-detalle, sedes con precio y estado, referidos, borrar demos (D-173, D-236, D-240, D-246) | Historial sin sede, módulos por sede, fotos en soporte, pruebas | 9.40, 9.41, BC, 9.9 |
| **Lo que ve la clienta** | Página pública, reserva, portal con PIN, reseña (D-165, D-167) | Autorizar sus fotos y su reseña; tildes; la hora que caduca | 9.48, AV, AX |

### Foto del 10-ago, que se conserva

*Esta tabla no se mantuvo desde agosto y fue durante semanas lo primero que
creía quien llegaba nuevo. Se deja como historia de dónde se partió.* Un dueño
veía **16 módulos**. Esto era lo que había entonces.

| Módulo | Qué hace hoy | Qué le falta | Dónde se arregla |
|---|---|---|---|
| **Dashboard** | ✅ 4 indicadores comparados, gráfico, agenda de hoy, avisos | "Tu negocio en palabras" | F6.1 |
| **Agenda** | Lista básica | Pasa a ser el tablero día/semana/mes | F4.2, F4.3 |
| **Tickets** | Panel completo de cobro | Nivel 2 y 3; número de venta; píldora de estado | F4.4, F4.5 |
| **Clientes** | Solo lista | Nuevos vs recurrentes, retorno, valor, quién dejó de venir. Falta apellido separado | F4.6 |
| **Reportes** | Ventas, resultado financiero y **consolidado de todas las sedes**, con desglose de cuánto puso cada una ✅ (D-194) | Nivel 2 y 3, métodos de pago, comparación entre períodos | F4.7 |
| **Estilistas** | Lista y configuración | Producción por persona | F4.9 |
| **Servicios** | Catálogo y precios | Cuáles dejan más dinero. Foto por servicio | F4.9 |
| **Usuarios** | Invitar y gestionar. **El correo de invitación ya llega** (H-12 cerrado el 10-ago) y **un estilista no puede tener dos cuentas activas** (R, cerrado el 11-ago) | Separar el texto de la pantalla de acceso (**S**) y **poder dar acceso a una segunda sede** (**V**) | Fase 4 |
| **Inventario / Compras / Gastos** | Funcionan | Pulido visual | F4.8 |
| **Fotos de trabajos** | ✅ Privadas hasta aprobar, papelera. **Ya van amarradas al ticket, al cliente y al estilista** | **No se ve el número de ticket** en la galería; sin filtros por cliente ni por estilista; flujo de captura sin definir; tipos de foto confusos | F4.9, F5.7 |
| **Reseñas** | Funcionan | Respuestas asistidas | F6.3 |
| **Configuración** | Completa, con tema y versión | Numeración de ticket ajustable; explicar "Subir portada" | F4.9 |
| **Panel de plataforma** | Solo lectura de negocios | Rediseño completo, aprobar clientes, tarifas, referidos | F3.7, F7 |
| **Mi agenda / Mis fotos / Mis reseñas / Mi panel** *(estilista)* | Funcionan | Pulido | F4.9 |

---

## 5. LA RUTA

### FASE 0 — Que exista en internet ✅ CERRADA (06-ago)

Dominio comprado, Cloudflare Pages publicando en cada `push`, HTTPS,
instalable como app. **Costo real: ~12 USD al año.**

### FASE 1 — Que sea seguro compartirla ✅ CERRADA (06-ago)

Tope antiabuso en la reserva pública, rol Asistente con sus pantallas, 2FA.

### FASE 2 — Seguridad y red de protección ✅ **CERRADA (12-ago)** — 7 de 7

> **El paso 2.2 encontró que el respaldo estaba incompleto desde el 08-ago**
> (hallazgo Y): no incluía el esquema `private`, donde viven las 18 funciones
> que autorizan cada operación. **Se habrían recuperado todos los datos con la
> aplicación inutilizada.** Corregido y verificado el mismo día.
>
> Queda desbloqueada la mitad automática de H-03 (D-121).

| # | Paso | Quién | Estado |
|---|---|---|---|
| 2.1 | **Rotar las claves `service_role` y `secret`** (H-04). Expuestas el 03-ago | 👥 | ✅ **CERRADO 09-ago (D-127).** Se borró la clave secreta y se **desactivaron** las claves antiguas — mejor que rotarlas. Verificado de extremo a extremo en producción |
| 2.2 | Restaurar un respaldo de ensayo en un segundo proyecto gratuito (D-111) | 👥 | ✅ **CERRADO 12-ago (D-134).** Se creó `salonymas-ensayo`, se restauró y se comparó con un censo de 37 cifras: **36 idénticas**. La única diferencia es el registro de archivos de Storage (hallazgo Z). **Encontró que el respaldo llevaba 4 días incompleto** (hallazgo Y) |
| 2.3 | **Verificar el dominio en Resend** (H-12) | 👥 | ✅ **CERRADO 10-ago (D-128).** Eran tres problemas encadenados; el de fondo era una dependencia anclada con rango. *(El mismo arreglo en `send-low-stock-alert` ya se aplicó: paso 2.7.)* |
| 2.4 | Cerrar los almacenes de archivos (H-09) | 🤖 | ✅ D-119 |
| 2.5 | Marcar "Naguara de Uñas" como negocio de prueba | 🤖 | ✅ D-120 |
| 2.6 | Pruebas de las 3 reglas de dinero y de los roles (H-03) | 🤖 | ✅ D-121 — la mitad automática espera a 2.2 |
| 2.7 | **Arreglar `send-low-stock-alert`** — estaba rota por la misma causa que D-128 (hallazgo T) | 🤖 | ✅ **CERRADO 11-ago (D-131).** Arreglada, publicada (versión 6) y **verificada de extremo a extremo por el propietario**: consumo interno de 6 unidades de "Gel para Peinar" → quedó en 3 con mínimo 5 → **el correo llegó** desde `hola@salonymas.com` con el producto, la sede y las cantidades correctas |

### FASE 3 — Poder cobrar — *el camino corto al primer cliente*

| # | Paso | Quién | Estado |
|---|---|---|---|
| 3.1 | Confirmar con ePayco si admite **cobros recurrentes** | 👤 | ✅ **CONFIRMADO 09-ago: la cuenta sí admite cobros recurrentes.** Era el mayor riesgo de esta fase y quedó descartado |
| 3.2 | Consultar a un contador las obligaciones al facturar (DIAN, IVA) | 👤 | ⬜ |
| 3.3 | Términos de servicio y política de privacidad (Ley 1581) | 👥 | 🔄 **Contenido técnico construido 17-ago (D-144).** `TermsAndPrivacyPage`, enrutamiento público, checkbox obligatorio en registro, 113/113 pruebas. **Falta la mitad no técnica:** revisión de un abogado colombiano antes de tratarlo como vinculante |
| 3.4 | Subir Supabase a plan Pro (~25 USD/mes) | 👤 | ⬜ |
| 3.5 | Cargar los 3 planes con precios de lista y límites | 🤖 | ✅ **CERRADO 12-ago (D-136).** Precios en **pesos** (160/200/240 mil), columna renombrada a `price_cop`, y **se crearon "sedes" y "cuentas de equipo"**, que no existían como capacidad — sin ellas no se podía ni escribir *"3 sedes pero 25 cuentas"*. **Y se hacen cumplir**: antes `create_branch` no tenía ni una comprobación ⚠️ **Superado el 01-sep por D-188:** ya no hay tres planes, hay uno — *Todo Incluido*, $150.000 por sede (D-189). |
| 3.6 | **Precio y descuento por cliente** en la suscripción (columnas nuevas) | 🤖 | ✅ **CERRADO 12-ago (D-136).** Precio propio **o** descuento con fecha de fin, motivo obligatorio, y marca de pionero. **El pionero es un descuento sin fecha de fin**: 50% mientras siga activo. La regla de cuánto paga vive en `beautyos_precio_efectivo`, un solo sitio ⚠️ **Superado:** el pionero dejó de ser un 50% con D-221 (07-sep) y el precio por cliente pasó a ser **de cada sede** con D-252 (22-sep). Esta fila es historia, no regla. |
| 3.7 | **Filtro de aceptación:** formulario, estado `pending`, aprobar/rechazar. La prueba gratis **empieza al aprobar** | 🤖 | ✅ **CERRADO 16-ago (D-138).** Formulario con cuestionario (ciudad, sedes, equipo, medio), estado `pending` sin tiempo de prueba, aprobación con plan / 50% Pionero, rechazo con motivo, y pantalla de espera/rechazo para el usuario. 98/98 pruebas ⚠️ **Superado:** aprobar ya no aplica *"50% Pionero"* (D-221, D-252); el precio negociado se escribe en la sede principal. |
| 3.8 | Pantalla pública de planes (`list_public_plans` ya existe, dormida) | 🤖 | ✅ **CERRADO 17-ago (D-140).** Pantalla pública responsiva de planes y precios en COP (`PublicPlansPage`) conectada a `list_public_plans()` y catálogo de respaldo (D-124, D-136). Comparativa de características, FAQ, badges de prueba de 21 días / sin tarjeta de crédito, enrutamiento público `?planes=1` / `?pricing=1` y enlaces desde login y registro. 100/100 pruebas en verde ⚠️ **Superado por D-188:** la pantalla pública enseña un solo plan. |
| 3.9 | **ePayco con confirmación en el servidor.** Nunca creerle al navegador | 🤖 | ✅ **CERRADO 17-ago (D-141).** Edge Function `epayco-webhook` que valida la firma criptográfica SHA-256 en servidor con credenciales privadas (cero secretos en cliente). Idempotencia estricta en base de datos mediante `UNIQUE(provider, provider_event_id)` en `subscription_events` |
| 3.10 | Pago → suscripción: activar, renovar, manejar el fallido | 🤖 | ✅ **CERRADO 17-ago (D-141).** RPC `private.beautyos_procesar_evento_epayco` (solo `service_role`) que transiciona de inmediato a `active` por 1 mes al confirmar pago (reactivación automática). Si falla un cobro en negocio activo, pasa a `past_due` con 5 días de gracia (D-141). Botón de checkout multimetodo ePayco en Flutter (`EpaycoCheckoutService`), banner de gracia con cuenta regresiva día a día en `main.dart`, y tarjeta de Suscripción en Configuración. 103/103 pruebas en verde. **Migrado a Smart Checkout V2 el 23-ago** (Edge Functions `create-epayco-session` y `verify-epayco-transaction`, sesión segura en backend en vez de URL directa de checkout, commits `a73562f`…`47bc121`, no registrados individualmente). **Auditoría del mismo día (D-159) encontró y corrigió una regresión crítica**: la migración de esa tarde había eliminado la validación de monto contra el precio pactado (cualquier pago ≥ $10.000 COP activaba cualquier plan), dejaba que el cliente controlara `tenantId`/monto, tenía un fallback que podía activar el negocio equivocado, y un nombre de columna erróneo que habría hecho fallar todo evento de pago. Corregido en `20260823130000_epayco_validar_precio_por_plan.sql`, **aplicada en Supabase y verificada con el Control 179 contra la base real el 23-ago** (rechaza pago insuficiente, activa con el precio correcto). **El mismo día, a pedido del propietario (D-160), se rediseñó el ciclo de facturación:** de "reinicia el período en cada pago" a ciclos de 30 días anclados al primer pago (renovación anticipada acumula al final del período vigente, pago tardío en gracia prorratea hacia la próxima ancla, reactivación tras suspensión reancla — salvo suspensión manual del owner, que bloquea la reactivación por pago), con precedencia absoluta del plan/precio pactado desde el panel (`price_reason is not null` cubre tanto precio fijo como solo-descuento). Corrige de paso dos bugs: `plan_id` nunca se actualizaba al pagar, y el candado de pactado no cubría descuento-sin-precio-fijo. Migración `20260823150000_ciclo_facturacion_ancla_y_plan_pactado.sql`, **aplicada en Supabase y verificada con el Control 180 (9 pruebas) contra la base real el 23-ago**; Edge Function `create-epayco-session` redesplegada (v7). `git push` hecho y **confirmado en producción el 23-ago**: el texto nuevo del checkout con precio pactado ya aparece en el `main.dart.js` publicado en `salonymas.com` |
| 3.11 | Avisos por correo **10, 5 y 3 días** antes de vencer | 🤖 | ✅ **CERRADO 17-ago (D-143, disparador diario D-145).** Edge Function `send-subscription-expiry-alerts` con Resend (`hola@salonymas.com`), tabla `subscription_notification_logs` con filtro anti-spam diario, cuenta regresiva diaria durante los 5 días de gracia, botón directo de pago en ePayco y suspensión automática de cuentas que agotaron la gracia (`private.beautyos_suspender_suscripciones_vencidas`). **Disparador diario real** con `pg_cron`+`pg_net` (08:00 hora Colombia), secreto en Supabase Vault — requiere el paso manual de `vault.create_secret(...)` con el valor real de `CRON_SECRET`, fuera de git. 106/106 pruebas en verde |
| 3.12 | 🔴 **Se adelantó a la Fase 2 a propósito** — la regla de oro pide anotar por qué: el tope de correos se agota probando, así que **bloqueaba las pruebas de ese día**, no solo las ventas de mañana. **Que los correos de cuenta salgan por Resend, no por Supabase.** El *"Confirma tu correo"* del registro lo mandaba el servicio interno de Supabase (`noreply@mail.app.supabase.io`), con un **tope diario bajísimo** que el propietario agotó el 10-ago probando (`email rate limit exceeded`) | 👥 | ✅ **CERRADO 11-ago (D-133).** SMTP de Supabase Auth apuntando a Resend. **Verificado de extremo a extremo:** el correo llegó desde `hola@salonymas.com`, Resend lo registró con `200`, y el registro se completó. El tope subió solo a 30/hora. **Queda el hallazgo W: esos correos están en inglés** |
| 3.13 | **Traducir las plantillas de correo de cuenta** (hallazgo W) — confirmar registro, invitación, recuperar contraseña, cambio de correo y reautenticación | 👥 | ✅ **CERRADO 17-ago (D-146).** Las 6 plantillas redactadas en español con la marca de Salón y Más, listas para copiar/pegar en `docs/02_operacion/PLANTILLAS_CORREO_AUTH.md`. **Falta el paso manual, fuera de git:** pegarlas una por una en Authentication → Emails → Templates del panel de Supabase |

### FASE 4 — Pulido módulo a módulo ✅ **CERRADA (22-ago)**

| # | Paso | Quién | Estado |
|---|---|---|---|
| 4.1 | Número de ticket consecutivo y ajustable | 🤖 | ✅ D-117 |
| 4.2 | Las dos funciones del tablero de agenda | 🤖 | ✅ **CERRADO 17-ago (D-147).** RPCs `get_ticket_board_counts_v2` y `get_ticket_board_list_v2` con granularidad dinámica (15m, 30m, 1h, día), zona horaria `America/Bogota`, lista Nivel 2 con consecutivo `#0000701`, cliente, teléfono para WhatsApp directo y saldos. 5/5 pruebas SQL en verde (Control 173). **Ampliado el 27-ago (D-163):** la tarjeta del Nivel 2 abre con un toque la Ficha Completa Nivel 3 del ticket (ver detalle en el paso 4.5) |
| 4.3 | **El tablero:** día, semana, mes + buscador múltiple + refresco automático | 🤖 | ✅ **CERRADO 17-ago (D-148, corregido D-149).** `AgendaPage` en Flutter con 3 vistas (Día con granularidad de 15 min, Semana, Mes), banner de regla del cero y cancelaciones/no asistió (D-101), cuadrícula con ceros atenuados, lista de Nivel 2 con consecutivo `#0000701`, WhatsApp directo (sin "+", D-149) y Realtime híbrido. **Días pasados atenuados en Semana/Mes** (D-101, D-149). 123/123 pruebas en verde |
| 4.4 | **Número de venta** al cerrar el ticket (hallazgo P) | 🤖 | ✅ **CERRADO 17-ago (D-150, corregido D-151).** Separación estricta de consecutivo operativo (`ticket_code` D-117) y venta contable (`sale_code` ej. `VTA-0000001` / DIAN). Tabla `branch_sale_numbering` por sede, triggers atómicos e inmutables, backfill histórico y chips duales en Nivel 2. **Inmutabilidad reforzada frente a reapertura** (`void_ticket_payment`, D-151): un ticket que ya tiene número de venta lo conserva sin importar cuántas veces se reabra y se vuelva a cerrar. 128/128 pruebas Flutter, 7/7 en el Control 174 |
| 4.5 | Tickets: pulido del nivel 2 y 3 + cambiar `TicketStatusBadge` por `StatusPill` (hallazgo N) | 🤖 | ✅ **CERRADO 18-ago (D-152).** Buscador universal (cliente, celular, `#cita`, `VTA-venta`, servicios, estilistas), selector temporal rápido (Hoy, Semana, Mes, Rango), chips de estado semánticos con conteo en vivo, filtro por estilista, chips duales correlativos, enlace directo a WhatsApp sin `+`, eliminación total de `TicketStatusBadge` adoptando `StatusPill`, y modal responsivo Nivel 3 (`_TicketDetailSheet`) con scroll continuo organizado en 6 tarjetas modulares. 132/132 pruebas en verde. **Ampliado el 27-ago (D-163)** a pedido del propietario: la tarjeta del Nivel 2 de Agenda abre esta misma Ficha Completa con un toque (sin pasar por la pestaña de Tickets); el `StatusPill` del header ahora es un botón con tooltip "Cambiar estado" que dispara el diálogo de cambio de estado; y el salón puede cobrar abonos/anticipos desde que la cita se solicita, no solo en `finalizado`/`cerrado` — el bloqueo real estaba en el RPC `register_ticket_payment` (exigía `finalizado` y cerraba la cita + generaba comisiones apenas el saldo llegaba a 0), corregido con un helper compartido (`private.beautyos_close_ticket_if_fully_paid`) que solo cierra y genera comisión cuando el ticket ya está `finalizado` de verdad. Migración `20260827100000_abonos_en_citas_activas.sql` y control `183_test_abonos_en_citas_activas.sql` (5 casos en `ROLLBACK`) escritos y revisados; **pendiente que el propietario los aplique en Supabase**. 157/157 pruebas en verde *(Nota del 23-sep: esa migración **sí está aplicada** — D-250 usa `beautyos_close_ticket_if_fully_paid` en vivo.)* |
| 4.6 | Clientes: análisis de retorno y valor. Decidir si se separa el apellido | 🤖 | ✅ **CERRADO 18-ago (D-153).** Se mantiene nombre comercial unificado con helper extractor reactivo `firstName` para WhatsApp. `get_clients_management_summary()` con métricas RFM: visitas, gasto total acumulado, ticket promedio, cadencia de visita ("cada ~X días"), última cita y segmentación automática (VIP, Recurrente, Nuevo, En riesgo +45 días). `ClientesPage` con buscador universal, chips de segmentación con contadores, `ClientRow` con WhatsApp contextual y ficha de detalle Nivel 3 (`_ClientDetailSheet`). 138/138 pruebas en verde |
| 4.7 | Reportes: nivel 2 y 3, métodos de pago, comparación | 🤖 | ✅ **CERRADO 18-ago (D-154).** Selector temporal dinámico (Hoy, Esta semana, Este mes, Rango personalizado), desglose de cobros por método de pago (Efectivo, Transferencias Nequi/Daviplata, Tarjetas, Otros), arqueo de efectivo real en caja, comparación de tendencias vs período anterior (con guardián para negocios nuevos) y modales de drill-down Nivel 3 para comisiones por estilista y ventas por servicio. 141/141 pruebas en verde |
| 4.8 | Inventario, Compras y Gastos: pulido visual. **Y el plural de las unidades en el correo de stock bajo**: hoy dice *"quedó con 3 unidad"* y *"el mínimo (5 unidad)"*, porque la unidad se guarda en singular. Con unidades como `ml` o `gr` se lee bien; con `unidad` no. Visto el 11-ago en el correo real | 🤖 | ✅ **CERRADO 18-ago (D-155).** Corrección gramatical unificada (`formatUnitQuantity`) en Edge Function de alerta de stock bajo y modelos de Flutter (`1 unidad` vs `3 unidades` / unidades métricas intactas). Modernización integral de `InventarioPage`, `ComprasPage` y `GastosPage` con buscadores universales reactivos, chips de filtrado con contadores dinámicos y `AppColors.surface`. 146/146 pruebas en verde |
| 4.9 | Servicios, Estilistas y Configuración: pulido, producción por persona, numeración ajustable, tipos de foto. **Y la galería de fotos: mostrar el número de ticket y poder filtrar por cliente y por estilista** | 🤖 | ✅ **CERRADO 18-ago (D-156).** Galería de fotos (`FotosTrabajosPage`) con chip de cita `#0000701` conectado a BD, buscador y filtros por estilista y cliente. Servicios (`ServiciosPage`) y Estilistas (`EstilistasPage`) modernizados con buscadores interactivos, chips por categorías/especialidades y métricas. Configuración (`ConfiguracionPage`) con `SaleNumberingCard` y diálogo para prefijo/número/resolución DIAN con preview en vivo (`previewNextCode`) y políticas de fotos de trabajo. 151/151 pruebas en verde |
| 4.10 | **Barra inferior de celular con acciones**, no módulos (hallazgo D) + **Rediseño integral de Shell, Header blanco minimalista y Sidebar categorizado estilo WeiBook/Fresha** | 🤖 | ✅ **CERRADO 22-ago (D-157).** Rediseño integral de la experiencia visual y arquitectura de información en `lib/main.dart`: (1) Header superior minimalista en fondo blanco (`AppColors.surface`) con borde sutil, isotipo degradado de Salón y Más/BeautyOS, selector de sede en píldora interactiva (`_BranchSelectorPill`), botón de Acción Rápida Global destacada (`+ Nueva Cita`), badge de prueba/gracia minimalista (`_TrialHeaderBadge`) y avatar de usuario con popup de seguridad/cierre de sesión. (2) Sidebar de escritorio categorizado (`_CategorizedSideMenu`) en 4 grupos semánticos con subtítulos (`OPERACIÓN`, `FINANZAS Y GESTIÓN`, `PORTAFOLIO`, `CATÁLOGO Y AJUSTES`), items ergonómicos redondeados con indicador activo. (3) Navegación móvil (`_MobileNavBar`) con los 4 destinos clave (`Agenda`, `Tickets`, `Clientes`, `Dashboard`) y modal "Más" categorizado. 152/152 pruebas en verde. |
| 4.11 | Rediseño del panel de plataforma (hallazgo O) + **Ficha Completa Nivel 3 de Negocio (bosquejo del propietario) y selector interactivo de planes en Checkout ePayco** | 🤖 | ✅ **CERRADO 22-ago (D-158).** Modernización integral de `PlatformPanelPage` (`lib/pages/platform_panel_page.dart`): (1) Header blanco con isotipo, (2) Banner superior de 5 KPIs cuantitativos interactivos (*Total salones, Por aprobar, Activos, En prueba, Gracia/Mora*), (3) Buscador universal en vivo por salón, titular, WhatsApp, correo o ciudad con chips de filtro rápido, (4) Tarjetas `_TenantCard` con botones rápidos de WhatsApp y llamada telefónica, (5) Ficha Completa Nivel 3 (`_TenantDetailSheet`) fiel al bosquejo del propietario (Datos de contacto, Plan y Tarifa fijada en COP, Botonera de gestión y Tabla de historial de pagos/periodos), (6) `EpaycoCheckoutService` con selector de planes interactivo (Básico, Business, Profesional) con cálculo automático del 50% Pionero. 156/156 pruebas en verde. **Ampliado el 23-ago (D-161)** a pedido del propietario tras pruebas reales y un segundo bosquejo suyo: la Tarjeta 1 de la Ficha Nivel 3 gana el botón "Editar Contacto" (RPC `platform_update_tenant_contact`, solo `platform_owner`) y muestra el nombre real del titular (`contact_name`, antes fingido con `correo.split('@').first`); la Tabla de historial (Tarjeta 4) pasa de dos filas sintéticas en memoria a consumir `platform_get_tenant_subscription_history` de verdad, con las 4 columnas del bosquejo (Fecha y Hora, Plan, Período Comprometido, Valor/Medio/Ref, con franquicia/banco/referencia de ePayco). El header (`main.dart`) gana una píldora verde con plan y fecha de corte cuando la suscripción está `ACTIVE`, pierde el botón suelto de "agregar sede" (reubicado en una tarjeta nueva de Configuración) y "+ Nueva Cita" abre el diálogo real en vez de solo cambiar de pestaña. El propio salón puede mantener su nombre de contacto titular, tipo de negocio, teléfono y WhatsApp desde Configuración (owner **o admin**) con la nueva RPC de autoservicio `update_tenant_contact_info`. Migración `20260823160000_contacto_titular_y_historial_completo.sql`, **aplicada en Supabase y verificada con el Control 181 contra la base real el 23-ago**. 156/156 pruebas en verde. **Refinado el mismo 23-ago (D-162):** la Tarjeta 1 se separa en A. Contacto Administrativo (agrega Teléfono, Ciudad e Instagram/Facebook de solo lectura) y B. Capacidad Operativa Real — en vivo, que reemplaza las "Sedes Estimadas"/"Equipo Estimado" del formulario de registro por conteos reales calculados en `platform_list_tenants()` (`real_branches_count` desde `branches.active`, `real_team_count`/`team_breakdown` desde `user_profiles`, con desglose por rol pluralizado, ej. "1 dueño, 2 admins, 3 estilistas"). El propio salón puede editar Instagram y Facebook desde Configuración (`update_tenant_contact_info` amplía de 4 a 6 parámetros). Migración `20260823170000_redes_sociales_y_equipo_real.sql`, **aplicada en Supabase y verificada con el Control 182 contra la base real el 23-ago**. 156/156 pruebas en verde. ⚠️ **Superado:** el selector de tres planes con el 50% lo retiró D-193 (02-sep), y el 50% salió del todo con D-252 (22-sep). |

### FASE 5 — La cara pública ✅ **CERRADA (29-ago)**

| # | Paso | Quién | Estado |
|---|---|---|---|
| 5.1 | Identificador único por negocio, sin ñ ni tildes (`salonymas.com/naguaradeunas`) | 🤖 | ✅ **CERRADO 27-ago (D-164).** Columna `slug` en `tenants`, `private.beautyos_slugify` (minúsculas, sin acentos ni eñe, separadores en guiones) y `private.beautyos_generate_unique_tenant_slug` (corto/reservado/largo/colisión con sufijo numérico). Backfill de los tenants existentes y `register_tenant` generándolo desde el nombre para los nuevos. Lista de palabras reservadas del sistema. Control `184_test_slugs_publicos.sql` |
| 5.2 | Función pública que lo resuelva sin sesión | 🤖 | ✅ **CERRADO 27-ago (D-164).** `get_public_salon_by_slug` (rol `anon`): solo datos de vitrina (nombre, tipo, logo, portada, tema, ciudad, dirección de la sede principal, WhatsApp, teléfono, Instagram, Facebook). `check_slug_availability` y `update_tenant_slug` (autoservicio, owner/admin) |
| 5.3 | Enrutado por ruta en Flutter y `_redirects` | 🤖 | ✅ **CERRADO 27-ago (D-164).** `web/_redirects` (`/* /index.html 200`, no existía) y `BeautyOSApp.build()` en `main.dart` resuelve el slug desde `Uri.base.pathSegments` (o `?salon=<slug>` de respaldo) antes de `AuthGate`, igual que las demás rutas públicas. `PublicSalonPage` nueva, con el mismo patrón visual que `PublicBookingPage` y el tema del negocio aplicado (D-093d) |
| 5.4 | Editarlo desde Configuración | 🤖 | ✅ **CERRADO 27-ago (D-164).** `PublicSalonLinkCard`: copiar, compartir por WhatsApp y "Modificar enlace" con comprobación de disponibilidad en vivo (debounce de 400 ms) contra `check_slug_availability`/`update_tenant_slug`. `get_business_settings` gana la columna `slug` |
| 5.5 | **La página del negocio:** portafolio, equipo, reseñas y reservar | 🤖 | ✅ **CERRADO 28-ago (D-165).** Cuatro RPC públicas nuevas (servicios, portafolio, equipo, reseñas) más `get_public_salon_by_slug` ampliada con `primary_branch_id` y `business_hours`. `PublicSalonPage` completa: encabezado con estrellas y botón "📅 Agendar Cita", servicios con "Reservar" que precarga la reserva, portafolio en grid con visor modal, equipo, reseñas y horarios/ubicación — secciones que se ocultan solas si no hay contenido. Control `185_test_perfil_publico_completo.sql`. 168/168 pruebas en verde. **Ampliado el 28-ago (D-166):** dirección física editable de la sede principal desde Configuración con botón "📍 Ver en Google Maps" en la página pública; `PublicBookingPage` retira "Conoce a nuestro equipo" (redundante con la página del negocio) y separa el servicio y el profesional en dos pasos (dropdown de servicio, luego dropdown de profesional con "Cualquiera disponible" combinando horarios de todos), botón renombrado a "Solicitar cita"; pantalla de éxito con WhatsApp al salón, Google Calendar y volver a la página del negocio. Migración `20260828180000` y control `186_test_direccion_sede_y_contacto.sql`. 168/168 pruebas en verde. **Migraciones `20260827160000` y `20260828180000` aplicadas en Supabase por el propietario.** |
| 5.6 | **Cuenta del cliente final:** ver sus fotos y su historial de visitas | 🤖 | ✅ **CERRADO 29-ago (D-167).** Portal "Mis citas y fotos" con PIN de 4 dígitos, asignado solo por el salón (nunca autoservicio — decisión de seguridad confirmada con el propietario). `client_portal_authenticate`/`get_client_portal_data` (RPC públicas, hash salado, bloqueo tras 5 intentos, token de 60 días) y `admin_reset_client_portal_pin` desde la Ficha del cliente. `ClientPortalPage`: próximas citas, fotos con visor modal (`PhotoGridViewer`, compartido con el portafolio de D-165) y calificar servicios pendientes. Control `187_test_habeas_data_y_portal_cliente.sql`. 176/176 pruebas en verde |
| 5.7 | **Permiso de publicación de la clienta** — hoy no existe el campo | 🤖 | ✅ **CERRADO 29-ago (D-167).** `work_photos.client_consent`/`client_consent_at`; `set_work_photo_portfolio_approval` rechaza publicar en portafolio sin consentimiento (Ley 1581); checkbox en `AddWorkPhotoDialog` e indicador visual en `FotosTrabajosPage`. Migración `20260829120000` **aplicada en Supabase y Control 187 (11 casos) en verde contra la base real**, tras corregir dos bugs reales encontrados al correrlo (ver D-167) ⚠️ **Cerrado a medias, visto el 18-sep (AQ):** la casilla de consentimiento la marca quien sube la foto, no la clienta. Se completa en el **9.48**. |

### FASE 6 — El plan Profesional: IA, WhatsApp y redes

*(El plan Profesional dejó de existir el 01-sep: todo va en Todo Incluido,
D-188. El nombre de la fase se conserva. Siguen abiertos 6.4 y 6.5, que
dependen de Meta; el 6.5 pasó a ser el 9.26.)*

| # | Paso | Quién | Estado |
|---|---|---|---|
| 6.1 | ⭐ **"Tu negocio en palabras"** — el Dashboard contado en un párrafo | 🤖 | ✅ **CERRADO 29-ago (D-168).** `NarrativaNegocioBuilder` (`lib/widgets/tu_negocio_en_palabras_card.dart`): motor narrativo determinista, sin IA, sobre los datos que el Dashboard ya carga (`DashboardHoy`/`DashboardOverview`) — saludo por hora (mañana/tarde/noche), ritmo de citas, avisos de confirmar/cobrar/reactivar, tendencia de ventas solo con historia suficiente, y tres chips de acción a Agenda/Tickets/Clientes. Sin migración: no consume datos nuevos. 197/197 pruebas en verde |
| 6.2 | **Estudio de publicación:** foto estandarizada + reseña + datos, lista para Instagram | 🤖 | ✅ **CERRADO 29-ago (D-169), versión determinista.** `get_publication_studio_data`: junta servicio(s) del ticket y una reseña real de 4-5 estrellas (si existe) para una foto ya aprobada para portafolio. Composición 100% en Flutter (`RepaintBoundary.toImage()`, sin servidor de imágenes): tarjeta 1080×1080 con foto + logo + servicio + reseña opcional + WhatsApp, descargable como PNG vía `data:` URI (sin paquete nuevo). **No conecta con Instagram** (eso es 6.4, aparte). **Decisión de producto del propietario:** abierto a todos los planes por ahora, no exclusivo de Profesional como preveía D-124 — se restringe el día que se sume mejora con IA real. Botón en la Galería de fotos, habilitado solo con la foto ya publicada. 205/205 pruebas en verde |
| 6.3 | Respuestas a reseñas asistidas | 🤖 | ✅ **CERRADO 29-ago (D-170), versión determinista.** `ReviewReplyDraftBuilder`: plantilla por franja de calificación (5★/4★/3★/1-2★) con el nombre de la clienta y el servicio, que el salón edita antes de guardar. `set_review_reply` (owner/admin) guarda/edita/quita la respuesta; se publica bajo la reseña en la página pública del negocio. No exclusivo de ningún plan. 216/216 pruebas en verde |
| 6.4 | Publicación automática en Instagram | 🤖 | ⬜ **Requiere revisión de Meta** |
| 6.5 | **WhatsApp con agente:** servicios, horarios, disponibilidad, agendar | 🤖 | ⬜ **Requiere verificación de empresa con Meta. Semanas** |
| 6.6 | Blog de artículos de belleza y estética | 🤖 | ✅ **CERRADO 29-ago (D-171).** Blog **por cada salón** (decisión confirmada con el propietario), sin url propia por artículo todavía. Tabla `blog_posts` (tenant, no sede), bucket `blog-covers`, RPC autoservicio (`create`/`update`/`delete_blog_post`, `get_blog_posts_summary`) y pública (`get_public_salon_blog_posts`, solo publicados). Módulo "Blog" nuevo (owner/admin) y sección "Blog" en la página pública (D-165) con `PublicBlogPostPage` (`Navigator.push`, mismo patrón que reserva/reseñas). 222/222 pruebas en verde |

> ⚠️ **Nada de la fase 6 se vende como disponible hasta que exista.** Cobrar
> por WhatsApp e Instagram sin haberlos construido es una promesa que no se
> puede cumplir, y con el primer cliente eso no se recupera.

**Reglas de la IA, para que no se olviden:**

1. **La IA embellece la foto, nunca el trabajo.** Mejorar luz y encuadre, sí;
   "arreglar" unas uñas para que se vean mejor de lo que quedaron, jamás.
2. **La llave de la IA vive en el servidor**, nunca en la aplicación.
3. **Se descarta "sugerir horarios" y "rellenar huecos"** por el mismo motivo
   por el que se descartó el porcentaje de ocupación (D-110, D-114): el horario
   se guarda por negocio y no por profesional, así que sería inventar precisión.

### FASE 7 — Tu panel de dueño de la plataforma ✅ **CERRADA (30-ago)**

| # | Paso | Quién | Estado |
|---|---|---|---|
| 7.1 | Ver cada cliente: desde cuándo, cuántos periodos ha pagado, cuánto debe | 🤖 | ✅ **CERRADO 30-ago (D-172).** Antigüedad, períodos pagados, LTV en COP, mora con monto adeudado y botón WhatsApp de cobro cordial. Migración `20260829200000` y Control `191` aplicados y en verde (21/21 aserciones). 238/238 pruebas Flutter |
| 7.2 | **Cambiar tarifas y límites por cliente** — pantalla sobre `tenant_feature_overrides`, que ya existe | 🤖 | ✅ **CERRADO 30-ago (D-172).** RPCs `platform_get`/`set`/`delete_tenant_feature_override` y Tarjeta 5 en la Ficha Nivel 3 para conceder/revocar excepciones (sedes, equipo). Misma migración y control 191 |
| 7.3 | **Sistema de referidos:** quién trajo a quién y qué comisión le corresponde | 🤖 | ✅ **CERRADO 30-ago (D-173).** Partners con código propio, comisión configurable (% o fijo; primer pago/N meses/recurrente), generación automática al pagar por ePayco, liquidación consolidada. Migración `20260830100000` y Control `192` aplicados y en verde (22/22 casos en Supabase). 257/257 pruebas Flutter |
| 7.4 | Métricas del negocio SaaS: activos, morosos, cancelados, ingreso mensual | 🤖 | ✅ **CERRADO 30-ago (D-172).** Cabecera ejecutiva del SaaS (`platform_get_saas_metrics`): MRR estimado, recaudo histórico, salud de cartera y conversión prueba→pago. Misma migración y control 191 |

**Además, D-173 unificó visualmente el Panel de Plataforma:** se retiró la
`_PlatformKPIBanner` (5 tarjetas blancas redundantes con las píldoras de
filtro) y el cuerpo pasó a dos pestañas ejecutivas: `🏪 Salones Clientes` y
`🤝 Partners y Referidos`.

### FASE 8 — Limpieza técnica, seguridad y pulido final a producción

| # | Paso | Quién | Estado |
|---|---|---|---|
| 8.1 | Eliminar las 42 funciones que nadie llama (H-05), en dos pasos | 🤖 | ✅ **CERRADO 30-ago (D-176).** 30 funciones huérfanas purgadas con `DROP FUNCTION IF EXISTS`; 6 funciones internas blindadas con `COMMENT ON FUNCTION` "NO ELIMINAR". Migración `20260830160000` y Control `195` en verde en Supabase |
| 8.2 | Unificar las reglas de las columnas de dinero (H-08) | 🤖 | ✅ **CERRADO 30-ago (D-175).** Bug de `round(x, 2)` corregido a `round(x)` en comisiones; 14 candados `CHECK (col = round(col)) NOT VALID` aplicados y diagnosticados con 0 violaciones históricas. Migración `20260830140000` y Control `194` en verde en Supabase |
| 8.3 | Alinear el permiso suelto de Storage (H-11) | 🤖 | ✅ **CERRADO 30-ago (D-174).** Revocado de public/anon en `beautyos_can_upload_work_photo(uuid)`, solo authenticated. Migración `20260830120000` y Control `193` aplicados y en verde (3/3 casos en Supabase) |
| 8.4 | **Blindar Edge Functions y sincronizar configuración (Hallazgo U):** `verify_jwt = true` para funciones con sesión de usuario y declarar funciones de Smart Checkout en `config.toml` | 🤖 | ✅ **CERRADO 30-ago (D-177).** `verify_jwt = true` en `send-invitation-email`, `send-low-stock-alert` y `create-epayco-session`; `verify_jwt = false` en `epayco-webhook`, `verify-epayco-transaction` y alertas. 6 funciones desplegadas en la nube y verificadas con rechazo 401 perimetral sin sesión |
| 8.5 | **Clarificar pantalla de acceso para colaboradores invitados (Hallazgo S):** separar camino de dueño ("Registra tu salón") frente a empleado invitado ("Inicia sesión con tu correo") | 🤖 | ✅ **CERRADO 30-ago (D-178).** Subtítulo neutro ("Ingresa a tu cuenta") en `login_page.dart` con tarjeta guía para colaboradores invitados y botón explícito "Registra tu negocio gratis"; `register_page.dart` titulado como "Registra tu negocio" con enlace de retorno para invitados. 259 pruebas en verde |
| 8.6 | **Gestión de sedes para usuarios de equipo multi-sede (Hallazgo V):** interfaz en Usuarios y RPC para asignar/revocar acceso a sedes (`branch_memberships`) a un colaborador existente | 🤖 | ✅ Cerrado (D-179) |
| 8.7 | **Reenvío de correos de soporte `hola@salonymas.com` (Idea I-12):** activación en Cloudflare Email Routing para recibir respuestas de salones en Gmail | 👥 | ✅ **Cerrado 30-ago (D-180).** Enrutamiento en Cloudflare hacia `juankdev2026@gmail.com` desacoplado de Resend (que envía por `send.salonymas.com`). Documentación operativa en `docs/02_operacion/CORREO_Y_DOMINIO.md` |
| 8.8 | **Broche de oro: Onboarding guiado "Primeros pasos"** — checklist interactivo de bienvenida en Dashboard para salones nuevos | 🤖 | ✅ **Escrito 01-sep (D-186).** `get_onboarding_progress` + `PrimerosPasosCard`: contador «X de 4», barra de avance y botón **Empezar** por paso. Los cuatro son servicios, equipo, horario y primera cita — el portafolio queda fuera porque desde D-184 es de plan Profesional. Desaparece sola al completarse. Migración `20260901180000` y Control `200`. 278/278 pruebas en verde. ✅ **CERRADO 01-sep: migración aplicada en Supabase y Control 200 en verde contra la base real, con `ROLLBACK` limpio** ⚠️ **Superado en dos cosas:** desde D-188 no hay plan Profesional —el portafolio es para todos— y desde D-251 los pasos son **cinco**. |
| 8.9 | 🔴 **Cerrar TL-01: `verify-epayco-transaction` activaba suscripciones con pagos hechos en otro comercio de ePayco.** Primer hallazgo de la auditoría de 4 revisiones del 01-sep | 🤖 | 🔄 **Escrito 01-sep (D-181).** `verify_jwt = true`, comparación de `x_cust_id_cliente` contra `EPAYCO_P_CUST_ID`, negocio resuelto desde `tenant_memberships` de quien llama, y eliminado el rastreo por prefijo de factura. `flutter analyze` 0/0 y 262/262 en verde. **Desplegado el 06-sep (v15). Sigue pendiente verificarlo contra una transacción real — pasa a ser el paso 9.7.** No cierra TL-02 |
| 8.10 | 🔴 **Cerrar TL-02: la firma de ePayco no cubre `x_extra1` ni `x_extra2`**, así que un pago propio legítimo se puede reenviar con el negocio cambiado. Necesita tabla de intenciones de pago generada en el servidor | 🤖 | ✅ **Escrito 01-sep (D-182).** Tabla `subscription_payment_intents` (RLS deny-all), `beautyos_registrar_intencion_pago` y `beautyos_resolver_intencion_pago` (solo `service_role`). `create-epayco-session` registra la intención **antes** del checkout; `epayco-webhook` resuelve por `x_id_invoice` y **falla cerrado**. Retirado el rastreo por prefijo de factura. Migración `20260901120000` y Control `197`. ✅ **CERRADO 01-sep: migración aplicada en Supabase, Control 197 en verde (9 de 9 casos contra la base real, con el ataque de TL-02 rechazado y `ROLLBACK` limpio), y las dos Edge Functions desplegadas en la nube en el orden correcto** |

| 8.11 | 🔴 **Cerrar TL-04 y TL-05: el portal de la clienta permitía enumerar qué celulares son clientas de un salón**, sin tope y sin adivinar ningún PIN (Ley 1581), y además sin índice sobre la búsqueda por dígitos del teléfono | 🤖 | ✅ **Escrito 01-sep (D-183).** Un solo mensaje para los cuatro casos de fallo, tiempo de respuesta igualado, e índice funcional `clients_tenant_phone_digits_idx`. Migración `20260901140000` y Control `198`. ✅ **CERRADO 01-sep: migración aplicada en Supabase y Control 198 en verde (9 de 9 casos contra la base real — oráculo eliminado, los cuatro mensajes idénticos, índice funcional verificado y `ROLLBACK` limpio)** |
| 8.12 | **Cerrar TL-06: el PIN del portal es `sha256` de una sola vuelta sobre 4 dígitos**, y 5 intentos errados bloquean a la clienta real sabiendo solo su celular | 🤖 | ✅ **Escrito 01-sep (D-185).** bcrypt con `pgcrypto`, **migración al vuelo** de los hashes heredados cuando la clienta entra (nadie pierde su PIN), y bloqueo escalonado 1/5/30 min en vez de 15 planos. Migración `20260901160000` y Control `199`. ✅ **CERRADO 01-sep: migración aplicada en Supabase y Control 199 en verde (9 de 9 casos contra la base real — bcrypt activo, migración al vuelo probada, bloqueo escalonado y `ROLLBACK` limpio)** |
| 8.13 | 🔴 **Cerrar TL-19: la interfaz nunca consulta `get_my_entitlements()`.** Un salón en Básico ve módulos que no tiene, entra, y recibe una excepción de PostgreSQL en crudo. Toda la escalera de precios es invisible dentro del producto | 🤖 | ✅ **Escrito 01-sep (D-184).** `EntitlementsService`, `requiredFeature` en `BeautyModule` y `PlanLockedPage`: el módulo **se ve con candado** (esconderlo mataría la venta) y explica qué gana al subir. Falla **abierto** a propósito. Acotado a Reportes, Inventario, Compras y Gastos, que son los que de verdad revientan (Fotos y Reseñas van aparte, en 8.14). Botón de WhatsApp a soporte con el mensaje pre-armado del módulo. ✅ **CERRADO 01-sep.** `flutter analyze` 0/0 y 271/271 pruebas en verde ⚠️ **Contexto superado por D-188:** ya no hay escalera de planes. Los candados vuelven con el 9.41 (módulos por sede) **y sugieren planes que ya no existen** (hallazgo **BD**). |
| 8.14 | **Candado por botón en Fotos de trabajos y Reseñas.** El Plan Maestro §3 las reserva al Profesional, pero `portfolio` solo protege `create_work_photo` y `reviews` solo `public_create_review`: bloquear el módulo entero escondería fotos que el salón ya tiene. Necesita candado en la acción, no en el módulo | 🤖 | ✅ **CERRADO 01-sep (D-187).** `mostrarCandadoDePlan` en los dos únicos sitios que abren esas acciones. **El caso de Reseñas era el peor:** el salón manda el enlace y es **la clienta** quien recibe el error. Al estilista se le da un mensaje distinto que al dueño. 279/279 pruebas en verde. **Cierra la auditoría de 4 revisiones** ⚠️ **Contexto superado por D-188** (no hay plan Profesional). Ver hallazgo **BD**. |

| 8.15 | 🔴 **Etapa 2 de D-188 — suscripción por sede.** Tabla propia con su estado y su ancla, la sede nace pendiente, `beautyos_precio_efectivo` por sede. Y el tope de equipo pasa a ser por sede: hoy `create_team_invitation` cuenta el tenant entero, así que la promesa de "10 por sede" (D-189) no es cierta para multi-sede hasta que se arregle aquí | 🤖 | ✅ **CERRADO 02-sep (D-190).** `branch_subscriptions` con estado, período y precio por sede; las sedes existentes **heredan lo que se les vendió** y solo las nuevas nacen pendientes; la plataforma las activa a mano hasta la Etapa 3. Migración `20260902120000` y Control `202`, en verde (9 de 9). **El tope de equipo por sede se resolvió en 8.18** (D-196); la pantalla del salón se resolvió en 8.16 |
| 8.16 | 🔴 **Etapa 3 de D-188 — ePayco por sede.** `create-epayco-session` y las intenciones de pago (D-182) con `branch_id`, prorrateo hasta la fecha ancla al activar a mitad de ciclo (reutiliza D-160), alertas y panel de plataforma por sede | 🤖 | ✅ **CERRADO 02-sep (D-191, D-192, D-193).** 3a: `branch_id` en las intenciones y `beautyos_calcular_cargo_sede` (migración `20260902140000`, Control `203`). 3b: `beautyos_procesar_pago_de_sede`, checkout y webhook con `branchId` (migración `20260902160000`, Control `204`), y la pantalla de sedes en Configuración con su estado y botón de pago — de paso se corrigió `epayco_checkout_service.dart`, que desde el 01-sep enseñaba los tres planes retirados con sus precios viejos. **Ya se puede vender la segunda sede.** Las alertas de vencimiento por sede se resolvieron en 8.18 (D-196) |
| 8.17 | **Bloque 1 "Velocidad Operativa de Mostrador": cita express, cobro directo y WhatsApp pre-armado.** Encargo directo del propietario citando UX-01/UX-03/UX-04/UX-05/UX-10 de la Revisión 2; esa numeración no coincidía con la de la auditoría verificada (UX-04 ahí es Onboarding, UX-05 quedó refutado) y se aclaró con el propietario antes de tocar código | 🤖 | ✅ **CERRADO 02-sep (D-195).** Cita express reutiliza "Cualquiera disponible" (D-166) en vez de saltarse el cálculo de disponibilidad, cerrando la mitad barata del hallazgo C-01. Botón "Cobrar" nuevo en la tarjeta de Agenda, mismo mecanismo que D-163, abriendo el pago directo sin la Ficha Completa (la lista de Tickets ya lo hacía, por eso UX-05 estaba refutado). WhatsApp con mensaje pre-armado vía `buildAppointmentReminderMessage`. `flutter analyze` 0/0 y **297 de 297 pruebas en verde** (5 nuevas). Sin migración: es un bloque de interfaz, no toca base de datos |
| 8.18 | 🔴 **Bloque 2 "Pulido Multi-Sede y Alertas de Suscripción": tope de equipo por sede y alertas de vencimiento por sede.** Los dos pendientes que D-189 (apartado del tope) y el HANDOFF de D-195 (apartado 3.1, alertas) dejaron escritos, ahora que la Etapa 2/3 de D-188 ya existe | 🤖 | ✅ **CERRADO 02-sep (D-196): aplicado en Supabase y verificado en producción.** `private.beautyos_require_team_limit` multiplica el tope (9) por las sedes activas: la principal cuenta si el negocio está `entitled` (cero regresión), las secundarias por su propio `branch_subscriptions.status`. `send-subscription-expiry-alerts` agrupa alertas de negocio y de sedes secundarias en un solo correo por dueño, y ya está desplegada en producción. Migración `20260902200000` aplicada y **Control `206` en verde: 10 de 10 casos, con `ROLLBACK` limpio** (tope multi-sede proporcional, cero regresión en la principal, alertas de sede agrupadas, y los permisos verificados). `flutter analyze` 0/0 y 297/297 pruebas en verde (sin cambios en Flutter: el bloque es de base de datos y Edge Function). No se construyó suspensión automática por sede: el encargo pedía alertas, no suspensión |
| 8.19 | **Bloque 3 "Pipeline de Integración Continua": `.github/workflows/ci.yml` corriendo `flutter analyze` y `flutter test` en cada push a `main` y cada pull request.** Cierra TL-07 de la auditoría técnica del 01-sep | 🤖 | ✅ **CERRADO 02-sep (D-197).** Flutter fijado en `3.44.2`/`stable` (misma versión de desarrollo). Sin secretos: la `publishableKey` es pública por diseño (TL-07 lo confirmó) y las pruebas usan servicios falsos, nunca la base real. `flutter analyze` sin `--fatal-infos` a propósito: hay 2 infos preexistentes que habrían roto el primer run sin que nadie tocara nada |
| 8.20 | **Bloque 4 "Función canónica de moneda": una sola `formatCOP`, corregida para negativos, reemplazando duplicados.** Cierra TL-12 de la auditoría técnica del 01-sep | 🤖 | ✅ **CERRADO 02-sep (D-198).** `formatCOP` (`lib/models/ticket_board.dart`) es ahora el único formateador de pesos del proyecto. Antes de tocar código se buscó el mismo patrón de bug más allá de los 7 `_formatCop` que cita TL-12, y aparecieron 6 copias más bajo `formatMoney`/`_formatMoney` con el mismo defecto — se le devolvió la decisión de alcance al propietario, que aprobó incluirlas: **13 archivos migrados en total**, ninguno con alias de compatibilidad. El bug: el signo `-` contaba como un dígito más para el separador de miles (`$-.100` en vez de `-$100`), tocando utilidad neta, saldos y comisiones. `flutter analyze` 0/0 y **301 de 301 pruebas en verde** (4 nuevas cubriendo negativos, decimales y millones) |
| 8.21 | ⚠️ **Corregido después por D-203 (paso 8.26): al retirar el respaldo se cayó Tickets en producción — ver ahí antes de fiarse de esta fila.** **Bloque 5 "Rendimiento, Resiliencia y Optimización de Carga": el error que se tragaba solo, las fotos sin comprimir y la lista de Tickets sin techo.** Cierra TL-16, TL-20 y TL-09 de la auditoría técnica del 01-sep, y C-03 de los hallazgos propios | 🤖 | ✅ **CERRADO 02-sep (D-199).** **TL-16:** fuera el `catch (_)` de `tickets_service.dart` **y el respaldo entero a `get_tickets_summary_v2`** — dejar la caída viva era seguir teniendo un segundo camino que tapa el fallo del primero; ahora un corte de red o un rechazo de permisos de sede se ve, en vez de disfrazarse de "no hay tickets". **TL-20:** los 5 servicios de subida comprimen a 1920 px y calidad 85, con la política en **un solo archivo** (`lib/services/image_compression.dart`) en vez de repetida cinco veces — la lección de D-198 aplicada antes de que duela. Se comprobó antes de aplicarlo que no rompe la transparencia de un logo PNG. **TL-09:** se avisó **antes de tocar el archivo** de que el `ListView.builder` que pedía el encargo no habría ahorrado nada (`AppPage` es un `SingleChildScrollView`, y `shrinkWrap` construye igualmente todos los hijos); el propietario eligió entre las tres salidas reales y fijó **tandas de 10 con botón "Ver más"**. **C-03:** `test/salto_agenda_tickets_test.dart` blinda las dos mitades de la adyacencia Agenda→Tickets de D-163 — que Tickets siga siendo el módulo siguiente, y que todo rol que ve Agenda vea también Tickets, que es la que se rompe sin ruido. `flutter analyze` 0/0 y **313 de 313 pruebas en verde** (12 nuevas). Sin migración SQL ni Edge Function. **Queda abierto el otro tercio de TL-09:** la consulta sigue trayendo el historial completo (`p_start_date: null`); este paso acota lo que se pinta, no lo que se trae |
| 8.22 | **Bloque 6 "Velocidad de Caja y Feedback de Pasarela": atajo de saldo exacto al cobrar, el retorno de ePayco deja de ser mudo, y limpieza de `TicketRow`.** Cierra UX-07 (la mitad de interfaz) y C-02 de la auditoría del 01-sep | 🤖 | ✅ **CERRADO 02-sep (D-200).** Bloque enteramente de interfaz: sin migración SQL ni Edge Function. **Tres premisas del encargo no coincidían con el código y se dijeron antes de escribir nada:** `_TicketPaymentDialog` no existe (hay un solo diálogo de cobro), `_verifyPendingEpaycoTransaction` tampoco (la verificación está en línea dentro de `_loadHomeContext`), y el campo de monto **ya venía relleno** con el saldo desde D-163 — lo que faltaba era la vuelta atrás tras teclear un abono parcial, no el valor inicial. **UX-07:** `ActionChip` "Pagar saldo exacto ($150.000)" encima del campo, con `formatCOP` (D-198); rellena el entero exacto y pasa el foco a "Registrar pago". `PaymentsDialog` pasó a ser público para poder probarlo, igual que `TicketRow` (H-03). **ePayco:** el resultado de `verify-epayco-transaction` ya no se tira. **Un fallo de esa llamada NO se le cuenta al dueño como pago fallido**, porque la vía autoritativa es el webhook (D-141) y activa igual: los cinco caminos de fallo dicen "estamos validando", nunca "error". Y solo se promete "suscripción activa" cuando la base devuelve `newStatus == active`, porque un negocio en revisión registra el pago sin reactivar (D-125/D-138). **C-02:** `TicketRow` baja de 8 callbacks obligatorios a 3. Ninguna acción se perdió — las seis retiradas viven en la Ficha Completa — y la condición que se quitó era demostrablemente redundante, con prueba que lo fija. `flutter analyze` 0/0 y **333 de 333 pruebas en verde** (20 nuevas). **Queda abierta la otra mitad de UX-07:** distinguir Nequi de Daviplata es migración + control SQL + recalcular Reportes, porque los métodos de pago son un `CHECK` de base de datos, no una lista de interfaz (C-04) |
| 8.23 | **Bloque 7 "Ciclo de vida reactivo de pestañas": carga perezosa de módulos y refresco al entrar.** Cierra **TL-10** y, con él, el **Hallazgo Q** — abierto desde el 09-ago | 🤖 | ✅ **CERRADO 03-sep (D-201).** `IndexedStack` construía **los 16 módulos a la vez** al abrir sesión, y por eso volver a una pestaña no recargaba nada: nunca se había ido. Ahora un módulo no se construye hasta que se entra en él, y volver a entrar lo remonta. **El mecanismo ya estaba en el proyecto:** 17 de los 19 módulos llevan `ValueKey('...-branchId')` y se remontan al cambiar de sede; D-201 le añade un contador de visitas. Se descartó la interfaz `Recargable` con `GlobalKey` (habría conservado filtros, pero obligaba a tocar las 16 páginas). **Toda la navegación pasa por un embudo único** (`_irAIndice`): había cuatro caminos sueltos y con carga perezosa uno que no marque el módulo deja la pantalla en blanco. Hay prueba que falla si aparece un quinto. **Y el módulo visible se construye siempre**, esté o no marcado, para que la pantalla en blanco sea imposible por construcción. **Configuración es la única excepción al remontaje** (elección del propietario entre tres opciones): siete campos de texto en la propia página. **Coste aceptado:** Tickets pierde sus filtros y el "Ver 10 más" de D-199 al volver. Incluye **C-05**: fuera los desplegables "Sedes" y "Equipo" del registro, huérfanos desde D-162 — verificado antes que ninguna pantalla los muestra y que la RPC los deja en 1 por defecto. `flutter analyze` 0/0 y **341 de 341 pruebas en verde** (8 nuevas, que esta vez cuentan montajes reales en vez de leer el código). Sin migración SQL **✅ VERIFICADO A MANO POR EL PROPIETARIO el 04-sep:** recorrido completo de los 16 módulos en producción, el cambio de sede actualiza los datos sin fallos y el flujo Agenda → Tickets → Cobro → Reportes va limpio. **Cero pantallas rojas.** Estuvo siete sesiones esperando esta comprobación, que ninguna prueba podía hacer: ninguna monta la aplicación real con sesión (regla 21) |
| 8.24 | **Bloque 8 "Blindaje de cabeceras web de seguridad": anti-clickjacking y permisos de navegador en `web/_headers`.** Cierra la parte de **TL-03** que se puede cerrar desde el repositorio | 🤖 | ✅ **CERRADO 04-sep (D-202)** — *con una casilla de 👤 abierta, ver abajo.* Se añaden `X-Frame-Options: DENY`, `Content-Security-Policy: frame-ancestors 'none'` y `Permissions-Policy: camera=(), microphone=(), geolocation=()`, sin tocar las tres que ya estaban (`nosniff`, `Referrer-Policy` y el `Cache-Control` de D-096). **Comprobado antes de escribir:** no hay **ni un** `<iframe>` propio en todo el repositorio (por eso `DENY` y no `SAMEORIGIN`), y la app no usa cámara, micrófono ni ubicación (`image_picker` abre la galería). **La CSP lleva solo `frame-ancestors`, y es deliberado:** añadirle `default-src`, `script-src` o `frame-src` bloquearía `checkout.epayco.co` —la pasarela— y `passkeys_bundle.js`, y ese fallo **no se ve en las pruebas, solo cuando un negocio real intenta pagar**. Hay una prueba que lo impide y el aviso está dentro del propio `_headers`. `flutter analyze` 0/0 y **347 de 347 pruebas en verde** (6 nuevas). Sin migración SQL |
| 8.25 | 🔴 **Activar HSTS en Cloudflare** (SSL/TLS → Edge Certificates → HSTS). **Es la mitad de TL-03 que no se puede cerrar desde el repositorio**: escribir `Strict-Transport-Security` en `web/_headers` no lo activa, solo deja una línea que aparenta proteger | 👤 | ⏳ **Pendiente del propietario.** Lo separó D-202 en vez de darlo por hecho dentro del paso 8.24. La regla 12 pide permiso antes de tocar Cloudflare, así que esto no lo puede hacer el asistente |
| 8.26 | 🔴 **Hotfix: la pantalla de Tickets caída en producción.** `PostgrestException: Rango de fechas invalido.` al entrar a "Tickets & Caja". **Corrige a D-199** | 🤖 | ✅ **CERRADO 04-sep (D-203).** `get_ticket_board_list_v2` rechaza las fechas nulas **desde que existe** (17-ago), así que la llamada principal de Tickets **nunca funcionó**: fallaba en cada carga y el `catch (_)` la tapaba cayendo al respaldo. D-199 quitó el catch **y el respaldo**, y el fallo salió a la luz. **Se retiró un respaldo sin comprobar antes que el camino principal funcionara**, y las pruebas de D-199 no podían verlo porque leen el código fuente: seguía siendo cierto que el catch no estaba, con la pantalla rota. Arreglo conservador elegido por el propietario: llamar directo a `get_tickets_summary_v2`, sin respaldo ni catch. **Destapa 2,5 semanas de degradación silenciosa** — esa RPC no trae `sale_code` ni `client_phone`, así que el chip del número de venta, el botón de WhatsApp (D-195) y la búsqueda por teléfono **nunca se vieron**. Va al paso 8.27. Se añade `test/contrato_rpc_fechas_test.dart`, que compara el Dart con el SQL — la prueba que lo habría cazado. `flutter analyze` 0/0 y **350 de 350 en verde** |
| 8.27 | **Recuperar lo que el respaldo escondía 2,5 semanas: chip del número de venta, botón de WhatsApp y búsqueda por teléfono en la lista de Tickets.** Sale de D-203 | 🤖 | ✅ **CERRADO 04-sep (D-204).** Desbloqueado por el dato que faltaba: el propietario contó en la base y hay **cero** tickets con `scheduled_at` nulo, que era el riesgo que frenó a D-203. Se vuelve a `get_ticket_board_list_v2` con un rango de fechas real. **Comparando columna por columna aparecieron dos diferencias que ninguna prueba miraba:** el saldo se llama `pending_balance` en una RPC y `balance_amount` en la otra (leer solo la vieja habría enseñado **saldo cero en todos los tickets**; no pasó porque el modelo ya leía las dos, pero se comprobó), y **el orden está invertido** — la del Tablero ordena ascendente porque es una línea de tiempo, así que abrir Tickets habría mostrado los diez tickets **más viejos** del salón. Se reordena en el cliente y no en el SQL, porque esa RPC también la usa el Tablero de Agenda (D-148), donde el orden ascendente sí es el correcto. ⚠️ **Corrección a lo que decía esta misma fila: NO cierra el tercio pendiente de TL-09.** El rango es amplio a propósito para no cambiar lo que la pantalla enseña, así que la consulta sigue trayendo el historial completo. Acotarlo exige decidir qué significa "todos" para el salón, que es decisión de producto. **Anotado (regla 20):** `scheduled_at` admite nulos; hoy hay cero, pero si algún día se crea un ticket sin fecha desaparecerá de la lista en silencio. El arreglo durable es una migración que deje de excluirlos. `flutter analyze` 0/0 y **358 de 358 pruebas en verde** (8 nuevas, de comportamiento) |
| 8.28 | **El Dashboard hace caso a la sede activa, y su selector se unifica con el de Reportes.** Hasta ahora abría siempre en consolidado y los números no cambiaban al cambiar de sede en la píldora | 🤖 | ✅ **CERRADO 04-sep (D-205).** **Dos de las tres premisas del encargo no coincidían con el código y se comprobaron antes de escribir:** el Dashboard **sí** permitía alternar sedes (tenía un desplegable), y el remontaje al cambiar de sede **ya lo arreglaba D-201** — la llave con `branchId` se pone igual, pero como algo explícito, no como arreglo de un fallo vivo. **El arreglo de fondo es el ámbito por defecto:** `_sedeElegida` (`String?`) pasa a `_consolidado` (`bool`), mismo nombre y significado que en `ReportesPage` (D-194), arrancando en la sede activa. Es menos estado y hace que la píldora sea la única que decide qué sede se mira. **El desplegable se sustituye por el `SegmentedButton`, no se acompaña:** los dos juntos serían dos controles para el mismo estado. Elección del propietario entre tres opciones. **Cuesta un clic**: para ver otra sede ahora se cambia la píldora. **"Primeros pasos" no se recarga al cambiar de ámbito** aunque el encargo lo pedía: se pide con `branchId` y no depende del ámbito, así que sería una consulta de más sin efecto. `flutter analyze` 0/0 y **365 de 365 pruebas en verde** (7 nuevas, de comportamiento). Sin migración SQL: `p_branch_ids` nulo o vacío ya significaba "todas" desde D-110 |
| 8.29 | **Limpieza visual: fuera las 9 tarjetas fijas de cabecera heredadas del prototipo** («conectado a Supabase», «Módulo base de Configuración») | 🤖 | ✅ **CERRADO 04-sep (D-206).** 66 líneas retiradas de 9 pantallas. **Las nueve no eran lo mismo:** seis eran prototipo puro —cinco con «Supabase» y la de Configuración en futuro («aquí *configuraremos*… reglas *futuras* de WhatsApp e IA»)— pero **tres llevaban reglas de permisos que el subtítulo no dice** (quién puede anular un gasto o una compra, y que las contraseñas no se tocan desde Usuarios). Elección del propietario entre tres opciones: **esas tres reglas se movieron al subtítulo**, así que se recupera todo el espacio vertical sin perder información. **Dos correcciones de más, del mismo defecto:** los textos «Cargando clientes/tickets desde Supabase…». **Y dos cosas que NO se tocaron:** la mención legal a Supabase en Términos (encargado del tratamiento, Ley 1581 — quitarla sería regresión de cumplimiento) y siete descripciones de error que nombran funciones internas; reescribirlas queda anotado. Se añade `test/sin_jerga_tecnica_test.dart`: lo que no tiene guardián, vuelve. `flutter analyze` 0/0 y **367 de 367 pruebas en verde** |
| 8.30 | 🔴 **No se podía pagar: el token vencido llegaba a la Edge Function y esta respondía 401.** Reportado en producción al darle a «Renovar suscripción con ePayco» | 🤖 | ✅ **CERRADO 04-sep (D-207).** Los tokens caducan a la hora y **nadie forzaba el refresco antes de llamar**. La causa es más honda que el sitio reportado: el SDK arma la cabecera con `currentSession` y **la guarda**, actualizándola solo cuando ocurre un refresco — leerla a mano no era peor, era exactamente lo mismo. **El fallo estaba en los 4 sitios que llaman Edge Functions con `verify_jwt = true`, y en dos era invisible** porque el error ya se traga por diseño: el correo de invitación y la alerta de stock bajo **simplemente no salían**. El de la vuelta de la pasarela fallaba blando (D-200), así que el dueño se quedaba sin confirmación sin saber por qué. Alcance aprobado por el propietario: los cuatro. Se cierra con un helper canónico (`lib/services/sesion_supabase.dart`) y las cuatro llamadas quedan idénticas. Se añade una prueba que lee `config.toml` y `lib/` **a la vez** para que no aparezca un quinto sitio — y **cazó una inconsistencia real antes de subirse**. `flutter analyze` 0/0 y **375 de 375 pruebas en verde** (8 nuevas). Sin migración SQL y **sin desplegar Edge Functions**: es enteramente Flutter |
| 8.31 | **Las tarjetas de error dejan de nombrarle funciones de la base al salón.** Sale de D-206 | 🤖 | ✅ **CERRADO 04-sep (D-208).** Seis descripciones —cinco en Configuración, una en Inventario— pasan de *«Revisa la conexión con Supabase o la función `get_business_settings`»* a *«Revisa tu conexión a internet o intenta nuevamente más tarde»*. **Eran seis, no siete: D-206 contó mal y se corrige aquí.** **El guardián de excepciones muertas de D-206 funcionó una sesión después de escribirse:** al reescribir los textos, falló solo y avisó de que sus propias excepciones sobraban, citando el motivo que tenían. La lista baja de tres entradas a una — solo queda la declaración legal de Términos. **El guardián nuevo lee migraciones y pantallas a la vez** y cubre **las 219 funciones** del proyecto, no las dos que nombraba el encargo: una lista escrita a mano solo vigila lo que ya se rompió una vez. ⚠️ **Anotado, no resuelto:** esos paneles son `const` y **descartan `snapshot.error`**. Ni se enseña, ni se guarda, ni se reporta a `MonitoreoService`. El salón ya ve un mensaje útil, pero **sigue sin haber forma de saber qué falló**. No es una línea: llamar a `reportarError` dentro de un `builder` dispararía en cada reconstrucción. `flutter analyze` 0/0 y **376 de 376 pruebas en verde** |
| 8.32 | **Reporte sistemático de errores técnicos a MonitoreoService (Sentry) con `MonitoreoService.capturar` y saneamiento total de excepciones en UI.** Cierra el apartado 4 de D-208 | 🤖 | ✅ **CERRADO 04-sep (D-209).** Los servicios instrumentan `MonitoreoService.capturar<T>(accion, motivo: ...)`: el error se reporta a Sentry exactamente **una sola vez** al fallar la llamada asíncrona (sin spam en reconstrucciones de widgets de Flutter) y se relanza (`rethrow`) para que el `FutureBuilder` muestre el panel amigable. Se erradican las 16 apariciones de `snapshot.error.toString()` en pantallas de `lib/pages/` reemplazándolas por mensajes claros. Se robustece `MonitoreoService._limpiar` para sanitizar correos y teléfonos (Ley 1581) en mensajes y excepciones antes de enviar a Sentry. Suite y guardián estricto en `test/monitoreo_service_test.dart`. `flutter analyze` 0/0 y **382 de 382 pruebas en verde** (6 nuevas) |
| 8.33 | **Blindaje total de excepciones en UI, sanitización de breadcrumbs en Sentry y guardián ampliado.** Cierra los hallazgos de auditoría sobre D-209 | 🤖 | ✅ **CERRADO 04-sep (D-210).** Se sanitizan `evento.breadcrumbs` y teléfonos colombianos con espacios/guiones en `MonitoreoService._limpiar` (Ley 1581). Se eliminan todas las fugas residuales `${snapshot.error}` y excepciones crudas en 6 archivos de interfaz (`dashboard_page.dart`, `settings_page.dart`, `authenticated_router.dart`, `publication_studio_dialog.dart`, `main.dart`, `security_settings_dialog.dart`). El guardián estricto en `test/monitoreo_service_test.dart` cubre ahora recursivamente `lib/pages/`, `lib/widgets/` y `lib/main.dart` con lista blanca documentada para el operador de plataforma. `flutter analyze` 0/0 y **383 de 383 pruebas en verde** (7 pruebas en la suite de monitoreo) |
| 8.34 | **Guardián de excepciones muertas para la lista blanca, sanitización de teléfonos fijos y errores Postgres, y prevención de re-disparos en FutureBuilder.** Cierra las 4 observaciones de auditoría sobre D-210 | 🤖 | ✅ **CERRADO 04-sep (D-211).** Se incorpora prueba de excepciones muertas para la lista blanca del guardián en `test/monitoreo_service_test.dart`. Se extiende la sanitización de `MonitoreoService` para cubrir teléfonos fijos (60x) y violaciones de restricciones de Postgres (`Key (...) = (...)`). Se convierten `_Level2Sheet` (`agenda_page.dart`) y `_TenantDetailSheet` (`platform_panel_page.dart`) a `StatefulWidget` inicializando sus `Future` en `initState()` para eliminar re-disparos de llamadas/reportes durante reconstrucciones de widgets. `flutter analyze` 0/0 y **385 de 385 pruebas en verde** (9 en la suite de monitoreo) |
| 8.35 | **Corrección integral del Panel de Plataforma y Checkout ePayco: chips de filtro cortados, estado prueba vencida, fallback plan pro y resiliencia 401.** Reportado por el propietario en producción con capturas | 🤖 | ✅ **CERRADO 04-sep (D-212).** Chips de filtro con iconos nativos (sin truncamiento), badge y banner dinámico de 'PRUEBA VENCIDA' cuando `trial_ends_at < now()`, migración RPC `platform_update_tenant_pricing` con mapeo de planes legacy a 'pro', y reintento automático con sesión renovada en CheckoutService ante 401. `flutter analyze` 0/0 y **386 de 386 pruebas en verde** (1 nueva). Migración SQL `20260904180000_platform_pricing_fallback_pro_d212.sql` |
| 8.36 | **Desenmascaramiento de errores de pasarela ePayco y flujo directo de registro para colaboradores invitados.** Reportado por el propietario en producción | 🤖 | ✅ **CERRADO 05-sep (D-213).** Desacople estricto entre errores de pasarela ePayco ("sesión de pago") y expiración de sesión Supabase Auth en `epayco_checkout_service.dart`, eliminando el falso aviso de "Tu sesión ha expirado" y mostrando el mensaje fidedigno de la pasarela/servidor. Flujo directo de registro para colaboradores invitados: botón '¿Primera vez? Crea tu contraseña de colaborador →' en `LoginPage` y modo `isCollaborator: true` en `RegisterPage` ('Crea tu cuenta de colaborador') con enrutamiento automático post-signUp a `AcceptInvitationPage`. Clarificación de modelo de prueba vencida (D-014/D-046/D-053). `flutter analyze` 0/0 y **387 de 387 pruebas en verde** |
| 8.37 | 🔴 **El cobro no podía ejecutarse: las seis RPC del cobro viven en `private` y PostgREST solo invoca funciones de `public`.** Sale de la activación de planes reportada en producción tras 8.36 | 🤖 | ✅ **CERRADO 06-sep (D-214).** Migración `20260905180000` con seis wrappers `public.*` `security definer`, `execute` solo para `service_role` (el perímetro de D-177 no se toca). Se corrigió además que **la migración se había commiteado rota**, con los `$$` escapados y BOM desde un heredoc de PowerShell. Se priorizan `SUPABASE_SECRET_KEYS` / `SUPABASE_PUBLISHABLE_KEYS` ante *"Legacy API keys are disabled"*, extraídas al módulo compartido `_shared/supabase_keys.ts`, y `create-epayco-session` resuelve el negocio en cascada. `flutter analyze` 0/0 y 387/387 pruebas en verde. **VERIFICADO EN PRODUCCIÓN el 06-sep (D-215):** migración aplicada con respaldo previo, las cuatro funciones desplegadas, secret key `edge_functions` creada y checkout probado hasta que la pasarela abre. Los registros muestran la clave moderna y **la desaparición de los avisos de fallback**, que es la prueba de que los wrappers trabajan. Falta ejercitar el webhook, que necesita un pago completado |
| 8.38 | **Añadir verificación estática de las Edge Functions al CI (hallazgo AB).** Sale de D-215: un `ReferenceError` trivial llegó a producción porque nadie mira `supabase/functions/` | 🤖 | ➡️ **Movido al paso 9.6** (D-216). Se anota aquí para no perder el rastro, pero **manda el 9.6**: dos pasos abiertos para el mismo trabajo son la duplicación que esta fase viene a quitar |

> **La auditoría de 4 revisiones del 01-sep** (técnica, UX, producto y crítica
> brutal/seguridad) está verificada hallazgo por hallazgo en
> `docs/01_arquitectura/auditorias/AUDITORIA_4_REVISIONES_2026-09-01.md`. Ese
> documento **no manda sobre este**: es el expediente de evidencia. Los pasos
> que salgan de él entran aquí, uno a uno, como 8.9, 8.10 y siguientes.

---

### FASE 9 — Puesta a punto antes del cliente cero

**Por qué existe esta fase (D-216).** El MVP está listo y **no hay ni un cliente real: todos los negocios y los datos de la base son de prueba.** Ese es el momento más barato de toda la vida del producto para tocar la estructura y ordenar lo que se desordenó por el camino. Cada semana con un salón real dentro, lo mismo cuesta el doble y arriesga el negocio de otra persona.

**No es un rediseño.** Es cerrar el producto que ya existe. Estimación: dos a tres semanas.

**El orden cambió el 07-sep.** Cuando nació esta fase (D-216), el bloque A iba primero: no se puede planificar sobre documentos que mienten. Sigue siendo cierto, pero dejó de ser lo más urgente: la salida a redes (D-217) puso diez personas esperando una tarifa, y la auditoría (D-220) destapó que la base la sobrescribe sola. **El orden vigente es el de la tabla de abajo**; los bloques A a F se conservan porque agrupan bien por tema, pero ya no dictan la secuencia. Lo único que no se negocia nunca: **el ensayo general (9.24) va antes que el rediseño (9.25)** — sin ver un salón trabajando, reestructurar es adivinar caro.

#### El orden de ejecución

##### Orden propuesto el 23-sep (revisión integral, D-254) — pendiente de aprobación del propietario

**Por qué se rehace.** La tabla del 07-sep dejó de gobernar cuando el recorrido
con personas reales (9.49) destapó lo que había que hacer primero; desde
entonces el orden lo iban dictando los HANDOFF, **contra la regla 1 del §2**:
*este documento manda sobre qué falta y en qué orden*. Se vuelve a escribir aquí.

| Turno | Qué | Por qué en este orden |
|---|---|---|
| **A. Que el primer cobro funcione** | **BI** (vencer sin pagar: gracia, aviso y corte de sedes) → **9.8 + 9.7** con dinero propio y la sede de Éxito a $4.500 👤 → 9.40 (historial con sede) → **AD** → 9.9 (pruebas de precios y aprobaciones) | **El camino de cobro que queda nunca ha cobrado un peso real** (§5 de `ARQUITECTURA_Y_EVOLUCION`). Mejor descubrirlo con $4.500 propios que con el primer cliente |
| **B. Lo legal, antes de datos de otra persona** | **9.20** (abogado: términos, privacidad y derechos de la clienta sobre sus datos) · **9.16** (contador) · **9.48** (que la clienta autorice, con diseño y calma) · **BC** (decidir el acceso de soporte) · **AS** | Ley 1581. Hoy no hay datos de terceros; **el día que entren, todo esto tiene que estar hecho**, y el 9.48 conviene diseñarlo con el abogado |
| **C. Lo que ve el cliente, barato** | **BG** (la segunda `register_tenant`) · **AV** (tildes, incluida la página de precios) · **BD** · AR · AX · AN · los cuatro **AK** · I-18 · 9.17 (HSTS, 👤 dos minutos) · 9.30 · AA · BH | Media hora cada uno. Se nota el primer día |
| **D. El campo** | **9.33** (protocolo de bienvenida) · **9.24** (entrevistas con el guion) · **9.26 (Meta) ya mismo, en paralelo** | 9.24 sigue bloqueando la estructura; el trámite de Meta son semanas de espera y lleva parado desde el 07-sep |
| **E. La estructura** | **9.25** (después de 9.24) → 9.13 y 9.14 al reestructurar → **I-19** → 9.41 · AI | Rediseñar antes de ver un salón es adivinar caro; sacar la lógica de las pantallas **durante** la reestructura evita hacerlo dos veces |

##### El orden del 07-sep, que se conserva como historia

Los bloques A a F de abajo **agrupan por tema**. Esta tabla dice **en qué orden se hacen**, que no es el mismo,
porque la auditoría del 07-sep (D-220) y la salida a redes (D-217) cambiaron las urgencias. **No se pasa de turno
sin cerrar el anterior.**

| Turno | Pasos | Puerta |
|---|---|---|
| **1. La puerta del piloto** | ~~9.28~~ → ~~9.22~~ → **9.29 → 9.19 → 9.15** | Nada de esto puede estar mal cuando entre el salón 1 |
| **2. El camino del dinero** | 9.27, 9.10, 9.7, 9.8 | Antes de que alguien pague de verdad |
| **3. La red** | 9.6, 9.9 | Antes de tocar el código grande |
| **4. La base: seguridad y estabilidad** | 9.15, 9.17, 9.18 | Antes de guardar datos de otra persona |
| **5. Higiene** | 9.1 a 9.5, 9.11, 9.30, 9.31, 9.32 | Cuando haya hueco |
| **6. El campo, y solo entonces el rediseño** | 9.33 → 9.24 → 9.25, con 9.26 en paralelo | **9.25 no se toca sin 9.24** |

**Actualizado el 07-sep:** 9.28 y 9.22 quedaron cerrados el mismo día. La tarifa es **50.000 fijos** (D-222) y se puede cobrar hoy sin aplicar la migración, simplemente sin marcar la casilla "Pionero". **El turno 1 arranca ahora en 9.29:** las métricas cuentan tres negocios de ensayo y nadie puede marcarlos.


#### Bloque A — Que los papeles digan la verdad

| # | Qué | Quién | Estado |
|---|---|---|---|
| 9.1 | **Retirar el §6 del `docs/README.md`** — 248 entradas cronológicas mantenidas a mano — y sustituirlo por cómo leer el `git log`, que lleva ese registro solo y nunca miente. Es la enfermedad de D-126 y D-131 reaparecida en el índice | 🤖 | ✅ **CERRADO 06-sep.** Las 248 entradas se **archivaron**, no se borraron: `_archivo/REGISTRO_CRONOLOGICO_HASTA_2026-09-06.md`. El §6 pasa a explicar las tres órdenes de `git log` que responden lo mismo sin desincronizarse. El README baja de 371 a 135 líneas |
| 9.2 | **Arreglar los marcadores de estado contradictorios** de la Fase 8: los pasos 8.10, 8.11 y 8.13 empiezan la celda en 🔄 y la terminan en ✅ CERRADO. Quien busque lo que falta se lleva tres falsos positivos | 🤖 | ✅ **CERRADO 06-sep.** Eran **cinco**, no tres: 8.8, 8.10, 8.11, 8.12 y 8.13. El primer intento arrastró también al **8.9, que SÍ está abierto** — detectado al releer y devuelto a 🔄. La Fase 8 queda con dos pasos abiertos reales (8.9 y 8.38), ambos recogidos aquí como 9.7 y 9.6 |
| 9.3 | **Un número, un sitio.** Los módulos son 14, 16 o 20 según dónde se mire; las decisiones, 211 o 215; el archivo histórico, 75 o 124 documentos | 🤖 | ✅ **CERRADO 06-sep.** Los recuentos copiados se **quitan** en vez de actualizarse, que es la regla de D-131: los números exactos viven en su fuente y los índices dejan de repetirlos. Apareció de paso que la "Regla de actualización" del README mandaba actualizar el `PLAN_DE_LANZAMIENTO`, **archivado el 09-ago por D-126** |
| 9.4 | **Sacar los tamaños de archivo de la tabla de módulos** (§4). Dice que Tickets tiene 3.699 líneas y tiene 5.121; que Agenda tiene 234 y tiene 1.797. Un número que envejece dentro de un documento vivo es una mina enterrada | 🤖 | ✅ **CERRADO 06-sep.** Fuera "3.699 líneas" de Tickets y "234 líneas" de Agenda |
| 9.5 | 🔴 **Los `.txt` de OneDrive NO son un duplicado: 23 de los 51 son el único registro que existe del primer mes del proyecto.** Se dio por hecho que repetían lo mismo que `_archivo/handoffs/` y **es falso** (comprobado el 06-sep): los `.md` del repositorio arrancan el **22-jul** y los `.txt` el **27-jun**, con los pasos 001 al 1085, más las dos "FUENTE CANÓNICA" (446 KB de narración consolidada). Y en el tramo que sí se solapa tampoco coinciden: los `.md` guardan **las decisiones**, los `.txt` guardan además el **historial detallado de pasos** numerado 1..1640. Barrido de secretos: **0 coincidencias**, se pueden versionar sin riesgo | 👥 | ⬜ **Propuesta: importarlos a `docs/_archivo/`** para que la historia del proyecto deje de depender de una carpeta de OneDrive sin control de versiones. El `.docx` se queda fuera, que es lo que manda `AGENTS.md` |

#### Bloque B — Red donde pasa el dinero

| # | Qué | Quién | Estado |
|---|---|---|---|
| 9.6 | El **`deno check` en el CI** (era 8.38, hallazgo AB). Hoy `supabase/functions/` no lo mira nadie | 🤖 | ✅ **CERRADO 09-sep (D-234).** Trabajo aparte en el CI, en paralelo al de Flutter, comprobando **función por función con su propio `deno.json`** y sin detenerse en el primer fallo. ⚠️ **La primera corrida es la prueba:** `deno` no está en el equipo del propietario (regla 14) y puede salir roja — es la primera vez que este código pasa por un comprobador de tipos |
| 9.7 | 🔴 **Cerrar 8.9 de verdad: verificar TL-01 contra una transacción real.** Sigue abierto desde el 01-sep. Es la última oportunidad de hacerlo con dinero propio y no de un cliente | 👤 | ⬜ |
| 9.8 | **Probar el ciclo completo de cobro**: pago → webhook → activación. Nadie lo ha ejercitado entero nunca. Con la tarifa de prueba de 10.000 COP, cuesta diez mil pesos saberlo | 👤 | ⬜ ⚠️ **Más urgente desde el 22-sep:** los dos únicos cobros reales de la historia fueron por el camino del negocio, que D-252 cerró. **El camino de la sede —el único que queda— nunca ha cobrado un peso real.** La sede de la Peluquería Éxito tiene pactados **$4.500**: es la prueba real más barata posible, y cierra también el 9.7. |
| 9.9 | **Pruebas sobre el Panel de Plataforma**: 4.230 líneas y cero pruebas. No cubrirlo entero — solo **precios y aprobaciones**, que es la caja registradora del SaaS | 🤖 | ⬜ |
| 9.10 | **Cerrar el hallazgo W**: decidir si el fallback del cargo debe existir o si un fallo de la RPC debe fallar a la vista | 👤 | ✅ **CERRADO 08-sep (D-227): se retira el fallback entero.** 503 si la RPC falla, 409 si no hay cargo. Aparecieron **tres defectos más** al quitarlo, incluido que cobraba el monto del negocio por una sede. Y el mensaje del servidor **por fin llega limpio** al salón. 391 pruebas en verde |
| 9.11 | **Sacar `activar_pago_naguara.sql` de los scripts de verificación.** Una intervención manual sobre un pago real no puede ser indistinguible de una prueba (`AGENTS.md`: trazabilidad del historial financiero) | 🤖 | ✅ **CERRADO 09-sep (D-233).** Pasa a `supabase/sql/intervenciones/` con un `LEEME.md` que fija cuatro reglas. La carpeta de controles vuelve a tener solo cosas que se pueden correr sin miedo: **199 controles idempotentes** |

#### Bloque C — El ensayo general

| # | Qué | Quién | Estado |
|---|---|---|---|
| 9.12 | **Recorrer el producto entero como una dueña nueva, de cero y sin saltarse nada:** registrarse, esperar aprobación, pagar, configurar servicios, invitar a una estilista, crear una cita, cobrarla, cerrar la caja y mirar los reportes. Anotando **cada** fricción | 👥 | ⬜ *(23-sep: hecho en gran parte por el recorrido con personas reales del 17 al 19-sep, paso 9.49 — **salvo pagar**, que sigue en el 9.8.)* |

> **Este paso define el bloque D.** Nadie lo ha hecho nunca de una sentada. Va a encontrar más que cualquier auditoría, porque las auditorías miran el código y esto mira el producto.

#### Bloque D — La ventana que se cierra

| # | Qué | Quién | Estado |
|---|---|---|---|
| 9.13 | **Sacar la lógica de dinero de `tickets_page.dart`** (5.121 líneas) a `services/`. Mover cómo se cobra fuera de dónde se dibujan los botones | 🤖 | ⬜ *(23-sep: **creció** desde que se anotó: 5.458 líneas.)* |
| 9.14 | Lo mismo con `platform_panel_page.dart` (4.230) y `settings_page.dart` (3.311) | 🤖 | ⬜ *(23-sep: **crecieron**: el panel 5.420 líneas —1.190 más— y configuración 3.631.)* |

> **Por qué ahora.** `services/` promedia 80 líneas por archivo: son cascarones. La lógica del negocio vive dentro de las pantallas, y `pages/` concentra el 62% del código. Eso es lo que hizo que el hallazgo Q tardara tres semanas y media en encontrar su causa. **El alcance se decide con lo que salga de 9.12, no antes.**

#### Bloque E — Requisitos duros del cliente cero

| # | Qué | Quién | Estado |
|---|---|---|---|
| 9.15 | **Supabase a plan Pro** (era 3.4). En el gratuito no hay recuperación a un punto en el tiempo: si se pierden datos, se vuelve al último respaldo corrido a mano | 👤 | ✅ **DECIDIDO 08-sep (D-226): se aplaza** hasta el primer cliente que pague. Riesgo evaluado: el gratuito aguanta diez, con dos condiciones — respaldo **cada semana** y vigilar el almacenamiento (pagar al pasar de 700 MB) |
| 9.16 | **El contador: DIAN e IVA** (era 3.2). Facturar sin saber qué se debe es un problema legal, no técnico | 👤 | ⬜ |
| 9.17 | **HSTS en Cloudflare** (era 8.25) y **rotar la contraseña de la base** (hallazgo X, nunca se ha rotado) | 👤 | ✅ **DECIDIDO 08-sep (D-226): no se rota.** Solo la sabe el propietario y no hay fuga. **Sigue viva la condición del hallazgo X:** pasa a urgente si se teclea en sesión compartida, se pega en un chat o sale en una captura. HSTS queda aparte |
| 9.18 | **Documentar la recuperación de fotos** (hallazgo Z): al restaurar, `storage.objects` vuelve vacío. Con fotos de un cliente real deja de ser una curiosidad | 🤖 | ⬜ |
| 9.19 | 🔴 **La base limpia.** Los negocios de prueba viven en la base de producción junto a una intervención manual sobre un pago real. Decidir si se purgan o se marcan con el mecanismo de D-120. **Y la pregunta de detrás: ¿las métricas de plataforma los están contando?** Si sí, el primer número real será falso | 👥 | ✅ **CERRADO 08-sep (D-226): se quedan.** Marcados con `is_demo` y útiles como ojos para ver las pantallas como las ve una clienta |
| 9.20 | **Cerrar 3.3**: Términos y Privacidad están escritos (D-144) pero el paso sigue en 🔄 | 👥 | ⬜ |

#### Bloque F — La cara

| # | Qué | Quién | Estado |
|---|---|---|---|
| 9.21 | **I-16, la identidad visual**: logo definitivo y paleta extendida. Lleva en el buzón sin asignar desde que se anotó, y **el cliente cero ve la marca antes que el producto** | 👥 | ⬜ |
| 9.22 | **Fijar la tarifa de los diez primeros.** El anuncio del 07-sep prometía "tarifa preferencial congelada de por vida" sin decir cuánto (D-217) | 👤 | ✅ **CERRADO 07-sep (D-222): 50.000 fijos, congelados de por vida, por el negocio; las sedes adicionales a tarifa vigente.** No son pioneros sino **socios de diseño**. Se aplica con `price_cop = 50000` y `discount_percent` **vacío** — se acumulan, no se eligen ⚠️ **Superado por D-252 (22-sep):** el precio ya no va en el negocio sino **en cada sede**; los $50.000 de un socio de diseño se pactan en su sede. |
| 9.23 | **Cerrar lo que queda de las redes:** fijar la publicación en el perfil de Instagram, añadir el enlace a `salonymas.com` desde la app móvil (el escritorio no lo permite) y atender el grupo "Peluquerías Bogotá" | 👤 | ⬜ Detalle en `02_operacion/REDES_SOCIALES.md` |
| 9.24 | 🔴 **Observar un día entero de trabajo en el salón piloto.** Sin demostrar nada y sin hablar: mirar y contar. Cuántos WhatsApp entran y qué pasa con cada uno, dónde se apunta físicamente una cita, qué ocurre con quien llega sin cita, cómo cobran y quién apunta qué, cómo cuadran la caja al cerrar, cómo y cuándo le pagan a la estilista, y dónde están hoy las fotos de los trabajos | 👤 | ⬜ **Bloquea al 9.25.** El propietario no tiene salón propio y nunca ha visto uno trabajar: sin esto, la estructura de D-219 es una hipótesis  **18-sep: se aborda por ENTREVISTA, no por observacion.** El propietario no tiene salon propio ni acceso a uno para pasar el dia, asi que se preguntara a administradores de salones reales con `docs/03_referencias/GUION_ENTREVISTA_SALONES_9.24.txt` — ocho bloques, cada uno decidiendo algo concreto de los cinco modulos, mas una hoja de conteo por si dejan quedarse un rato. **Se anota la rebaja a proposito: una entrevista cuenta lo que la gente DICE, una observacion lo que HACE.** El guion lo compensa preguntando siempre por la ultima vez concreta y pidiendo ver el cuaderno, el Excel o el grupo de WhatsApp. **Con dos entrevistas se decide si los 5 modulos se sostienen; con cuatro, como separar las pantallas por rol.** |
| 9.25 | **Reestructurar de 15 módulos a 5 lugares** (HOY, CLIENTAS, MI DINERO, MI VITRINA, AJUSTES) y separar la app de la estilista, según D-219. **No se borra ningún módulo: cambia dónde se entra.** La maquinaria ya existe — `requiredFeature` y `PlanLockedPage` de D-184 sirven igual para escalonar lo que se ve | 🤖 | ⬜ **Bloqueado por el 9.24.** Cierra también el hallazgo Ñ: las fotos no son un sitio, son un paso |
| 9.26 | **Arrancar la verificación de empresa con Meta** para el agente de WhatsApp (era 6.5). Son **semanas de espera, no de trabajo**: el trámite corre solo mientras se construye otra cosa | 👤 | ⬜ Por D-219, el agente de WhatsApp deja de ser un extra y pasa a ser la cuña competitiva *(23-sep: sin arrancar desde el 07-sep. Son semanas de espera que no cuestan trabajo: cuanto antes se pida, antes llega.)* |
| 9.27 | 🔴 **P0 — una sede secundaria pagada nunca se activa.** `verify-epayco-transaction` no lee `branch_id` y llama siempre a `beautyos_procesar_evento_epayco`; el webhook sí bifurca a `beautyos_procesar_pago_de_sede`, pero llega después, encuentra el evento ya insertado y aborta por idempotencia. **El dinero sale y la sede queda en `pending` para siempre** (D-220) | 🤖 | ✅ **CERRADO 07-sep (D-224).** Resuelve la intención por factura, extrae `branch_id` y bifurca igual que el webhook. De paso el negocio deja de salir de `x_extra1` —que viaja fuera de la firma de ePayco— y sale de la intención, manteniendo la comprobación de pertenencia de D-181. **Pendiente desplegar** |
| 9.28 | **Quitar el 50% automático del pionero.** D-212 había reintroducido lo que D-188 y D-189 abolieron: marcar "Pionero" pisaba el precio pactado | 🤖 | ✅ **CERRADO 07-sep (D-221).** `is_founder` vuelve a ser etiqueta, y marcar pionero sin precio ni descuento **falla a propósito**. Migración `20260907120000`. **Pendiente aplicarla en Supabase.** Ya **no bloquea** al 9.22: se puede cobrar hoy sin marcar la casilla. Pero sin ella se pierde la etiqueta de quiénes fueron los diez primeros, y el 50% sigue armado para quien la pulse mañana (D-222) *(Nota del 23-sep: la migración **sí está aplicada** — el texto vivo extraído el 22-sep tenía la guarda de D-221. Y su aviso resultó profético: el 50% seguía armado en `platform_approve_tenant`, cerrado con D-252.)* |
| 9.29 | 🔴 **Nadie puede marcar un negocio como de prueba.** D-120 creó `is_demo` con un `update` a mano dentro de una migración y **no dejó ni RPC ni botón**. Consulta del 07-sep: **los tres negocios en `is_demo = false`** — Naguara (que el script de ePayco revirtió), Prueba Barbería Elite y Exportadora, que nacieron con el `default false`. Las métricas del SaaS llevan meses contando ensayos (D-220) | 🤖 + 👤 | ✅ **CERRADO ENTERO 07-sep (D-225).** Nace `platform_set_tenant_demo` — solo dueño de plataforma, reversible y con rastro —, la migración `20260907140000` marcó los tres negocios (verificado: `is_demo = true` los tres, y `anon` no alcanza la función), y el Panel tiene su fila *"Negocio de ensayo"* con botón para marcar y desmarcar, con aviso de las consecuencias. `flutter analyze` 0/0 y 388 pruebas en verde |
| 9.30 | La URL del webhook está quemada en `create-epayco-session:385`. Si el proyecto se clona o se mueve, ePayco sigue avisando al backend viejo sin que nadie se entere | 🤖 | ⬜ Usar `${SUPABASE_URL}`, que el módulo compartido ya exporta |
| 9.31 | **Hallazgo AA:** `send-invitation-email` y `send-low-stock-alert` repiten a mano la cascada de claves en vez de importar `_shared/supabase_keys.ts`. **No están rotas** — prefieren la clave publicable— pero la misma lógica vive en tres sitios | 🤖 | ⬜ Sin urgencia |
| 9.32 | **`applies_after_discount` promete algo que no cumple.** Se guarda en cada fila de comisión y el cálculo nunca lo mira. **Hoy es inofensivo: la caja no aplica descuentos.** Se vuelve real el día que se añadan (D-220) | 🤖 | ⬜ Documentarlo como pendiente, o quitar la columna. Lo que no puede quedarse es a medias |
| 9.33 | 🔴 **El protocolo de bienvenida de los 10 pioneros.** Hoy, si alguien escribe por WhatsApp, la respuesta es improvisar. Debe quedar escrito: qué se responde, la demostración de 20 minutos **empezando por el día de trabajo y no por el menú**, el precio pactado con su motivo, crear y aprobar el negocio (D-138, *nadie entra solo*), la configuración inicial acompañada —que es donde se pierden los clientes—, qué se mira en los días 3, 10 y 20, y **la conversación de precio del día 21 con fecha en el calendario**. Y qué recibe el proyecto a cambio: mirar un día de trabajo, avisar cuando algo se rompe, y decir la verdad | 👥 | ⬜ Documento vivo en `02_operacion/`. Un piloto que solo recibe no da información |
| 9.34 | 🔴 **Las métricas y las comisiones de partners no cuentan el dinero de las sedes** (D-231). El MRR solo mira `tenant_subscriptions`, y el cobrado histórico y las comisiones suman una clave que el pago de sede no escribe. **Un partner que refiera un salón de tres sedes cobraría por una** | 🤖 | ✅ **ESCRITO 08-sep (D-232).** El pago de sede escribe `monto_cop_recibido` — arregla el histórico **y las comisiones** sin tocar quien las calcula — y el MRR pasa a sumar sedes activas. Migración `20260908120000` y **control 208** de comportamiento. ✅ **APLICADA el 08-sep** con respaldo previo (`Backup_2026-09-08_18-56-08`): dos `CREATE FUNCTION` y `COMMIT` limpio. **El control 208 falló en su primera corrida por culpa del propio control** — `branches.slug` es obligatorio y el fixture no lo ponía— y una segunda por el disparador que ya crea la suscripción de cada sede. **CONTROL 208: 5 de 5 en verde el 08-sep**, con `ROLLBACK` limpio |
| 9.35 | **El Panel no deja ver el estado de cada sede.** Lista negocios, y las sedes salen solo como número. Tras un pago de sede, el panel se ve igual antes y después | 🤖 | ✅ **CERRADO 09-sep (D-235).** Nace `platform_get_tenant_branches` y la ficha del negocio muestra cada sede con su estado, precio y vencimiento. **La regla de "al día" se copia de la que ve el salón**, para que no signifique cosas distintas a cada lado del teléfono. Control **209** con 6 comprobaciones. `analyze` 0/0 y 391 pruebas. **Pendiente aplicar la migración** *(Nota del 23-sep: **aplicada** — `platform_get_tenant_branches` está viva; la usan el 9.36 y D-252.)* |
| 9.36 | **No hay dónde tocar el precio ni el estado de una sede.** `platform_set_branch_subscription` existe desde D-190 y **no la llama nadie**, así que la promesa de D-222 — *"las sedes adicionales van a tarifa vigente"* — era imposible de cumplir para los negocios que ya existían | 🤖 | ✅ **CERRADO 09-sep (D-236).** Botón por sede con estado, precio, motivo y vencimiento, solo para el dueño de plataforma, y **aviso en rojo al mover la fecha**. Control **210**, el primero de esa función. `analyze` 0/0 y 391 pruebas. **Pendiente correr el control** *(Nota del 23-sep: el control 210 **se corrió y quedó en verde**, ver el 9.37.)* |
| 9.37 | 🔴 **No se podía quitar el precio pactado de una sede.** `coalesce(p_price_cop, price_cop)` conserva el valor al recibir vacío, así que la función sabía poner y cambiar pero nunca limpiar — y el diálogo de D-236 prometía *"vacío = tarifa vigente"*. Lo encontró el propietario intentando cumplir D-222 | 🤖 | ✅ **CERRADO 09-sep (D-237).** Parámetro explícito `p_limpiar_precio`, y el lector dice `tiene_precio_pactado` para que la pantalla no deduzca el acuerdo comparando un texto. **Segundo fallo hallado al arreglarlo:** el campo se prellenaba con el precio efectivo y guardar sin tocar nada congelaba el de lista. Control **210** a 8 comprobaciones. ✅ **APLICADA el 09-sep.** Falló la primera vez por faltar el `DROP` del lector — cambiar el tipo de retorno lo exige igual que añadir un parámetro — y no aplicó nada. **El control 210 confirmó el arreglo:** sus comprobaciones 6 y 7, las que prueban quitar el precio, pasaron en verde |
| 9.38 | 🔴 **Crear una sede no la hace aparecer en la aplicacion.** El aviso promete que ya se le pueden asignar servicios y estilistas, pero Configuracion no recarga el contexto, que es donde vive la lista de sedes. **Con una sola sede el selector ni siquiera es un menu**, asi que no hay nada que pulsar. Lo encontro el propietario tras crear la segunda sede de Prueba Barberia Elite | 🤖 | ✅ **CERRADO 09-sep (D-238).** `onSedeCreada` obligatorio de Configuracion a `main.dart`, enganchado al mismo refresco del badge de prueba. No cambia de sede solo: la lista se actualiza y decide el propietario. Guardian nuevo `sede_creada_se_ve_test.dart`, **cuya primera version daba verde con la llamada comentada** — ahora lee el codigo sin comentarios. Sin migracion: el servidor ya estaba bien. `analyze` 0/0 y **393 pruebas en verde**. ✅ **VERIFICADO en produccion el 16-sep:** el propietario creo una segunda sede y el selector se convirtio en menu desplegable con las dos, sin pulsar F5 |
| 9.39 | **El Panel es una lista con ventanitas.** La ficha del negocio se abre como hoja emergente que tapa la pantalla y **se cierra en cada accion**, asi que no hay forma de trabajar un cliente de seguido. Pedido con un bosquejo a mano el 16-sep | 🤖 | ✅ **CERRADO 16-sep (D-240).** Maestro-detalle a partir de 1.000 px: lista a la izquierda, ficha fija a la derecha que ya no se cierra al actuar. Un solo constructor para las dos casas. Las sedes suben a tarjeta propia. `ValueKey` por negocio y seleccion revalidada en cada construccion --los dos fallos que no darian error--. Guardian `ficha_del_negocio_correcto_test.dart`. `analyze` 0/0 y **396 pruebas** |
| 9.40 | 🔴 **El historial de pagos no dice de que sede fue cada cobro.** `subscription_payment_intents` lo sabe desde D-191, pero `TenantSubscriptionHistoryEntry` no tiene campo de sede y el dato se tira. Con el cobro yendo por sede (D-239), el historial es una lista de importes sin duenyo --y es literalmente lo que el propietario no supo explicar al pagar Barberia Elite | 🤖 | Pendiente. Subir la sede al lector y al modelo, y mostrarla en cada renglon. **Toca el camino del dinero: lleva control propio** |
| 9.41 | **No se puede elegir que modulos ve cada sede.** Varios clientes dijeron que no necesitan la mitad; D-239 decide que el duenyo de plataforma los elija por sede aunque el plan de derecho a todos. Hoy `get_my_entitlements()` resuelve por negocio via `get_my_tenant_id()` y **no sabe que es una sede** | 🤖 | Pendiente. Servidor primero (donde se guarda la seleccion y dimension de sede en la resolucion), app despues --solo 3 archivos y 10 referencias--. **Ojo:** al cambiar de sede hay que volver a resolver los modulos, que es la misma costura de D-238 |
| 9.42 | **La sede no tiene vida propia.** El Panel partido en dos (D-240) ensenya a la derecha el negocio con una lista de sedes dentro, y el propietario pidio lo contrario: la sede, con **su** contacto, **su** correo y **su** direccion, que en la realidad seran distintos de los del duenyo | 🤖 | ✅ **CERRADO 16-sep (D-241).** Columna `manager_name` nueva --lo unico que faltaba de verdad--; el lector devuelve los siete campos; nace `platform_update_branch_info`, **la puerta que faltaba desde julio**. Escribe siempre los siete y sin defaults: vacio vacia, para no repetir el `coalesce` de D-237. Sedes en pestanyas con el estado de pago en un punto de color. Control **211** con 9 comprobaciones y 5 pruebas de modelo. `analyze` 0/0 y **401 pruebas**. ✅ **APLICADA el 16-sep** con respaldo previo (`Backup_2026-09-16_15-59-36`), `COMMIT` limpio a la primera. **CONTROL 211: 9 de 9 en verde**, incluida la 5 --escribir vacio vacia--, que es la que impide repetir el `coalesce` de D-237 |
| 9.43 | 🔴 **Configuracion escribe siempre la sede principal.** `get_business_settings` y `update_tenant_contact_info` hacen `is_primary = true`: dentro de la segunda sede, la casilla de direccion ensenya la de la primera **y al corregirla la pisa, sin aviso**. Encontrado el 16-sep al construir la puerta del salon | 🤖 | ✅ **CERRADO 16-sep (D-242).** La direccion **sale** de los datos del negocio --mientras dos casillas escriban la misma columna, una pisa a la otra-- y nacen `get_branch_info` y `update_branch_info`, protegidas **por sede** con `beautyos_resolve_branch_access`: un admin de la sede A no alcanza la B. Tarjeta nueva "Datos de esta sede" en Configuracion, que se rehace al cambiar de sede. Control **212** con 9 comprobaciones. `analyze` 0/0 y **404 pruebas**. ✅ **APLICADA el 16-sep** con respaldo previo (`Backup_2026-09-16_18-35-15`), los dos `DROP` y `COMMIT` limpio. **CONTROL 212: 9 de 9 en verde a la primera.** Se publico antes de migrar: de los dos huecos posibles, ese degrada con un aviso en vez de reventar el guardado |
| 9.44 | **La lista de clientes ensenya el nombre del negocio y su tarifa, y ninguna de las dos cosas le toca.** Un cliente es una persona, y desde D-239 el precio se pacta por sede. Pedido por el propietario el 16-sep | 🤖 | ✅ **CERRADO 16-sep (D-243).** La tarjeta encabeza con el propietario y baja el negocio a segunda linea; el negocio se repite en cada sede; **el plan y la tarifa salen de la lista** y viven en la ficha de cada sede. Columna de 400 a 470 px: partia "Exportadora" a la mitad. `analyze` 0/0 y **404 pruebas** |
| 9.45 | **La lista de clientes ensenya datos que no son del cliente, y la ficha esconde las sedes en una tarjeta.** El nombre del negocio y el LTV son de la sede desde D-239; y las pestanyas de sede estaban a media ficha en vez de mandar sobre ella. Tres capturas del propietario, 16-sep | 🤖 | ✅ **CERRADO 16-sep (D-244).** La tarjeta ensenya persona, correo, celular, whatsapp, **sedes con su estado** y fecha de registro. Nace `branches_breakdown` con el patron de `team_breakdown` (D-162). Las pestanyas suben a la cabecera y la sede pasa a ser la tarjeta 1. Control **213** con 6 comprobaciones. `analyze` 0/0 y **404 pruebas**. ✅ **APLICADA el 16-sep** (`Backup_2026-09-16_19-38-13`), `COMMIT` limpio. **CONTROL 213: 6 de 6 en verde a la primera** |
| 9.46 | 🔴 **P0: nadie puede registrarse.** `register_tenant` busca el plan `'profesional'`, jubilado por D-188 el 01-sep. Dieciseis dias sin poder dar de alta a nadie, y lo descubrio un invitado a probar la app, no una prueba | 🤖 | ✅ **CERRADO 17-sep (D-245).** El plan deja de buscarse por codigo --escribir 'pro' repetiria la trampa--: se toma el unico activo y, si hay cero o varios, se para y dice cual es el caso. Nace el **control 214**, el que faltaba: recorre el registro entero. Su comprobacion contra la regresion **ejecuta** en vez de leer la fuente, tras dos veredictos falsos por culpa de un comentario. ✅ **APLICADA** (`Backup_2026-09-17_11-04-15`). **CONTROL 214: 7 de 7 en verde** |
| 9.47 | **No hay forma de limpiar los negocios de prueba antes de que entren los diez socios.** Pedido por el propietario el 17-sep: *"no quiero ver una APP llena de clientes ficticios"* | 🤖 | ✅ **CERRADO 17-sep (D-246).** `platform_delete_demo_tenant`, que **se niega si el negocio no esta marcado como prueba**. Descubre las tablas en `information_schema` --el repositorio solo conoce 25 y le faltan las grandes--, borra a pasadas para no calcular el orden de las claves, y **verifica que no quede una fila**. Rastro en `deleted_demo_tenants` con el dinero. Boton en la ficha, solo en los demos, con el nombre escrito a mano. Control **215** con 8 comprobaciones. ✅ **APLICADA** (`Backup_2026-09-17_11-31-07`), **8 de 8 en verde** |
| 9.48 | 🔴 **Que sea la clienta quien autorice publicar su foto y su reseña, no el salón en su nombre.** Pedido por el propietario el 18-sep después de subir una foto como estilista: *"al ser juez y parte quedaría mal… lo podría hacer así la cliente ni se dé por enterada"*. **Son dos consentimientos, no uno, y hoy ninguno se le pide a ella:** **(1) la foto** — `AddWorkPhotoDialog` tiene una casilla que marca **quien sube la foto**, incluido el propio estilista desde *Mi agenda* (**AQ**); **(2) la reseña** — se publica en la página pública **con el nombre real de la clienta** (`clients.name`, visto en `_ReviewTile`) y ese mismo nombre viaja al *Estudio de publicación* que arma la pieza para Instagram (D-169). **La clienta escribe su opinión; el salón decide publicarla con su nombre encima, y nadie le pregunta.** Y una tercera pieza que aparece sola al tocar esto: **AU**, el interruptor *Visible al cliente* que promete lo que no puede cumplir | 🤖 | ⬜ **Pendiente.** La tubería **ya existe y no hay que inventarla**: el **portal de la clienta** (D-167, paso 5.6) y el **enlace de reseña** son sitios donde ella ya entra, identificada con su PIN. El candado del servidor también existe y aguanta — probado el 18-sep: *"No se puede publicar en portafolio sin consentimiento de la clienta (Ley 1581)"* —; **lo que falta es que la llave la tenga ella.** Mientras se construye, lo honesto es **cambiar el texto de la casilla**: *"confirmo que pedí su autorización"* dicho por quien la pide, en vez de afirmar un hecho de un tercero ausente. **Cierra AQ, AU y la mitad de Ñ**, y toca **Ley 1581** — conviene revisarlo con quien lleve lo legal antes de dar por bueno el diseño |
| 9.49 | **Terminar el recorrido del cliente con personas reales: del catálogo al cobro, y de ahí al portal de la clienta y la reserva pública.** Venía a medias del 17-sep con los pasos 1 a 8 hechos | 👤🤖 | ✅ **CERRADO 18-sep (D-247).** Se hicieron los pasos **9 a 16** y se añadieron dos que no estaban en el guion — **19** (página pública y portal de la clienta) y **20** (reserva pública) —, más el **11b** que faltaba. **Doce hallazgos nuevos** (AM a AX) y cinco enunciados corregidos (AH, AK, AL, AG, N). **AJ quedó probado con dinero real**: $7.200 sobre $18.000. Los pasos **17 y 18 quedan aplazados a propósito**: el borrado se probó contra **Exportadora** — 17 filas, 10 tablas — y el negocio de prueba se conserva vivo como banco de pruebas |
| 9.50 | 🔴 **La puerta: el correo de confirmación deja de llevar enlace y pasa a llevar un código, y un acceso fallido deja de ser mudo.** Hallazgos **AH** y **AM**, más la mitad de **AL** | 🤖👤 | ✅ **CERRADO 19-sep (D-248).** **Código escrito el 18-sep (D-248)** y en verde: `flutter analyze` 0/0 y **411 pruebas** (suben de 404 por las 7 del aviso). **Falta lo que de verdad lo prueba:** que el propietario pegue la plantilla nueva en Supabase y **se registre alguien de cero con un correo real**. **Hasta entonces no se marca cerrado**, porque lo escrito solo demuestra intención — es la regla que costo D-245 **19-sep: primer intento probado y corregido (AY).** El código real trae **ocho** dígitos y la pantalla exigía seis; arreglado sin escribir la longitud a mano. **417 pruebas.** Sigue en curso hasta que alguien complete el registro entero. **19-sep, segunda corrección (AZ):** el propietario cerró la pestaña y se quedó sin dónde escribir el código; la pantalla de acceso aprende a recibirlo y lo ofrece sola ante `email_not_confirmed`. **VERIFICADO EN PRODUCCIÓN el 19-sep** por el propietario, de cero y hasta *«Cuéntanos sobre tu negocio»*. Cierra **AH**, **AM**, **AY**, **AZ**, **BA** y media **AL**. `analyze` 0/0 y **421 pruebas**. |
| 9.51 | 🔴 **Un teléfono, una clienta: la reserva pública deja de fabricar fichas duplicadas — y el tope antiabuso deja de saltarse con un espacio.** Hallazgo **AW** | 🤖👤 | ✅ **CERRADO 19-sep.** Migración `20260919180000_un_telefono_una_clienta_aw.sql` y **control 216** escritos el 19-sep. **Al escribirlo apareció lo que el hallazgo no decía:** `c.phone = <texto>` estaba **tres** veces en la función, y dos son los topes de **H-02** — 4 citas futuras y 8 en 24 horas —, así que **el tope se saltaba cambiando el formato del número**. No era higiene de datos: era la puerta de la agenda. **Falta aplicarla en Supabase y correr el control**, que es lo único que lo prueba **APLICADA y CONTROL 216 en verde (6 de 6)** el 19-sep. *(El primer intento dio FALLO 5 y era del control, no de la función: la lista de formatos incluía `+57 3506815629`, que en dígitos son doce. Corregido y anotado dentro del propio control.)* |
| 9.52 | 🔴 **Un teléfono, una persona: el celular pasa a ser la llave de la clienta, con formato impuesto al escribirlo.** Decisión del propietario, 19-sep | 🤖👤 | ✅ **CERRADO 19-sep (D-249).** *"Un numero de telefono no se puede compartir (ni con la hermana ni con la mama ni con nadie), ese numero sera nuestra llave de usuario"*, y el argumento no es de higiene sino de consentimiento: **si dos personas comparten llave, una autoriza publicar las fotos de la otra y el portal le enseña su historial**. Emparenta con **AQ** y con el paso **9.48**. Más: **el campo exige diez dígitos** y el indicativo lo pone el sistema según el país, con lo que el caso `+57` desaparece de raíz en vez de parchearse. **Orden obligado, porque un índice único no se puede crear sobre duplicados vivos:** (1) contar los que hay, (2) fundir fichas — herramienta nueva, y los cuatro David de la Peluquería son su banco de pruebas —, (3) normalizar lo guardado, (4) índice único, (5) el campo en la pantalla. **El «10» no se escribe a mano en cinco sitios**: uno en la base y uno en la aplicación, marcados, porque esa trampa ya costó D-245 y **AY** esta misma semana **APLICADO Y VERIFICADO.** Migración aplicada y **CONTROL 217 en verde, 8 de 8**. **No hizo falta la herramienta de fundir fichas**: el propietario ya había separado los duplicados, y la consulta devolvió cero. Nacen `private.beautyos_celular_valido` (base) y `CelularColombiano` (app) — **los dos únicos sitios con el 10** —, el índice único parcial, y el campo de diez dígitos en **Clientes y en la reserva pública**. `analyze` 0/0, **429 pruebas**. Probado en producción por el propietario. |
| 9.53 | 🔴 **El dueño no podía dar por terminado un servicio, y sin eso no nacía la comisión aunque la clienta pagara.** Hallazgo **AP** | 🤖👤 | ✅ **CERRADO 20-sep (D-250).** **No faltaba permiso, faltaba el botón:** el servidor autorizaba a dueño, admin y recepción desde julio, y el lector de servicios ya existía. **Cero migraciones para esa parte.** Nace la ventana *Atender servicios* en la ficha del ticket, con el paso que toca por servicio y pregunta antes de finalizar. La tabla de estados se mudó a `AccionesDeTicket`: estaba escrita a mano en la pantalla del estilista y copiarla habría sido **el cuarto golpe del mismo patrón en cuatro días** (D-245, **AY**, el celular) — el primero cortado antes de costar algo. **El control destapó una segunda mitad que el hallazgo no decía:** la comisión solo se calculaba al entrar un pago, así que **un anticipo del 100% la mataba para siempre**. D-163 había descrito ese caso palabra por palabra y puesto el remedio… **en una puerta que no se puede abrir**. Migración `20260920120000` se lo pone a la que sí se usa, escrita comparando línea por línea contra el texto vivo (152 → 153). `analyze` 0/0 y **436 pruebas**. **CONTROL 218: 9 de 9 en verde**, probando **los dos órdenes** de cobro. **Verificado en producción por el propietario**, el segundo orden con el usuario del asistente: pago por transferencia durante la atención, finalizado después, ticket cerrado y la comisión visible en *Mi panel financiero* del estilista |
| 9.54 | 🔴 **La comisión nacía decidida: un 40% heredado de un default de tabla que nadie confirmaba.** Hallazgo **AJ** | 🤖👤 | ✅ **CERRADO 20-sep (D-251).** Medido primero: **3 políticas en la base, las 3 en el 40%, 0 tocadas desde que nacieron**. El propietario descartó que naciera en 0% — *también es un número inventado, solo que más barato para el salón* — y eligió **avisar sin bloquear**. Columna `confirmed_at`; **guardar es confirmar**, aunque no cambie ningún número. Aviso en los tres sitios: quinto paso de *Primeros pasos*, recuadro en *Configuración*, y una nota en el panel del estilista mediante `my_commission_policy_is_confirmed`, que devuelve **solo un booleano** para que sepa si la aprobaron sin ver el porcentaje. La tabla entra en una migración (un pedazo de **AI**, que **no** queda cerrado). `analyze` 0/0 y **444 pruebas**. **CONTROL 219: 9 de 9**, con la comprobación 3 vigilando que **no** se rompa la operación. **Verificado en producción por el propietario** en los tres sitios |
| 9.55 | 🔴 **Dos precios y DOS BOTONES DE PAGO en la misma pantalla, y el de arriba cobraba por un camino que no activa ninguna sede.** Hallazgo **AG**, parte 1 | 🤖👤 | ✅ **CERRADO 22-sep (D-252).** Medido primero: **3 funciones en el camino del dinero**, **los tres negocios con una sede a tarifa de lista** mientras su negocio decía otra cosa, y **2 cobros en toda la historia, los dos de negocios de prueba**. Los tres acuerdos bajan a sus sedes con guardia que aborta nombrando las huérfanas. **No se retira la función de cobro: se cierra la puerta** — el webhook usa ese mismo cálculo para liquidar, y borrarlo habría hecho que un pago confirmado dejara de registrarse. Fuera el botón de pago del negocio y las cifras inventadas del Dart. **CONTROL 220: 7 de 7** |
| 9.56 | 🔴 **La función que aprueba clientes nuevos seguía escribiendo el 50% de pionero que D-221 retiró.** Hallazgo **AG**, parte 2 | 🤖👤 | ✅ **CERRADO 22-sep (D-252).** Salió de una pregunta del propietario: *"¿puedo seguir definiendo precios como yo quiera a quienes entren en adelante?"*. El 50% tenía **seis escondites**, uno de ellos esta función y otro un cartel que se lo prometía por escrito al salón. **Aprobar escribe el precio en la sede principal**, un pionero sin precio se niega, y el descuento porcentual se rechaza por las dos puertas. **CONTROL 221: 9 de 9, y vigila LAS DOS PUERTAS** — que es lo que al 207 le faltó |


---

## 6. BUZÓN DE IDEAS

**Aquí caen las ideas cuando aparecen, sin interrumpir lo que se está
construyendo.** Cada una se mira al cerrar una fase y se manda a la que le
toque, o se descarta con su motivo.

| # | Idea | De cuándo | Destino |
|---|---|---|---|
| I-01 | Citas recurrentes **mensuales** (hoy solo diaria y semanal) | 07-ago | Sin asignar |
| I-02 | Crear una serie larga de citas es lento | 07-ago | Junto con I-01 |
| I-03 | **Cierre de sesión por inactividad** (30 min) — el riesgo real es la recepcionista que deja la sesión abierta | 07-ago | Sin asignar |
| I-04 | **Verificar el celular del cliente con un código** al reservar. Cierra del todo H-02. Cuesta por mensaje | 06-ago | Sin asignar |
| I-05 | **Que la reserva pública no ocupe el horario** hasta que el negocio confirme. Arreglo estructural de H-02 | 06-ago | Sin asignar |
| I-06 | **Pagos en línea para los negocios clientes**, que cada salón cobre a sus propias clientas. ⚠️ No confundir con ePayco cobrando la suscripción: es una pasarela por negocio, cambia la figura legal | 09-ago | Sin asignar — con contador |
| I-07 | Días específicos por sede para un estilista | 27-jul | Bajado de prioridad (D-074) |
| I-08 | Paquetes / membresías de sesiones para el cliente final | 28-jul | Pausado hasta que un negocio real lo pida |
| I-09 | Propinas — no existe ni la columna | 08-ago | Sin asignar |
| I-10 | Vista de ausencias de todo el equipo para el administrador | 27-jul | Sin asignar |
| I-11 | **Correos del salón a SUS clientas**: cumpleaños, recordatorios de cita. Idea del propietario, 09-ago | 09-ago | **No existe nada.** Decidido cómo se hará cuando toque: salen del dominio propio pero **con el nombre del salón como remitente** y respuesta al correo del salón, para que la clienta vea *"Naguara de Uñas"* y no *"Salón y Más"*. Van por **subdominio aparte**, para que si algún día caen en spam no arrastren a los correos de negocio. Fase 6 |
| I-12 | **Reenvío gratuito de `hola@salonymas.com` al Gmail del propietario** con Cloudflare Email Routing | — | ✅ **Cerrado 30-ago (D-180, Paso 8.7).** Enrutamiento en Cloudflare hacia `juankdev2026@gmail.com`. Arquitectura de cero colisiones con Resend (send vs raíz) y guía documentada en `docs/02_operacion/CORREO_Y_DOMINIO.md` |
| I-14 | ~~Decidir la matriz definitiva: qué módulo lleva cada plan.~~ | — | ✅ **RESUELTA 01-sep (D-188), y sin decidirse: ya no hay matriz.** Se retiró la escalera de tres planes y quedó uno solo con todo dentro, cobrado por sede. Las 15 casillas que esperaban cinco visitas a salones reales dejaron de existir. La idea del propietario —*«que los pequeños accedan a lo que solo tienen los grandes»*— se cumple entera: el salón de una sede paga menos que antes y tiene todo |
| I-13 | **Darle al asistente acceso directo a Supabase** con el conector oficial y un token revocable | 09-ago | Se aplazó el 09-ago hasta rotar las claves. **Ya están rotadas (D-127), así que la condición se cumplió.** Hoy el asistente dicta clics porque la extensión de Chrome tiene bloqueado el dominio de Supabase; con el conector leería registros y desplegaría funciones solo. **Es acceso permanente a producción: decisión del propietario, no del asistente** |
| I-15 | **El desglose "Ventas por Servicio" de Reportes (Nivel 3, D-154) puede cambiar de período con el tiempo.** `get_branch_reports_v3` ubica cada venta con `coalesce(t.closed_at, t.scheduled_at)`: un ticket "finalizado" (servicio prestado, saldo pendiente) aparece en el reporte del día del servicio mientras no se cierra; si se cierra días después, la venta se traslada silenciosamente al reporte del día de cierre. El total financiero de arriba (`total_received`) no tiene este problema porque usa `tp.received_at`, que es estable | 18-ago | Sin asignar — decidir a propósito si el desglose por servicio se ancla a `scheduled_at` (estable, mismo criterio que D-153) o se mantiene con `closed_at` (fecha de cierre contable, coherente con D-150). Hallazgo de la auditoría técnica del Paso 4.7 |
| I-16 | **Branding e identidad visual integral de Salón y Más** (diseño del logo definitivo, paleta extendida, manual de marca y kit de activos en alta resolución para marketing, pasarelas y material promocional). El propietario solicitó dejarlo explícitamente como último punto pendiente. | 23-ago | Último punto del backlog / Post-lanzamiento |
| I-17 | **El duenyo y el administrador ven exactamente lo mismo, y quiza no deberian.** Observado por el propietario el 17-sep recorriendo el alta de un salon: *"no se si al propietario le interese por ejemplo la agenda... a el solo le interesara dinero, finanzas, tendencias, proyecciones"*. Hoy los dos menus son identicos --quince entradas-- mientras que el de asistente son tres. **El asistente si esta bien delimitado; la frontera que falta es la de arriba.**  **Evidencia nueva del 18-sep, y esta vez con dinero:** el paso 13 del recorrido se hizo sin querer **desde la cuenta de la administradora**, y su Dashboard y sus Reportes son **identicos a los del duenyo** — incluidas **Ganancia Neta** y **Comisiones por Estilista**. O sea que la administradora ve **la rentabilidad del negocio y lo que gana cada companyero**. Ya no es "quiza no deberian" ver lo mismo: es que lo que comparten es justo lo mas sensible.| 17-sep | Aplazada por el propietario: no hay punto de inflexion claro todavia. Material de **D-219**, y probablemente no se resuelva bien sin el **paso 9.24** --observar un dia en un salon-- porque es justo la pregunta de quien usa que |
| I-18 | **Que la agenda abra en «Cada 1 hora» y no en «Cada 15 min».** Pedido por el propietario el 18-sep recorriendo el paso 12: con 15 minutos el día son **56 filas** y hay que buscar la cita; por hora cabe de un vistazo. Hoy el valor inicial es `'15min'` (`agenda_page.dart:120`) y el selector ya ofrece las tres opciones, así que el cambio es una palabra | 18-sep | Sin asignar. **Barato, pero decide por todos los sal ones:** una barbería de cortes de 20 minutos quizá quiera lo contrario. Lo correcto es **recordar la última elección de cada quien**, que cuesta poco más y no impone criterio |
| I-19 | **Rehacer el layout de todas las pantallas, para todos los roles.** Pedido por el propietario el 20-sep al cerrar AJ: *"más adelante también deseo reformar el layout de todas las pantallas de todos los roles"* | 20-sep | Sin asignar. **No es el 9.25 y conviene no confundirlos:** aquel cambia **dónde se entra** — 15 módulos a 5 lugares, D-219 — y esto cambia **qué hay dentro de cada pantalla**. Emparenta con **I-17** (dueño y admin ven exactamente lo mismo) y con **I-16** (identidad visual). **Va después del 9.25, no antes:** rediseñar quince pantallas que luego se reagrupan en cinco sitios es hacer el trabajo dos veces — y el 9.25 sigue bloqueado por el **9.24**, las entrevistas a salones reales |

---

## 7. Deuda y hallazgos abiertos

> ✅ **`epayco_checkout_service.dart` quedó obsoleto con D-188 y nadie lo notó.**
> Ofrecía un desplegable con los tres planes retirados y sus precios viejos, y
> elegía `profesional` por defecto. El cobro salía bien, pero al dueño se le
> enseñaba un precio que no era el suyo. **Cerrado el 02-sep (D-193)**, y la
> corrección no fue actualizar las cifras: fue quitarlas del cliente, porque esa
> cuenta vive en el servidor.

### De la auditoría integral del 06-ago

| # | Hallazgo | Estado |
|---|---|---|
| H-01 | Rol Asistente sin pantallas | ✅ Cerrado (D-092) |
| H-02 | Reserva pública sin protección | ✅ Mitigado (D-092). Fondo en I-04, I-05 |
| H-03 | Sin pruebas de dinero ni roles | 🔄 Mitad hecha (D-121). **La otra mitad quedó desbloqueada el 12-ago:** ya existe base de ensayo (2.2) contra la que correr las pruebas solas |
| H-04 | Claves expuestas sin rotar | ✅ **Cerrado 09-ago (D-127)** |
| H-05 | 42 funciones heredadas | ✅ **Cerrado 30-ago (D-176, Paso 8.1)** |
| H-06 | Documento rector desactualizado | ✅ Cerrado — y este documento lo reemplaza |
| H-07 | `pubspec.lock` sin versionar | ✅ Cerrado (D-091) |
| H-08 | Columnas de dinero inconsistentes | ✅ **Cerrado 30-ago (D-175, Paso 8.2)** |
| H-09 | Archivos públicos permanentes | ✅ Cerrado (D-119) |
| H-10 | `social_publishing` sin nada detrás | 🔄 Es la fase 6 |
| H-11 | Permiso suelto de Storage | ✅ **Cerrado 30-ago (D-174, Paso 8.3)** |
| H-12 | Resend en sandbox: los correos no llegan | ✅ **Cerrado 10-ago (D-128)** |
| H-13 | Commits sin publicar | ✅ Cerrado |

### Anotados en el camino

| # | Hallazgo | Destino |
|---|---|---|
| **D** | La barra de celular podría llevar **acciones**, no módulos | Paso 4.10 — ✅ **Cerrado con su paso** (D-157). *(Marcado el 23-sep: la fila nunca se actualizó al cerrar el paso.)* |
| **G** | Restauración de ensayo del respaldo | Paso 2.2 — ✅ **Cerrado con su paso** (D-134). *(Marcado el 23-sep.)* |
| **N** | `TicketStatusBadge` contradice a D-097 y D-101. **Evidencia en vivo del 18-sep, y en la misma pantalla:** el ticket recién creado luce la etiqueta **«Solicitado»** y el filtro que lo contiene se llama **«Por confirmar»**. No es un fallo, es el disenyo de D-101 — `etiqueta` da el estado técnico (once) y `grupo` da lo que te exige (cinco) —, pero **quien mira la pantalla ve dos nombres para lo mismo y no tiene forma de saber que son dos sistemas** | Paso 4.5 |
| **Ñ** | **El flujo de las fotos no está definido:** quién las toma, cuándo, por dónde. Y los tipos "Final" y "Portafolio" sobran | Pasos 4.9 y 5.7 **El flujo lo definió el propietario el 23-sep (D-260):** *"esta foto solo se toma con el consentimiento de la persona estando allí, y solo ella puede habilitar o autorizar su publicación desde el link que se le envía"*. Es decir: tomarla, con la clienta presente y de acuerdo; publicarla, **solo ella**, desde su enlace. Queda por decidir lo de los tipos *Final* y *Portafolio* |
| **O** | Rediseño del panel de plataforma | Paso 4.11 — ✅ **Cerrado con su paso** (D-158). *(Marcado el 23-sep.)* |
| **P** | **El consecutivo de ticket no sirve como número contable** | Paso 4.4 — ✅ **Cerrado con su paso** (D-150). *(Marcado el 23-sep.)* |
| **Q** | **Ningún módulo se actualiza solo.** Empezó como "que el administrador se entere cuando el estilista finaliza", pero el 09-ago se comprobó que es general: **entrar a un módulo no recarga sus datos**, hay que pulsar Actualizar o F5. Se vio con las fotos, y aplica igual a Tickets, Clientes y el resto | ✅ **CERRADO 03-sep (D-201, paso 8.23).** Estuvo abierto **tres semanas y media sin causa raíz identificada**; la encontró la auditoría del 01-sep en TL-10: `IndexedStack` construía los 16 módulos al abrir sesión, así que volver a una pestaña no recargaba nada porque nunca se había ido. Ahora cada entrada remonta el módulo y su `initState` recarga. **Excepción deliberada: Configuración**, donde se escribe a mano |
| **R** | 🔴 **Se puede invitar a DOS cuentas distintas al MISMO estilista del catálogo.** Lo encontró el propietario el 10-ago: invitó `elboga010` vinculándolo a "Erick Chaparro", que ya tenía cuenta con `elboga005`. **Quedaron dos usuarios llamados Erick Chaparro apuntando al mismo estilista.** **No es cosmético:** las dos cuentas ven la misma agenda, las mismas comisiones y las mismas fotos, porque todo cuelga del `stylist_id`, y no hay forma de saber cuál es la persona real. Se arregla **rechazando la invitación** cuando ese estilista ya tiene una cuenta activa vinculada. *(El duplicado real quedó suspendido, no borrado.)* | ✅ **Cerrado 11-ago (D-132).** Se adelantó a la fase 4 a petición del propietario, y la regla de oro pide decir por qué: **el duplicado ya existía en producción y cada invitación nueva podía crear otro.** Verificado con 7 controles y una prueba de escritura real |
| **Y** | 🔴 **El respaldo llevaba 4 días sin incluir el esquema `private`**, donde viven las **18 funciones que autorizan cada operación del negocio** (`beautyos_resolve_branch_access` la primera) y los disparadores que numeran el ticket. **Restaurarlo habría devuelto todos los datos con la aplicación inutilizada:** ni agenda, ni cobrar, ni fotos. `respaldo_supabase.ps1` volcaba solo `public`, `auth` y `storage` desde el 08-ago (D-111). **Nadie podía saberlo porque nadie había restaurado nunca un respaldo** | ✅ **Cerrado 12-ago (D-134).** Lo encontró el propio paso 2.2, que es exactamente para lo que existe. **Regla nueva en el guion: un esquema nuevo se añade a esa línea en el mismo cambio** |
| **Z** | **Al restaurar, el registro de archivos de Storage no vuelve.** `storage.objects` pasó de 11 filas a 0: en un Supabase gestionado no eres superusuario y esas tablas de plataforma no se dejan escribir. **Lo que SÍ vuelve es `public.work_photos`** (6 = 6), con el ticket, el cliente y el estilista de cada foto — o sea, **no se pierde a qué pertenece cada imagen**, solo hay que volver a subirlas desde `respaldo_archivos.ps1` | Documentar el procedimiento de recuperación de fotos. Fase 8, o antes si entra un cliente con muchas fotos |
| **X** | **La contraseña de la base de datos nunca se ha rotado.** No formaba parte de H-04 —aquello eran las claves `service_role` y `secret`— y por eso se quedó fuera del paso 2.1. Se escribe a mano en cada migración con `aplicar_sql.ps1` y **no se guarda en ningún archivo**, que es lo correcto. **No hay ninguna fuga conocida:** esto es higiene, no incendio | Fase 8, con el resto de limpieza. **Pasa a urgente si alguna vez se teclea en una sesión compartida, se pega en un chat o aparece en una captura** — ✅ **Decidido el 08-sep (D-226): no se rota**, con la condición de arriba viva. No es trabajo pendiente: es vigilancia. |
| **V** | **No hay forma de dar acceso a una segunda sede a alguien que ya tiene cuenta.** Encontrado el 11-ago verificando el hallazgo R: solo **tres** funciones insertan en `branch_memberships` — `register_tenant`, `create_branch` y `accept_team_invitation` — y ninguna sirve para eso; `create_branch` solo mete a quien crea la sede. **Consecuencia real con las dos sedes de hoy:** puedes asignar a una estilista a trabajar en la sede 2 en el catálogo (`branch_stylists`) y **su cuenta no podrá verla** (`branch_memberships`). Son dos cosas distintas y hoy solo se puede tocar una | ✅ **Cerrado 30-ago (D-179, Paso 8.6).** Migración `20260830170000`, RPCs `get_tenant_user_branches` y `set_tenant_user_branches` con sincronización en `branch_stylists`, modelo `UserBranchAccess` y selector interactivo de sedes en `_ManageUserDialog` (`UsuariosPage`). Control `196` y 262 pruebas en verde |
| **S** | **La pantalla de acceso solo le habla a los dueños.** Dice *"¿No tienes cuenta? Crea tu negocio"*, pero por ahí también entra un **empleado invitado**, que no viene a crear ningún negocio. Observación del propietario, 10-ago | ✅ **Cerrado 30-ago (D-178, Paso 8.5).** Subtítulo neutro en login ("Ingresa a tu cuenta") con guía para invitados por correo y botón destacado "Registra tu negocio gratis"; `RegisterPage` titulada "Registra tu negocio en Salón y Más" con enlace de retorno que asiste a empleados invitados |
| **T** | **`send-low-stock-alert` sigue rota por la misma causa que D-128.** Usa `withSupabase` con la dependencia anclada como `^1` | **Va a fallar igual que la de invitaciones.** Mismo arreglo: quitar esa librería y fijar la versión — ✅ **Cerrado el 11-ago** en el paso **2.7** (D-131) |
| **U** | **Las dos Edge Functions se pueden ejecutar sin ninguna cuenta** (`verify_jwt = false`). | ✅ **Cerrado 30-ago (D-177, Paso 8.4)** |
| **W** | **El fallback del cargo devuelve el precio escrito a mano en el borde, y eso contradice a D-193.** Si la RPC de cálculo no devuelve fila, `create-epayco-session` calcula el monto leyendo `tenant_subscriptions` y, si no hay nada, usa `150000` y el plan `pro` literales dentro de la Edge Function. D-193 quitó exactamente esas cifras del cliente **porque esa cuenta vive en el servidor**; el fallback las devuelve al borde. Hoy coincide con D-189, pero un cambio de precio en la base **no** llega a esa línea. Encontrado el 06-sep al cerrar D-214 **Novedad del 06-sep (D-215):** ahora se sabe **detectar** si el fallback actuó — deja el aviso `Aviso al ejecutar RPC calcular cargo, usando fallback directo` en los registros, y su ausencia fue lo que probó que los wrappers funcionan. Detectarlo no es decidirlo: la pregunta de fondo sigue en pie | ✅ **CERRADO 08-sep (D-227).** Se retiró el fallback en vez de decidir cuándo confiar en él: no había caso legítimo. Y tenía tres defectos que este hallazgo no recogía — descuentos vencidos, la sede cobrada como negocio, y el prorrateo de D-160 ignorado |
| **AA** | **`send-invitation-email` y `send-low-stock-alert` repiten a mano la resolución de claves en vez de usar `_shared/supabase_keys.ts`.** *(Enunciado corregido el 06-sep: al anotarlo dije que “siguen con claves heredadas” y que **fallarían** al desactivarse las legacy. **Es falso.** Ambas prefieren `SUPABASE_PUBLISHABLE_KEYS`, que sí está poblada, y solo caen a `SUPABASE_ANON_KEY` si aquella falta. Además operan en el contexto del usuario que llama, bajo RLS, y no necesitan clave secreta: **no están rotas hoy.** El grep de `SUPABASE_ANON_KEY` me hizo leer una línea de respaldo como si fuera la principal.)* Lo que queda es duplicación real: la misma cascada escrita en tres sitios, que es de donde sale que una se arregle y las otras no | Fase 8. Importar el módulo compartido, sin urgencia |
| **AB** | 🔴 **Las Edge Functions no pasan ninguna verificación estática, y por ahí se coló un cobro roto en producción.** El 06-sep, con la migración aplicada y las claves resueltas, el checkout seguía fallando con `calc is not defined`: la reestructuración de D-214 movió `const calc` dentro del `if` del cálculo y dejó dos referencias a él en el `return` final, fuera de su ámbito. **Cualquier comprobador de tipos lo caza en segundos**, pero el CI solo corre `flutter analyze` y `flutter test`, que no miran `supabase/functions/`. Es el mismo hueco que TL-07 cerró para Dart, todavía abierto para TypeScript | Fase 8. Añadir un `deno check` sobre `supabase/functions/**` al CI — ✅ **Cerrado el 09-sep con el paso 9.6** (D-234): el CI pasa `deno check` por cada Edge Function. *(Marcado el 23-sep.)* |
| **AC** | 🟡 **La primera carga de la app pesa 12 MB, y la caché obliga a revalidarla entera en cada apertura.** Medido el 08-sep: `main.dart.js` 4,8 MB y `canvaskit.wasm` 7,2 MB, con `max-age=0, must-revalidate` en `/*`. **No es capacidad y no empeora con más usuarios** (D-226). Y Flutter compila todo en un archivo: el Panel de Plataforma viaja al navegador de cada salón | Dos arreglos. **Barato:** cachear `canvaskit/*` un año — ojo, afecta a `web_headers_security_test.dart`. **De fondo:** carga diferida por módulo, que **es el mismo trabajo que D-219** |
| **AD** | 🟡 **Cada refresco del contexto vuelve a verificar el mismo pago de ePayco.** `_loadHomeContext` lee `ref_payco` de la URL y **nunca la limpia**, asi que toda recarga — el badge de prueba, el aviso de suscripcion, y desde D-238 tambien crear una sede — vuelve a invocar `verify-epayco-transaction` y a mostrar el aviso de D-200. No pierde dinero (la via autoritativa es el webhook, D-141) pero gasta una llamada por refresco y repite un aviso ya leido. Encontrado el 09-sep al conectar el refresco de sedes | Fase 9. Consumir `ref_payco` una sola vez y quitarlo de la URL. **Toca el camino del dinero: merece su propio paso y su control** |
| **AE** | 🟡 **"Ver todas mis sedes" existe, pero solo dentro de Dashboard y Reportes.** Son dos interruptores locales (`_consolidado`, D-194) en dos de dieciseis pantallas; **el selector de sede de la cabecera no tiene "Todas"**. Encontrado el 16-sep por el propietario al estrenar su segunda sede: *"me da opcion de escoger una sede en especifico, no tengo la opcion de ver Todas"*. Y le hizo ver algo mas grande: que la pantalla del **duenyo de negocio** y la del **encargado de una sede** deberian ser distintas | Material de la reestructuracion de D-219, no un parche. El ambito (una sede / todas) deberia ser una propiedad de donde estas, no un boton suelto por pantalla. **Sigue bloqueado por el 9.24** |
| **AF** | 🔴 **La direccion de una sede secundaria se podia pisar sin aviso desde la pantalla del salon.** `get_business_settings` y `update_tenant_contact_info` resolvian la sede con `is_primary = true`, ignorando en cual estabas: dentro de la segunda sede veias la direccion de la primera, y corregirla escribia sobre la primera. Sin error, sin rastro y sin forma de notarlo hasta comparar dos pantallas. Encontrado el 16-sep por el propietario, con dos capturas que decian direcciones distintas de la misma sede | ✅ **Cerrado el mismo dia (D-242, paso 9.43).** La direccion salio de los datos del negocio en vez de duplicarse. **Solo habia datos de prueba cuando se encontro**; con un cliente real de dos sedes habria borrado un dato bueno |
| **AG** | 🟡 **La tarjeta "Plan y Tarifa Mensual Fijada del Negocio" sigue ensenyando cifras que desde D-239 ya no deciden nada.** Dice *Precio Mensual Fijado $10.000*, *Periodos Pagados* y *LTV* **del negocio**, cuando el que cobra es la sede. Es el mismo residuo que se quito de la tarjeta de la lista en D-243, todavia en pie dentro de la ficha. Lo encontro el propietario el 16-sep comparando dos numeros en la misma pantalla: arriba su sede a 150.000 --tarifa vigente-- y abajo el negocio a 10.000. *"Lo extrano es que arriba dice 150 mil y abajo 10 mil."* **Los dos son correctos**, y ese es justo el problema: responden preguntas distintas sin decir cual  **Evidencia del 17-sep, y ya no es teorica:** al aprobar un negocio con 97% de descuento, su propia pantalla de Configuracion enseña **"Plan Todo Incluido — $4.500 COP/mes"** y, diez centimetros mas abajo, **"Peluqueria Exito Prueba · $150.000 al mes"** con un boton *Activar esta sede*. **Dos precios para lo mismo y dos botones que cobran distinto**, porque `beautyos_precio_efectivo_sede` hace `coalesce(bs.price_cop, p.price_cop)` y **ni el precio pactado del negocio ni su descuento entran en esa formula**.  **Y el patrón correcto ya existe en esa misma pantalla**, visto el 18-sep en la ficha de Exportadora: la sede dice *Cuota mensual: $80.000* y justo debajo *"Ese precio es: **Un acuerdo — Pionero fundador: $80.000 por sede (lista $120.000). D-188**"*. **Ahí el número viene rotulado con lo que es y de dónde sale.** No hay que inventar nada: hay que hacer lo mismo con la tarjeta del negocio.| ✅ **CERRADO 22-sep (D-252), pasos 9.55 y 9.56.** **De los dos caminos que este hallazgo ofrecía, el propietario eligió un tercero:** *"retirar el precio acordado por negocio y dejarlo por sede, al final es así como se haría en la realidad"*. **Y el enunciado se quedaba corto en lo grave:** no eran dos precios, eran **dos botones de pago en la misma pantalla**, y el de arriba cobraba por un camino que **no activa ninguna sede** — se pagaba y no pasaba nada. Medido: **los tres negocios tenían una sede a tarifa de lista** mientras su negocio decía otra cosa, y Peluquería Éxito habría cobrado **$150.000** diciendo $4.500. Al abrirlo aparecieron **tres versiones vivas** del cálculo de cobro que el repositorio no menciona, y que **el webhook usa para liquidar** — por eso no se retiró la función, se cerró la puerta. Y aparecieron **seis escondites más del 50% de pionero** que D-221 había retirado, uno en la función que aprueba clientes y otro prometido por escrito al salón. **CONTROL 220 (7/7) y CONTROL 221 (9/9)**, este último vigilando **las dos puertas** |
| **AH** | 🔴 **El enlace de confirmación del correo llega ya gastado: no caduca por tiempo, lo consume el escáner del propio correo.** *(Enunciado corregido el 18-sep. Al anotarlo el 17-sep dije que **caduca** y que se arregla subiendo el tiempo en Supabase. **Es falso, y el arreglo que proponía no habría cambiado nada.**)* Verificado con el estilista: correo enviado **12:38**, pulsado **12:40**, y devuelve `error_code=otp_expired`. **Y aun así la cuenta quedó confirmada** — entró con su contraseña acto seguido, y eso solo es posible con el correo ya verificado, porque `signUp` no devolvió sesión (señal de que *Confirm email* está activo). **Las dos cosas juntas solo encajan si el enlace, que es de un solo uso, ya se había gastado antes del clic**, y solo lo tenía el buzón: los escáneres antifraude del correo lo visitan y con eso lo queman **18-sep, D-248: arreglo escrito, sin probar todavía.** La plantilla pasa a `{{ .Token }}` — un código no se puede gastar visitándolo — y `register_page.dart` gana la pantalla para teclearlo, con reenvío. **Paso 9.50, en curso hasta que alguien se registre de verdad.** | **No es el ajuste de *Authentication → Email* que se dijo.** Pide cambiar la plantilla al flujo que no se gasta al visitarlo — código de seis dígitos, o `token_hash` contra una pantalla propia —, y que esa pantalla diga qué pasó (**AM**). Las seis plantillas están en `PLANTILLAS_CORREO_AUTH.md` (D-146). **Confirmarlo cuesta un minuto**: Supabase → **Logs → Auth**, buscar los `verify` de las 12:38–12:40; si hay **dos**, uno desde una IP de Google, queda probado. **Revisar antes de invitar a los diez** ✅ **CERRADO Y VERIFICADO EN PRODUCCIÓN el 19-sep.** El propietario se registró de cero con `elboga014`, recibió el código, usó uno caducado a propósito — rechazado correctamente y **sin perder lo escrito** —, pidió otro y entró. **Y lo confirmó desde el teléfono lo que había empezado en el computador**, que con el enlace de PKCE era imposible. |
| **AI** | 🟡 **El repositorio no contiene el esquema completo de la base.** Las migraciones solo crean **25 tablas**; `tenants`, `clients`, `tickets` y `work_photos` --las que mas datos tienen-- nacieron antes de que el proyecto usara migraciones y **no estan en ningun archivo**. Encontrado el 17-sep al disenyar D-246: una lista de tablas sacada del repositorio se habria dejado fuera justo esas. **Consecuencia real:** la base no se puede reconstruir desde cero con `supabase db reset`; solo desde un respaldo. Los respaldos SI las llevan (`schema.sql`), asi que no hay riesgo de perdida, pero si de creer que el repositorio describe la base | Fase 9. Volcar el esquema que falta a una migracion de base, o dejar escrito en `MAPA_TECNICO.md` que el repositorio **no** es la fuente del esquema. **Lo segundo es honesto y cuesta diez minutos; lo primero es correcto y cuesta un dia** |
| **AJ** | 🔴 **El salon nace con una comision del 40% que nadie le pide confirmar, y esa cifra decide lo que gana una persona.** Al registrarse se inserta su `commission_policies` con los valores por defecto de la tabla, y **"Primeros pasos" no la vigila**: la lista pide servicios, equipo, horario y primera cita -- la comision no aparece. Peor: **`commission_policies` no esta en ninguna migracion** (hallazgo AI), asi que ese 40% vive en un default de la base que el repositorio no contiene y **nadie que lea el codigo lo veria jamas**. Encontrado el 17-sep por el propietario recorriendo el alta de un salon: *"puso de una vez horarios y comisiones, y eso deberia ser configurable desde la primera vez para evitar errores de cifras preestablecidas"*  **PROBADO CON DINERO REAL el 18-sep, paso 12 del recorrido:** el ticket #0000001 de $18.000 se cobró y en *Mi panel financiero* de Erick apareció **$7.200**. **Es el 40% exacto.** Ya no es una cifra en un default de tabla: es una cuenta por pagar a una persona, generada sin que nadie la aprobara nunca. Y el estilista la ve antes que el duenyo, porque el duenyo no tiene esa pantalla.| ✅ **CERRADO 20-sep (D-251), paso 9.54.** **La cifra de la advertencia resultó ser lo de menos: lo grave era el silencio.** Se midió antes de escribir y en la base había **3 políticas, las 3 en el 40%, y 0 tocadas desde que nacieron** — nadie había confirmado nunca su comisión. De los dos caminos que este hallazgo dejaba abiertos, el propietario descartó el de *nacer sin valor*: **0% también es un número inventado**, solo que más barato para el salón y exactamente igual de mudo para el estilista. Así que la comisión se sigue generando y lo que cambia es que **se nota**: quinto paso en *Primeros pasos*, recuadro en *Configuración*, y una nota en el panel del estilista — el único que veía la cifra, porque el dueño no tiene esa pantalla. **Corrección de este enunciado:** decía que el 40% *"vive en un default que el repositorio no contiene"*, y no era exacto — está en `supabase/sql/015`, que sí está en el repositorio pero **no es una migración**, que es peor de otra manera: nadie lee esa carpeta. Ahora la tabla está en una migración con el default comentado. **CONTROL 219: 9 de 9.** Verificado en producción por el propietario |
| **AK** | 🔴 **Los dialogos validan DESPUES de cerrarse, asi que un dato rechazado borra todo lo escrito.** El patron es el mismo en **al menos seis**, no en los cuatro que se anotaron el 17-sep (D-236, D-237, D-241, D-242): **el 18-sep aparecieron los dos de Clientes** — `clients_page.dart:90` al crear y `:131` al editar —, que son **los que un salon usa todos los dias**. Ahi el golpe se ve menos porque `create_client` **no valida nada** y casi nunca rechaza; cuando rechace — permisos, RLS — el aviso llega a una pantalla sin formulario, igual que los otros. **Conviene contarlos todos antes de arreglarlos: la lista de cuatro ya se quedó corta una vez.** El patron: `await showDialog(...)` cierra la ventana, y **solo entonces** se llama al servidor; si rechaza, el aviso llega a una pantalla donde el formulario ya no existe. Encontrado el 17-sep por el propietario, poniendo un correo sin arroba en los datos de su sede: *"me salio error abajo casi imperceptible y al parecer me saco sin guardar... para mi me daria jartera y no volveria a usarlo"*. **Tuvo que reescribir los siete campos por una arroba.** | Fase 9, con **AG** y **AJ**. El dialogo debe **quedarse abierto** y ensenyar el error junto al campo que lo causo. Nota: el servidor ya hace lo correcto --rechaza y explica--; lo que falla es que la pantalla tira el trabajo. **Es el tipo de fallo que no rompe nada y aun asi pierde clientes** **19-sep: dos de los seis arreglados (D-249), y no por barrer sino por necesidad.** Los de **Clientes** tenían que caer con D-249: esa decisión crea un rechazo que **ocurre de verdad** — *«ese celular ya es de Ana»* — y con el patrón viejo habría llegado con el formulario ya cerrado. Ahora llaman al servidor **desde dentro** y se quedan abiertos con lo escrito intacto. **Quedan cuatro**, y el patrón a seguir ya está escrito en `clients_page.dart`. **23-sep (D-259): caen los cuatro que quedaban, y al contarlos con `grep` aparecen seis más.** Arreglados: precio y estado de una sede (Panel); los datos propios de la sede, **que eran dos copias del mismo formulario** (Panel y Configuración) y ahora son uno, con el error junto al campo; y *Crear cliente rápido* de la nueva cita, el más serio, porque desde D-249 el servidor rechaza de verdad. **Siguen abiertos seis, todos en `tickets_page.dart`**: agregar servicio, cambiar estilista, quitar servicio, reprogramar, cambiar estado y corregir un servicio finalizado. Poco texto escrito, mismo patrón. **Van con el 9.13** |
| **AL** | 🟡 **Al empleado invitado le llega el correo del duenyo: le promete "tu prueba gratuita de 21 dias".** Supabase manda **una sola plantilla de "confirma tu correo"** para todo el que crea cuenta, y no distingue entre quien se registra como salon y quien fue invitado a trabajar en uno. **Es doblemente falso:** el empleado no tiene prueba --no es un negocio-- y, si el duenyo de plataforma cambio los dias al aprobar, **ni siquiera son 21**. Encontrado el 17-sep por el propietario invitando a su primera administradora: *"le sale al administrador 21 dias de prueba, eso debe ser un error pues el no es un negocio o sede sino un trabajador"* **Ampliado el 18-sep con el estilista, y la otra mitad SÍ es código nuestro:** la pantalla previa le dice *"Confírmalo e inicia sesión para **terminar de crear tu negocio**"* a alguien que no crea ningún negocio. Está en `register_page.dart:99`, y lo llamativo es que **esa pantalla ya sabe que es un colaborador**: `isCollaborator` cambia el título, el subtítulo y el botón, y se olvida justo del mensaje de confirmación. **El correo lo ajusta el propietario en Supabase; esta frase la arreglamos nosotros en una línea.** | Fase 9. Supabase permite pasar datos en el alta y usarlos en la plantilla; con eso el texto puede cambiar segun quien se registre. Las seis plantillas estan en `PLANTILLAS_CORREO_AUTH.md` (D-146). **No rompe nada, pero es la primera frase que un empleado lee de la aplicacion y es mentira** **Nota del 23-sep (D-256):** la frase de la pantalla (*"terminar de crear tu negocio"*) **ya no existe**: se fue con D-248. Queda solo la mitad del correo, que se ajusta en Supabase |
| **AM** | 🔴 **La aplicación no lee los errores de autenticación que le llegan en la dirección, así que un acceso fallido no dice absolutamente nada.** Supabase devuelve al invitado a `salonymas.com/?error=access_denied&error_code=otp_expired&error_description=...`, y `main.dart` lee **doce** parámetros de esa dirección — `reservar`, `resena`, `planes`, `terminos`, `privacidad`, `partners`, `salon`, `ref`, `ref_payco`… — y **ninguno es el error**. El invitado ve una pantalla de acceso normal, sin una sola palabra sobre lo que acaba de fallar. Encontrado el 18-sep con el estilista, y **es la otra mitad de AH**: el texto del fallo viajó en su barra de direcciones durante toda la sesión sin que nadie lo leyera. **Solo entró porque acababa de escribir esa contraseña dos minutos antes**; quien abra el correo al día siguiente no tendrá esa pista **18-sep, D-248: arreglo escrito, sin probar todavía.** Nace `AvisoDeEnlaceDeCorreo`, que lee el error de la dirección y devuelve **qué pasó y qué hacer**; la pantalla de acceso lo enseña en ámbar. 7 pruebas nuevas. **Paso 9.50.** | Fase 9, **con AH y antes de los diez socios**. Leer `error_description` al arrancar y enseñarlo con qué hacer (*"vuelve a iniciar sesión con tu correo y contraseña"*). Y limpiar el parámetro después, que es exactamente lo que **AD** pide para `ref_payco`: **el mismo defecto, la misma dirección sucia** ✅ **CERRADO el 19-sep.** Verificado en local con la dirección exacta del 18-sep y comprobado en el JavaScript publicado. |
| **AN** | 🟡 **Un estilista acaba con dos nombres distintos y nadie los concilia.** Al aceptar la invitación, `accept_team_invitation` guarda lo que la persona escribe en `user_profiles.full_name` y **no toca el nombre del catálogo**: quedan los dos. Comprobado el 18-sep a propósito por el propietario, que añadió un apellido al unirse: **Usuarios** dice *Erick Estilista Rodriguez* y la agenda, los tickets, las comisiones y la reserva pública siguen diciendo *Erick Estilista*. **No es el hallazgo R** — aquello eran dos cuentas para un estilista, y sigue cerrado —: es una cuenta con dos nombres | Fase 9, barato. Lo que falta es **decidir cuál manda** y escribirlo: el del catálogo es el que ve la clienta, así que el que teclea el empleado debería servir solo para saber quién hay detrás de la cuenta. **Atenuante:** la pantalla de Usuarios ya los enseña juntos — el título y la etiqueta *"Estilista: …"* —, o sea que la discrepancia **se ve**, no es silenciosa |
| **AO** | 🔴 **Un cliente se puede duplicar infinitas veces y no hay forma de borrar ninguno, y eso parte en dos su historial de valor.** Comprobado el 18-sep por el propietario, que creó *David Alonso Cliente 1* **dos veces con el mismo teléfono**: la aplicación aceptó las dos sin decir nada. `create_client` **no valida nada** — ni formato, ni repetidos — y el único índice único de `clients` es `(tenant_id, id)`, que es una clave técnica para las claves foráneas y **no mira el teléfono**. **No existe `delete_client` en ninguna parte**, ni en Dart ni en SQL: lo único que hay es el interruptor *Cliente activo*, y `get_clients_management_summary` **sigue devolviendo los inactivos** (`order by cm.active desc`), así que el duplicado no se va, solo baja al final de la lista. **Lo grave no es la fila repetida, es lo que le hace a las cifras:** Clientes existe para medir *Análisis de Retorno y Valor* — gasto histórico, total de visitas, frecuencia, y las etiquetas VIP / Recurrente / En riesgo. Una clienta fiel atendida la mitad de las veces bajo cada ficha **aparece como dos personas mediocres y no llega a VIP nunca**. Y es el caso de todos los días: dos personas en el mostrador y un nombre escrito dos veces.  **Segundo filo, confirmado por el propietario el 18-sep al agendar:** en el desplegable de *Nueva Cita* **los dos David se ven exactamente iguales** — mismo nombre, sin teléfono, sin fecha de alta, sin nada que los separe —. *"No se distinguían."* Así que no es solo que las cifras se partan sol as: **quien agenda elige a ciegas cuál de las dos mitades engorda.** **TERCER filo, y el peor, verificado en el código el 18-sep preparando la prueba del portal:** `client_portal_authenticate` resuelve a la clienta **por teléfono**, con `order by c.created_at limit 1` — **gana la ficha más vieja, siempre**. Con dos fichas del mismo número eso significa dos cosas, las dos silenciosas: **(1)** si la cita se agendó con la ficha nueva, la clienta entra con su PIN correcto y **ve un portal vacío**, sin citas ni fotos y sin una palabra que lo explique; **(2)** si el salón le asigna el PIN a la ficha nueva, **ese PIN no le sirve jamás** — le dirá *PIN incorrecto* siendo correcto — y **cada intento cuenta para el bloqueo de 15 minutos** (D-167), que se aplica sobre la ficha vieja. **Ya no es solo que las cifras se partan: el duplicado puede dejar a la clienta fuera de su propio historial.** **PROBADO EN PANTALLA el 18-sep**, con dos PIN distintos puestos a propósito: `1234` en una ficha, `5678` en la otra. El portal aceptó **solo el `5678`** — el de la ficha más vieja — y al `1234`, correcto y recién asignado por el salón, le respondió **«Celular o PIN incorrectos»**. **La ficha nueva es inalcanzable: su PIN no abre nunca y cada intento gasta una de las cinco vidas antes del bloqueo.**| Fase 9. **Dos cosas distintas y conviene no mezclarlas:** (1) **avisar al crear** si ya hay alguien con ese teléfono — avisar, no prohibir: dos hermanas comparten teléfono —; (2) **poder fundir dos fichas en una**, que es lo que repara el daño ya hecho. El borrado duro es otra pregunta y toca ley: en Colombia la **Ley 1581 de 2012** da al titular derecho de supresión de sus datos, y hoy **no hay ninguna forma de atender esa petición**. Conviene mirarlo con quien lleve lo legal antes de decidir entre borrar o anonimizar ✅ **CERRADO el 19-sep (D-249).** Ya no se pueden crear duplicados: el celular es único por negocio y la pantalla exige diez dígitos. Los tres filos caen con él — el historial no se parte, no hay dos fichas iguales que elegir al agendar, y el PIN del portal no puede apuntar a la ficha equivocada. **Lo que NO resuelve, dicho a propósito:** sigue sin haber forma de **borrar** una clienta ni de **fundir** dos fichas, y la Ley 1581 da derecho a la supresión. Eso queda vivo en **AS** y en el paso **9.48**. |
| **AP** | 🔴 **Nadie puede dar por terminado un servicio salvo el propio estilista desde su cuenta, y sin eso no hay comisión aunque la clienta haya pagado.** Encontrado el 18-sep preparando el paso 12 del recorrido. La cadena, verificada entera: un ticket llega a **Por cobrar** (`finalizado`) **solo** por `change_ticket_service_status_v2`, y el único sitio de toda la aplicación que la llama es `my_stylist_agenda_page.dart` — **Mi agenda**, la pantalla privada del estilista. Desde **Tickets & Caja** el botón *Estado* se queda sin opciones en `en_proceso`, porque `AccionesDeTicket.siguientesEstados` devuelve lista vacía ahí. Y cerrar el ticket **y generar la fila de `stylist_commissions`** exige `status = 'finalizado'`: la función de cierre se sale de vacío si no lo está. **Lo decisivo es que el servidor SÍ lo permite**: `change_ticket_service_status` autoriza a `tenant_owner`, `admin`, `assistant` y `stylist` — al estilista solo lo suyo —. **No falta permiso: falta el botón.** | ✅ **CERRADO 20-sep (D-250), paso 9.53.** **El diagnóstico era exacto y además incompleto, y las dos mitades importan.** Era verdad que no faltaba permiso sino el botón: el servidor autoriza a dueño, admin y recepción desde julio y el lector ya existía, así que esa mitad no costó ni una migración — solo la ventana *Atender servicios*. **La otra mitad la encontró el control 218 al fallar en su primera corrida, y tenía razón:** la comisión solo se calculaba cuando entraba un pago, así que si la clienta pagaba el 100% por adelantado (D-163), terminar el servicio ya no disparaba nada y el ticket se quedaba en *Por cobrar* con saldo cero para siempre. D-163 había previsto ese caso y dejado el remedio escrito **en `change_ticket_status`, una puerta que no se puede abrir** porque exige que todos los servicios estén ya terminados. La migración `20260920120000` se lo pone a `change_ticket_service_status`, que es por donde el ticket llega de verdad. **CONTROL 218: 9 de 9**, probando los dos órdenes de cobro. Verificado en producción por el propietario, incluido el caso con el usuario del asistente |
| **AQ** | 🔴 **El estilista firma por la clienta: él mismo marca la casilla que dice que ella autorizó publicar su foto.** Observado por el propietario el 18-sep subiendo la foto del servicio: *"al ser juez y parte quedaría mal... lo podría hacer así la cliente ni se dé por enterada"*. Verificado: `AddWorkPhotoDialog` es un `CheckboxListTile` corriente que rellena quien sube la foto, y ese diálogo lo abren **`my_stylist_agenda_page.dart`** y `tickets_page.dart`. **D-167 no está mal, está incompleto:** construyó el candado — el servidor rechaza aprobar para portafolio sin `client_consent` — y prometió *"el registro de quién autorizó y cuándo"*. Lo que se registra de verdad es **quién marcó la casilla**, que es la parte interesada. `client_consent_at` guarda la hora de un consentimiento que la clienta nunca dio. **La llave del candado se le entregó a quien quiere abrirlo** | Fase 9. La propuesta del propietario es la buena y ya existe la tubería: **que lo apruebe la clienta desde su portal** (D-167, paso 5.6) o desde el enlace de reseña que ya se le manda. Mientras tanto, lo honesto es **cambiar el texto** — *"confirmo que pedí su autorización"* dicho por quien la pide — en vez de afirmar un hecho de un tercero ausente. **Emparenta con el hallazgo Ñ**, que desde el 09-ago dice que *el flujo de las fotos no está definido y que los tipos "Final" y "Portafolio" sobran*: el propietario llegó a lo mismo por su cuenta, mirando la pantalla, sin haber leído Ñ **Mitad hecha el 23-sep (D-256):** el texto ya no afirma lo que hizo la clienta; dice lo que hizo quien marca la casilla (*"confirmo que le pedí su autorización"*). **Sigue abierto**: lo cierra que apruebe la clienta desde su portal (9.48) **El flujo destino lo definió el propietario el 23-sep (D-260):** la casilla del salón solo puede decir que la clienta, presente, aceptó que se le tomara la foto; **publicarla lo autoriza solo ella desde el enlace que se le envía**. Hoy esa casilla todavía habilita la publicación: construir la aprobación de la clienta es lo que cierra AQ |
| **AR** | 🟡 **Tu propio Panel cuenta las pruebas de dos maneras en la misma pantalla: arriba dice 1 y abajo dice 2.** Lo vio el propietario el 18-sep quitándole la marca de ensayo al negocio: la tarjeta *En pago / prueba* saltó de **0/0** a **0/1**, mientras la píldora *En Prueba* siguió diciendo **(2)** todo el rato. **La causa está en dos líneas seguidas de `platform_panel_page.dart`:** `activeCount` filtra `&& !t.isDemo`, y `trialCount`, justo debajo, **no lo hace**. La métrica de arriba viene del servidor, que sí excluye los ensayos (`where not t.is_demo`). **O sea que el mismo archivo se acuerda de excluir los demos en una línea y se olvida en la siguiente** | Fase 9, barato y con dos caminos honestos: igualar el criterio de la píldora al del servidor, **o** rotular cada número con lo que de verdad cuenta (*"1 prueba real, 2 contando ensayos"*). **Lo que no puede quedarse es como está:** es el tablero desde el que se mira el negocio, y cuando entren los diez socios los ensayos y las pruebas de verdad van a convivir. Misma familia que **AG**: dos números correctos que responden preguntas distintas sin decir cuál ✅ **Cerrado el 23-sep (D-257):** la píldora y su filtro excluyen los ensayos, igual que la métrica del servidor; los ensayos ya tenían su píldora propia, *Demos* |
| **AS** | 🔴 **Al borrar un negocio, las fotos de sus clientas se quedan publicadas en internet, y además se vuelven imposibles de encontrar.** Saltó el 18-sep de una pregunta del propietario — *"¿por qué sobreviven los archivos de Storage?"* — antes de borrar Exportadora. `platform_delete_demo_tenant` **no toca Storage a propósito** (D-246), y hay **dos almacenes públicos**: `work-photos` (las fotos aprobadas para portafolio, con URL permanente) y `stylist-photos`. Lo que se borra es la **fila** `work_photos`, que es justo lo único que decía de quién era ese archivo. **El archivo se queda, público, y ya nadie puede saber a qué negocio perteneció.** **Y choca de frente con AQ y con D-167:** la clienta autorizó publicar su foto **en el portafolio de ese salón**. El salón ya no existe; la foto sigue ahí. El almacén privado tiene el mismo problema en pequeño: los archivos quedan, inalcanzables pero ocupando espacio, sin forma de listarlos | Fase 9, **antes de borrar cualquier negocio con fotos de personas reales**. Ahora mismo no urge — los tres de ensayo no tienen clientas de verdad — **pero el borrado ya existe y el día que se use con un cliente que se da de baja, esto es una fuga.** Lo correcto es borrar los archivos **antes** que las filas, que es el mismo orden que ya razona `setPortfolioApproval` para retirar una foto: *primero sacarla de internet, después anotarlo*. Si se prefiere conservarlos, entonces hay que **dejar el inventario escrito** en `deleted_demo_tenants` — rutas y almacén — porque un archivo del que nadie sabe de quién es no se puede ni borrar ni entregar |
| **AT** | 🔴 **El producto no lo ha usado nunca nadie para trabajar. Ni un solo día, ni una sola persona.** *(Enunciado corregido el 18-sep, el mismo día que se escribió. Al borrar Exportadora dije que era **"el único pionero real"** y que **"se registró y no volvió"**. **Me lo inventé a partir de una señal:** la etiqueta ★ PIONERO de la ficha y el motivo de precio *"Pionero fundador: $80.000 por sede"*. El propietario lo desmintió: era **una persona cualquiera de un chat**, que al oír hablar de la app quiso ayudar comprobando que se podía crear una cuenta — **y lo hizo desde otro país**. Ni era pionero, ni había acuerdo comercial, ni "no volvió": **hizo exactamente lo que fue a hacer**. Es el mismo fallo de método de D-223: afirmar desde un rótulo en vez de desde el contenido.)* **Lo que queda en pie, y es más serio que lo que dije:** el inventario de aquel borrado sigue siendo cierto — **0 clientas, 0 tickets, 0 servicios, 0 fotos**, y las 17 filas todas de la máquina —, y con él cae el último candidato a usuario real. **Todo lo que hay en la base lo sembró el asistente (D-112) o nació de una prueba de este equipo.** Después de meses de construcción, **no existe ni un dato de uso**: nadie ha cobrado con esto una clienta suya, ni ha cuadrado su caja, ni le ha pagado a nadie | **No se arregla con código, y por eso importa tanto.** Refuerza el **9.24**: no hay telemetría que mirar ni usuario a quien preguntar, así que **la única forma de saber cómo trabaja un salón es ir a ver uno**. Como el propietario no tiene salón propio, el 9.24 se aborda **entrevistando a administradores de salones reales** con el guión de `docs/03_referencias/GUION_ENTREVISTA_SALONES_9.24.txt`. **Una entrevista es más débil que una observación** — cuenta lo que la gente dice, no lo que hace —, y por eso el guión pregunta por la última vez concreta y no por costumbres **Pista sin seguir, 19-sep:** en `auth.users` hay **25 cuentas**, y la primera es `antoniorb92@hotmail.com`, creada el **15-sep** y todavía **«esperando verificación»**. No es ninguno de los correos de prueba y el propietario no sabe quién es. Cae justo en la ventana en la que el enlace llegaba gastado (**AH**), así que **puede ser la primera víctima con nombre de ese fallo**. **No se investigó, por decisión del propietario:** *"estamos creciendo más los requerimientos que las soluciones"*. Queda escrito por si un día interesa contar cuántas cuentas hay en ese estado — sería la medida real de lo que costó AH. |
| **AU** | 🟡 **El interruptor «Visible al cliente» no hace visible la foto al cliente.** Comprobado de punta a punta el 18-sep: se activó en la galería — el servidor lo aceptó sin rechistar — y el portal de la clienta siguió diciendo *«Todavía no tienes fotos publicadas aquí»*. **No es un fallo del servidor, es una promesa que la pantalla no puede cumplir:** `get_client_portal_data` exige `visible_to_customer = true` **y además** `photo_url is not null`, y esa dirección solo existe cuando la foto se aprueba para portafolio y se muda al almacén público. El código lo sabe y lo dice en un comentario — *"exigiría generar URLs firmadas, que es infraestructura nueva… se deja fuera y se documenta, no se inventa a medias"* (D-167) —. **Lo que faltó fue decirlo en la pantalla.** Así que hoy el interruptor **solo puede hacer algo cuando ya se aprobó el portafolio**, que es justo el caso en el que ya se ve | Fase 9, con **AQ** y **Ñ**. O el interruptor genera una URL firmada y cumple lo que promete, o **dice lo que de verdad significa** y se apaga cuando no puede hacer nada. **Un interruptor que acepta que lo enciendas y no cambia nada es peor que no tenerlo:** el salón cree que le enseñó la foto a su clienta |
| **AV** | 🟡 **La pantalla donde la clienta deja su reseña no tiene una sola tilde, y es de cara al público.** Lo vio el propietario el 18-sep al enviarla: *«veo que dice resena no reseña»*. Dice **«Gracias por tu opinion. Tu resena quedara publicada despues de ser revisada»**, y también *Calificacion*, *cuentanos como te fue* y *Enviar resena*. **No es criterio del proyecto:** `public_review_page.dart` tiene **cero** caracteres acentuados y es **la única** página pública así — `public_salon_page` tiene 17, `client_portal_page` 15, `public_partner_page` 13, `public_booking_page` 5. Se escribió sin tildes y nadie volvió a mirarla | Fase 9, media hora. **Importa más de lo que parece por dónde aparece:** es una de las tres únicas pantallas que ve una clienta del salón, y *resena* no es *reseña* — es otra palabra. Un salón que cuida su imagen lo nota, y lo que le enseña a su clienta lleva su nombre, no el nuestro **Ampliado el 23-sep:** no es solo la reseña. **La página pública de precios** (`public_plans_page.dart`), que es lo primero que ve un salón interesado, dice *resenas*, *pagina web publica*, *21 dias*, *en linea*, *aqui*, *sabado*; y también el candado de reseñas de Tickets y un aviso del panel. ✅ **Cerrado el 23-sep (D-256):** las dos páginas públicas con tildes y con *¿*, más el candado de reseñas, el de fotos y el aviso del panel. Una prueba lee las dos páginas y falla si vuelven |
| **AW** | 🔴 **La reserva pública fabrica clientas duplicadas: compara el teléfono como texto exacto, mientras el portal lo compara por dígitos.** **Probado en producción el 18-sep, con el mismo número escrito de dos formas:** `3506815629` reutilizó la ficha existente — bien —, y **`350 681 56 29` creó una ficha nueva**. `public_create_booking` hace `where c.phone = v_client_phone`, texto contra texto; `client_portal_authenticate` hace `regexp_replace(c.phone, '[^0-9]', '')`. **Dos reglas distintas, en el mismo producto, para la misma pregunta: ¿es esta la misma persona?** El salón de prueba acabó con **cuatro fichas para dos personas reales**. **Y se encadena con AO hasta lo absurdo:** la clienta que reserva escribiendo su número con espacios estrena ficha, su cita cae ahí, y su PIN del portal — que abre **la ficha más vieja** — le enseñará un portal vacío. **Nadie tiene que equivocarse para que esto pase: basta escribir el propio teléfono como lo escribe cualquiera** | Fase 9, **con AO y antes de abrir la reserva pública a nadie**. La regla tiene que ser **una sola y por dígitos**, y lo más barato es **normalizar el teléfono al guardarlo**, no en cada consulta — así no hay una sexta función que mañana lo compare a su manera. **Esto es más urgente que AO a secas:** un duplicado escrito a mano lo comete el salón de vez en cuando; **este lo comete cada clienta que reserva sola, y la reserva pública es la puerta que se va a abrir a los diez socios** **19-sep: arreglo escrito, pendiente de aplicar (paso 9.51).** Al reescribir la función se vio que la comparación por texto estaba **tres veces**, y dos son los topes antiabuso de **H-02**: **el límite de 4 reservas se saltaba escribiendo el número con un espacio**, porque cada formato contaba como otra persona y el contador volvía a cero. Se pasa a la **misma expresión por dígitos** que ya usan el portal y el índice `clients_tenant_phone_digits_idx` — no se inventa una tercera. **No se normaliza lo guardado**, solo la comparación: el teléfono es el dato de la persona. ✅ **CERRADO Y VERIFICADO el 19-sep (paso 9.51).** Migración aplicada y **control 216 en verde, 6 de 6**, reservando de verdad contra la base: el mismo número escrito de cuatro formas es **una sola clienta**, uno distinto sí estrena ficha, y **el tope de H-02 cuenta los formatos juntos** — *«Ya tienes 4 citas pendientes con este número»*. **Queda un caso fuera:** `+57 3506815629` sigue siendo otra persona para la base, porque en dígitos son doce y no diez. Va en el paso **9.52**. |
| **AX** | 🟡 **La hora que la clienta acaba de elegir se le caduca mientras rellena el formulario, y el aviso le dice algo que no es verdad.** Vivido por el propietario el 18-sep reservando: *"empecé a digitar a las 18:14, cuando la iba a enviar ya eran las 18:15, me dio un mensaje de no tener disponibilidad"*. La causa exacta: la lista de horarios sale de `get_available_appointment_slots_v2`, que filtra `where cs.starts_at > now()`; a las 18:15:00 **las 18:15 dejan de existir**, y `public_create_booking` vuelve a preguntar por la misma función al enviar. **El candado es correcto — nadie debe reservar el pasado —; lo que está mal son dos cosas:** **(1)** el mensaje dice *«Ese horario ya no está disponible»*, que la clienta lee como *"alguien me ganó"*, cuando lo que pasó es que **se le hizo tarde**; y es **exactamente el mismo texto** que sale cuando sí se lo quitaron, así que no puede distinguir un caso del otro; **(2)** la lista **no se refresca**, de modo que sigue ofreciendo una hora que ya no se puede reservar | Fase 9, barato. Distinguir los dos casos en el mensaje (*«esa hora ya pasó, elige otra»* frente a *«acaban de reservarla»*) y refrescar o marcar en gris la hora vencida. **Parece un caso raro y no lo es:** con franjas de 15 minutos la hora más próxima está siempre a menos de un cuarto de hora, y quien reserva para ya mismo — de pie en la puerta del salón — es justo la clienta que no conviene perder ✅ **Cerrado el 23-sep (D-257):** la página comprueba la hora antes de enviar y distingue *"ya pasó"* de *"alguien acaba de reservarla"*; en los dos casos recarga la lista sin borrar lo escrito |
| **AY** | 🔴 **El arreglo de AH venía con el mismo error que AH: un número escrito a mano que vive en otro sitio.** `register_page.dart` exigía **exactamente 6 dígitos** — en el `maxLength` del campo, en la validación y en tres textos — y el correo real trajo **ocho**: `59875879`. **El campo le habría cortado dos dígitos** y la validación habría rechazado el resto con *«El código son 6 números»*, que además era mentira. La longitud del OTP **vive en Supabase** (Authentication → Sign In / Providers → Email) y se cambia desde su panel sin tocar una línea de Dart. **Es D-245 con otra ropa:** allí `register_tenant` llevaba escrito el código de un plan que vivía en la base, y quedó roto dieciseis días cuando ese plan se jubiló. Lo encontró el propietario el 19-sep **probando de verdad, el mismo día que se escribió el arreglo** | ✅ **Cerrado el 19-sep (D-248, corrección).** No se cambió el 6 por un 8 — eso repetiría la trampa para el siguiente cambio de ajuste —: nace `CodigoDeConfirmacion`, que **no sabe cuántos dígitos tiene un código**. Limpia espacios y guiones de lo que la gente pega, acepta de 4 a 12 dígitos como red contra la basura, y **deja que decida el servidor**. Fuera el `maxLength`, fuera los tres textos que prometían seis. **6 pruebas**, una de ellas con el código real de ocho **Verificado en producción el 19-sep:** el código real de **ocho** dígitos se acepta sin recortes. |
| **AZ** | 🔴 **Quien cierra la pestaña del registro se queda sin ningún sitio donde escribir su código.** Lo vivió el propietario el 19-sep, **a los diez minutos de estrenar D-248**: cerró la ventana de incógnito, la cuenta quedó creada y sin confirmar, y la pantalla de acceso **no tenía por dónde meter el código**. La salida existía — registrarse otra vez con el mismo correo hace que `signUp` reenvíe — **pero no se le ocurre a nadie**, y menos a quien no es técnico. **Es la consecuencia natural de haber cambiado un enlace por un código:** un enlace se puede abrir desde cualquier parte; un código necesita un sitio donde escribirlo, y solo había uno. Y de paso invalidaba lo que D-248 prometía: que con código se puede empezar en el computador y terminar en el teléfono | ✅ **Cerrado el 19-sep (D-248, segunda corrección).** La pantalla de acceso aprende a recibir un código, y **se ofrece sola en el momento exacto**: si al entrar Supabase responde `email_not_confirmed`, la pantalla cambia al campo del código y manda uno nuevo sin que nadie lo pida. Más una puerta explícita — *«¿Tienes un código sin usar? Confirma tu correo aquí»* — para quien llega con el código en la mano. **Ahora sí es cierto que se puede terminar en otro aparato**, porque `verifyOTP` no depende del navegador **Verificado en producción el 19-sep**, y por el camino que lo originó: el propietario entró con correo y contraseña, la pantalla cambió sola al campo del código y terminó **desde el móvil**. |
| **BA** | 🟡 **Los errores de la cuenta le hablan en inglés al salón: «*Token has expired or is invalid*».** Lo vio el propietario el 19-sep probando a propósito un código caducado. Está en inglés, dice **token** — palabra que nadie usa fuera de esto — y **no dice qué hacer**. El código hacía `errorMessage = error.message`, que deja pasar tal cual lo que responde Supabase, en **seis** sitios entre las dos pantallas de entrada. Es **AV con otra ropa** — texto de cara al cliente sin cuidar — y **AM con otra ropa**: un mensaje que no dice qué hacer sirve de poco | ✅ **Cerrado el 19-sep (D-248, tercera corrección).** Nace `MensajeDeAuth`, que traduce los ocho códigos que se ven de verdad y **deja pasar el original cuando no conoce uno** — un mensaje en inglés es malo, ninguno es peor, que es justo lo que hacía AM. **4 pruebas** |
| **BB** | 🟢 **Un motivo de precio que contradice su propio dato.** *Prueba Barberia Elite* tiene `price_reason = 'Pionero (50% de por vida)'` con `is_founder` en **false**, sin descuento, y un precio pactado de **$10.000** — que no se parece a un 50% de $150.000. Encontrado el 22-sep midiendo **AG**. **Ya se sabe de dónde salió:** lo escribió `platform_approve_tenant` sola, y esa causa quedó cerrada con D-252. El propietario confirmó que **el dato real es el precio pactado**. **Se escribió un `UPDATE` que lo limpiaba y se retiró antes de aplicar nada**, por dos razones: no habría tocado ni una fila —filtraba por `price_cop is null`— y **nadie lo había pedido**. | Sin fase. Es un rastro en un campo que **desde D-252 ya no cobra nada** y que la ficha enseña rotulado como *«Acuerdo del negocio — no se cobra»*. Se deja escrito en vez de borrarse: misma razón que el celular de nueve dígitos de D-249, adivinar el dato de alguien es peor que dejarlo señalado |

| **BC** | 🟡 **La vista de soporte de la plataforma no deja ver las fotos, y esa decisión fue tuya.** D-076 (27-jul): *el dueño de plataforma **sí** ve los datos de cualquier negocio —incluidas reseñas y fotos—, en solo lectura y sin rastro, **mientras la plataforma no tenga clientes reales***. Dos semanas después H-09 (D-119) hizo las fotos privadas, y esa pantalla —que es anterior— nunca aprendió a pedir permiso para verlas: sale *"Vista no disponible"*. **Una decisión rota en silencio por otra.** Y lo importante: **la condición de D-076 caduca el día del primer cliente real**, y no estaba en la lista de lo que cambia ese día. Encontrado el 23-sep en la revisión integral, por una pregunta del propietario | Antes del primer cliente real. **Decidir el acceso de soporte** con lo que pedía el diseño rector de julio (`ROLES_Y_PERMISOS` §6: solicitud, motivo, vencimiento, rastro) o confirmarlo como está. Ya anotado en `MAPA_TECNICO` §1-bis |
| **BD** | 🟡 **Los candados de plan sugieren subir a planes que no existen.** `main.dart` dice *"Business"* en cuatro módulos y como respaldo; Tickets y *Mi agenda* dicen *"Profesional"*; y dos pantallas caen a *"Profesional"* si no llega el nombre del plan. **Hoy no se ven** —D-188 abrió todo—, pero el **9.41** (módulos por sede, D-239) **los vuelve a encender**. Es el mismo tipo de resto que el 50% de D-252. Encontrado el 23-sep | Con el 9.41, o antes: el candado debe decir *"pídelo a Salón y Más"*, no el nombre de un plan ✅ **Cerrado el 23-sep (D-256):** se quitó el parámetro que nombraba el plan (`planSugerido`, `lockPlan`), no solo su valor. Los candados dicen *"pídelo a Salón y Más"*, y una prueba vigila que no vuelva |
| **BE** | 🟡 **Los documentos rectores de julio contradicen decisiones posteriores sin decirlo.** `ROLES_Y_PERMISOS` sigue diciendo que la plataforma no lee la operación (D-076 lo cambió) y que recepción no atiende servicios (el servidor lo permite desde julio, D-250 lo enseñó); `SUSCRIPCION_Y_ENTITLEMENTS` sigue con tres planes y sin cobro por sede (D-188, D-239); `MAPA_TECNICO` estaba congelado a mediados de agosto | ✅ **Cerrado el 23-sep (D-254):** cada uno lleva arriba un *"Estado a 23-sep"* con lo que otras decisiones cambiaron, sin tocar el texto de julio; el `MAPA_TECNICO` se puso al día; y nace `ARQUITECTURA_Y_EVOLUCION.md` con lo que se había perdido al archivar el `PROMPT_MAESTRO_IA` |
| **BF** | 🟡 **El recuento de hallazgos estaba mal desde hacía semanas.** Se contaba con `[A-Z]`, que **no incluye la Ñ**: el hallazgo Ñ existe, está abierto y nunca se contó. Y cinco filas (D, G, O, P, AB) estaban cerradas de hecho pero nunca se marcaron. Los HANDOFF daban cifras falsas con total seguridad | ✅ **Cerrado el 23-sep (D-254):** filas marcadas, y **el guardián cuenta los hallazgos solo** (Ñ incluida). Un número que se cuenta a mano en cada cierre es un número que diverge |

| **BI** | 🔴 **Vencer sin pagar no pasa a mora: no hay gracia, el aviso miente, y una sede que no paga no se corta nunca.** Encontrado el 23-sep por la radiografía de la base, **verificado en el código en tres sitios**: **(1)** solo un pago **rechazado** por ePayco pasa una suscripción de `active` a `past_due` (`beautyos_procesar_evento_epayco`); aquí el pago es **manual cada mes**, así que quien simplemente no paga **nunca produce un rechazo**. **(2)** El vigilante diario (`beautyos_suspender_suscripciones_vencidas`, del **17-ago**, anterior a las sedes) solo suspende lo que **ya** está en `past_due`/`grace`; no mira fechas vencidas, **ni sedes**. **(3)** La pantalla enseña el estado tal cual: *ACTIVO*, aunque el período haya vencido. **Consecuencia para un salón que olvida pagar:** al día siguiente de vencer, `beautyos_tenant_accepts_new_commitments` (que sí mira la fecha) le **niega crear citas** —internas y públicas— **sin los 5 días de gracia** que prometen D-141 y D-143, **sin la cuenta regresiva por correo** (solo sale en `past_due`/`grace`), mientras su pantalla dice *ACTIVO*, y con un mensaje falso: *"La prueba gratis de este negocio está vencida"*. **Y una sede secundaria que no paga sigue *Al día* y operando para siempre**: `al_dia` es solo `status in ('active','trialing')`, nada cambia el estado de una sede por fecha, y el acceso solo mira el negocio. **Es el agujero de ingreso que la Etapa 3 venía a cerrar.** **Evidencia viva:** la ficha de Naguara de Uñas enseñaba el 23-sep *"ACTIVE · Válido hasta el 22/09/2026"* y su sede *"Al día · Pagada hasta 22/09/2026"*. *(Falta verlo en pantalla: intentar una cita en Naguara, que es de prueba.)* | **Turno A, antes que nada del cobro.** Una transición diaria por **fecha** —negocio **y** sede— de `active` a `past_due` con su gracia, el mensaje del candado corregido, y un control que **recorra el vencimiento** en vez de leerlo. **No se sospechó en dos meses porque nadie ha dejado vencer una suscripción pagada**: los dos únicos pagos reales se hicieron y el ciclo nunca se completó. **Confirmado en pantalla por el propietario el 23-sep** (Nueva cita en Naguara → *"La prueba gratis… vencida"*). **Mitad de la pantalla hecha el 23-sep (D-255):** lo vencido se ve rojo en la cabecera, en Configuración, en *Tus sedes* y en el panel de plataforma, y **una sede vencida ya se puede renovar** —antes no tenía botón—. **Sigue abierta la mitad del servidor**, que es la que importa: la mora por fecha, la gracia, el mensaje del candado y el corte de las sedes secundarias **Mitad del servidor escrita el 23-sep, SIN APLICAR (D-258):** migración `20260923120000_vencer_sin_pagar_pasa_a_mora_bi.sql` + **control 222**. Queda, después de aplicarla, el mensaje del candado (con `intervenciones/extraer_candado_y_vencimiento_bi.sql`) y **una decisión del propietario**: si una sede secundaria suspendida debe dejar de agendar **Mitad del servidor APLICADA el 23-sep (D-258), control 222 en 10/10:** Naguara y dos sedes pasaron a mora con su gracia en la primera pasada, y la tarea diaria quedó programada. **Queda:** el mensaje del candado, y lo que decidió el propietario ese mismo día —*"cuando una sede no pague entra en modo suspendida por no pago… las sedes heredan los lineamientos del negocio"*—, que está pendiente de precisar **Decidido por el propietario el 23-sep (D-260):** una sede suspendida por no pago **deja de poder agendar citas nuevas** —internas y reservas en línea—; lo ya agendado se atiende y se cobra, y las demás sedes siguen normales. La sede **hereda del negocio la gracia, la suspensión y la fecha de corte**; **no** hereda el precio (D-252) ni los módulos (9.41). **Siguiente paso:** el propietario corre `intervenciones/extraer_candado_y_vencimiento_bi.sql`, ampliado para traer las dos puertas por las que nace una cita y la tabla `tickets` **23-sep (D-261): escrita, SIN APLICAR,** la migración que lo cierra —la sede suspendida no agenda y el candado dice el motivo real—, generada desde el texto vivo, con el **control 224**. Después de aplicarla falta que *Tus sedes* lo diga (16-ter) ✅ **Cerrado el 23-sep (D-262):** D-261 aplicada, control 224 en 9/9. La sede suspendida no agenda y la pantalla lo dice, también la cabecera, que para un negocio suspendido no enseñaba nada. Queda aparte la decisión **j** del HANDOFF: si la prueba gratis también tiene 5 días de gracia |
| **BG** | 🟡 **El fallo de D-245 sigue vivo en una segunda puerta.** `register_tenant` tiene **dos versiones**: la de 9 parámetros (la que usa la app, arreglada el 17-sep) y **una de 8, del 27-ago, que nadie retiró** cuando el 30-ago se añadió el parámetro de referidos. **Esa versión todavía busca el plan `'profesional'`**, jubilado el 01-sep. Hoy no la llama nadie —la app manda los 9—, pero cualquier llamada sin el código de referido encontraría dos candidatas. **Es la misma forma que el 50% de D-252:** se arregló una puerta y quedó la otra, y el control 214 vigila una firma. Encontrado el 23-sep por la radiografía | Turno C. Una migración de una línea (`drop` de la versión de 8 parámetros) y que el control 214 compruebe que queda **una sola** `register_tenant` **Migración escrita el 23-sep, SIN APLICAR (D-258):** `20260923130000_una_sola_register_tenant_y_permisos_de_private_bg_bh.sql` + **control 223**, que mira el catálogo y no una firma ✅ **Cerrado el 23-sep (D-258): aplicada por el propietario, control 223 en 5/5.** Queda una sola `register_tenant`, la de 9 parámetros |
| **BH** | 🟢 **Nueve funciones de `private` conservan el permiso de ejecución por defecto de PostgreSQL** (a `PUBLIC`, y por tanto a `anon`), entre ellas `beautyos_precio_efectivo`. **No se alcanzan desde internet**: la API solo publica el esquema `public` (D-214). Cinco son disparadores, que no se pueden llamar sueltos. Es higiene, no un agujero. Encontrado el 23-sep por la radiografía | Sin urgencia. `revoke ... from public` como hizo D-174 con H-11, para que la regla sea la misma en todo `private` **En la misma migración que BG, SIN APLICAR (D-258):** se quita a `PUBLIC` y `anon` y se da explícito a `authenticated` y `service_role`, para no romper nada con sesión ✅ **Cerrado el 23-sep (D-258): aplicada, control 223 en 5/5.** `anon` ya no ejecuta ninguna función de `private` |
| **BJ** | 🟢 **El resumen de la nueva cita enseñaba `\n` escrito**: *Corte Masculino con Luiscar\n23/09/2026*. Una barra escapada de más en `tickets_page.dart`. Visto por el propietario el 23-sep en sus capturas | ✅ **Cerrado el 23-sep (D-255)**, con una prueba que lo vigila |
| **BK** | 🔴 **Las píldoras de la cabecera abrían el cobro que D-252 cerró.** *Prueba vencida · Activar plan*, *N días de gracia · Pagar* y *Prueba: N d restantes* llamaban a `iniciarPago` sin sede —el cobro del negocio entero—, que el servidor niega desde el 22-sep: quien las pulsaba recibía un error. **El sexto letrero rancio de D-252**, en la cabecera y no en Configuración. Encontrado el 23-sep yendo a pintar de rojo la píldora vencida (BI) | ✅ **Cerrado el 23-sep (D-255):** las cuatro píldoras llevan a Configuración → *Tus sedes*, donde se paga cada sede. Una prueba vigila que la píldora no vuelva a llamar a `iniciarPago` |
| **BL** | 🟡 **Sospecha, no comprobada: puede haber mensajes de la base con las tildes dañadas.** `scripts/aplicar_sql.ps1` no fija la codificación, y psql en Windows puede leer un archivo UTF-8 como WIN1252: *"está"* se guardaría como *"estÃ¡"*. Hay migraciones aplicadas con tildes en sus mensajes (D-249, AG). Encontrado el 23-sep escribiendo D-261 | La migración de D-261 fija UTF-8, y la **comprobación 9 del control 224** barre toda la base. **Si encuentra funciones dañadas**: fijar la codificación en `aplicar_sql.ps1` y reescribir sus mensajes. **Si no**: se cierra sin tocar nada **23-sep (D-262): el control habló, y la causa era otra.** Una sola función dañada, `reopen_finished_ticket_service`. Las migraciones con tildes aplicadas con `aplicar_sql.ps1` quedaron bien, así que **el guion no se toca**. La dañada **no coincide con su versión del repositorio** (que no tiene tildes): se cambió por fuera de las migraciones. Siguiente: `intervenciones/extraer_tildes_danadas_bl.sql` **23-sep (D-263): corrección escrita, SIN APLICAR** —`20260923170000_las_tildes_de_corregir_servicio_bl.sql` y **control 225**—, generada desde el texto vivo. Eran cinco textos, uno de ellos la frase que se guarda en el historial del ticket. **Y D-262 se equivocó al describirla**: la función no está en el repositorio en absoluto; nació antes de las migraciones ✅ **Cerrado el 23-sep (D-264): aplicada, control 225 en 4/4**, y **cero** filas del historial guardadas con texto dañado: no hay nada viejo que corregir |
| **BM** | 🟡 **El estilista no puede agendar citas, y el diseño rector dice que sí.** Visto por el propietario el 23-sep con un estilista de la Peluquería Éxito: *Mi agenda* no tiene botón para una cita nueva **ni un aviso que diga por qué**. Verificado: `create_scheduled_ticket_with_service_v2` solo acepta `tenant_owner`, `admin` y `assistant` **desde el 20-jul** (tramo C2a); `ROLES_Y_PERMISOS` §2 y §3, del 19-jul, dicen que el estilista *"crea clientes y citas dentro de sus sedes"* (*Crear reserva: S/P*). **No hay ninguna decisión registrada que lo cambiara**: el servidor se escribió así y nadie lo contrastó. **No tiene que ver con la prueba** del negocio | **Decisión del propietario** (regla 20): si el estilista agenda, y para quién —solo para sí o para cualquiera de su sede—. Si **no**, que el diseño rector lo diga y que *Mi agenda* explique a quién pedírselo **Decidido por el propietario el 23-sep (D-263): el estilista agenda para sí mismo** —*"por si tiene conocidos y genera leads y ventas por sus medios"*—, en su propia agenda y no para otros. **Faltan dos respuestas antes de construir:** si puede dar de alta a la clienta nueva, y si la cita nace *por confirmar* o *confirmada* **Precisado el 23-sep (D-265):** la clienta nueva se da de alta desde *Mi agenda*, y la cita nace por confirmar. Lectura preparada: `intervenciones/extraer_agenda_del_estilista_bm.sql`. **Falta una respuesta:** qué clientas ve el estilista al escoger ✅ **Cerrado por decisión el 23-sep (D-266): el estilista NO agenda**, para que no pueda llevarse la base de clientas; si trae a alguien, comparte el enlace del salón o la reserva pública. Revierte D-263. `ROLES_Y_PERMISOS` lleva la nota |
| **BN** | 🟡 **Pagar en los días de gracia sale más barato: los días de gracia se usan y no se cobran.** D-160, regla 3, y `beautyos_calcular_cargo_sede`: al pagar tarde dentro de la gracia, la fecha de corte no se corre —bien—, **pero se cobra solo por los días que faltan hasta el corte**. Una sede de $30.000 que venció el 22 y paga el 25 paga **$27.000**. Lo destapó el propietario el 23-sep: *"al pagar dentro de esos 5 días se descuentan del período siguiente, o si no todo el mundo pagaría el día 5 de gracia"* | **Decisión del propietario** (regla de dinero, D-160): cobrar el mes completo aunque se pague en la gracia, manteniendo la fecha de corte. Es una línea en el cálculo de la sede y otra en el del negocio, con su control **Decidido por el propietario el 23-sep (D-264): se cobra el mes completo aunque se pague en la gracia**, sin correr la fecha de corte. Solo para las sedes: el cobro del negocio está cerrado desde D-252. Pendiente la lectura `intervenciones/extraer_cobro_de_sede_bn.sql` y la migración **Migración escrita el 23-sep, SIN APLICAR (D-265)**, en la misma de BO, con el **control 226**, que paga en vez de leer ✅ **Cerrado el 23-sep (D-266): aplicada, control 226 en 7/7**. Pagar en la gracia cobra el mes completo, sin correr la fecha de corte |
| **BO** | 🔴 **Sospecha, sin comprobar: un mínimo de $10.000 dejaría sin activar una sede pactada por debajo.** En el repositorio, `beautyos_procesar_pago_de_sede` exige `greatest(10000, monto)` a todo cobro no prorrateado (D-159, pensado para el cobro del negocio). **La sede de Éxito está en $4.500**: el pago real del paso 9.8 se cobraría y la sede no se activaría. Tampoco se sabe si ePayco acepta cobros de ese valor. Encontrado el 23-sep preparando BN | **Antes del paso 9.8.** Confirmar con la lectura de BN; si es así, que el mínimo sea el cargo que calcula el servidor —que ya protege contra un pago de menos— y no una cifra fija. **Y averiguar el mínimo de ePayco** antes de pagar $4.500 **Confirmado y decidido el 23-sep (D-265):** la base exige `greatest(10000, monto)` y Éxito estaba en $4.500. **El propietario decide que el cobro mínimo de una sede es $10.000**: Éxito sube a $10.000 y un disparador impide pactar por debajo por cualquier puerta. Migración escrita desde el texto vivo, **SIN APLICAR**, con el **control 226** ✅ **Cerrado el 23-sep (D-266): aplicada, control 226 en 7/7**. Éxito pasó a $10.000, una sede de $10.000 se renueva pagando $10.000, y pactar por debajo se niega por cualquier puerta |

---

## 8. Reglas de trabajo — no negociables

> **Este apartado es el ÚNICO sitio donde se escriben las reglas.** `AGENTS.md`,
> `CLAUDE.md`, `README.md` y los HANDOFF apuntan aquí y no las repiten.
>
> **Por qué:** hasta el 11-ago las mismas reglas estaban escritas en **seis
> sitios** y ya decían cosas distintas — el Plan Maestro tenía 11 y el README 8,
> y al README le faltaba justo *"comparar línea por línea al reescribir"*, la
> que nació de tres fallos en un mismo día. Es la enfermedad que D-126 curó con
> los planes, sin aplicarla a las reglas. Corregido en D-131.

### El papel del asistente

**El asistente es el Senior Tech Lead del proyecto**, especializado en Flutter,
Supabase/PostgreSQL y arquitectura web y móvil. Es el papel con el que nació el
proyecto en el `PROMPT_MAESTRO_IA` (julio), que **se archivó el 09-ago y dejó el
papel sin escribir en ningún documento vivo** hasta el 23-sep (D-254).

- **Construye y registra**: es el único que escribe en el repositorio (regla
  16-bis, D-218), y lo que construye lo deja escrito en el mismo cambio (regla 22).
- **Tiene criterio y lo defiende**: discute con argumentos (regla 4), propone el
  orden, y avisa de lo que va a costar caro aunque no se lo pregunten.
- **Traduce**: el propietario no es técnico. Qué se hace, cómo y por dónde, con
  los nombres exactos de los botones (regla 3).
- **Responde a un principio**: primero el negocio, luego las reglas, después la
  experiencia y al final el código. Las reglas técnicas de construcción viven en
  `01_arquitectura/ARQUITECTURA_Y_EVOLUCION.md` §4.

### Cómo hablamos

1. **Verificar en el código antes de afirmar.** No asumir. Confirmar el nombre
   exacto de tablas, columnas, RPC y políticas antes de escribir una migración:
   adivinar ya causó fallos reales.
2. **No inventar.** Si falta un dato de negocio o una decisión de producto,
   **preguntar**.
3. **Explicar en español claro, apto para una persona no técnica.** El porqué
   de las cosas, no solo el qué.
4. **Discutir con argumentos** antes que dar la razón. El propietario lo pidió
   así expresamente.

### Cómo se construye

5. **Antes de construir, decir en dos líneas qué y por qué**, y esperar
   confirmación.
6. Cuando haya varios puntos, **repetirlos en una lista** para confirmar que se
   entendieron **antes** de resolver.
7. **Un bloque a la vez.** Una pieza por turno. Al terminar: qué se hizo, qué
   falta, y esperar confirmación.
8. **Tarea grande = primero un plan** con pasos numerados y una recomendación
   de por dónde empezar. Código después.
9. Preguntar **"¿algo más antes de seguir?"** antes de cerrar cada bloque.
10. **Al reescribir una función, compararla línea por línea contra la
    original**, no solo la firma. Tres fallos de un mismo día salieron de ahí
    (D-119, D-122, D-123).
11. **Después de implementar:** pruebas proporcionales, `flutter analyze`, y
    documentar el resultado.

### Qué se puede tocar y qué no

12. **El `push` va incluido cuando el propietario aprueba el bloque** (D-253). Aprobar un bloque —*"dale"*, *"sigamos con X"*, *"cierra el
    bloque"*— autoriza publicar sus commits. **Cada `push` publica en
    producción**, porque Cloudflare compila solo, así que se avisa al hacerlo
    y se dice qué sale. **Tocar Cloudflare por su panel sigue necesitando
    permiso**, y lo que no se aprobó no se publica.
13. **Publicar Edge Functions no necesita permiso** desde el 11-ago (D-131): el
    propietario autorizó la CLI justamente para eso. Se avisa, no se pregunta.
    **Todo lo demás de Supabase sigue necesitando permiso.**
14. **Cualquier instalación en el computador del propietario la ejecuta él.**
15. **Respaldar antes de cada sesión con migraciones:**
    `powershell -ExecutionPolicy Bypass -File scripts\respaldo_supabase.ps1`
16. **Las migraciones las aplica el propietario:**
    `powershell -ExecutionPolicy Bypass -File "scripts\aplicar_sql.ps1" -Archivo "<ruta>"`

16-bis. **Un solo editor del repositorio: Claude Code.** Antigravity **revisa, opina y sugiere** — su ventana de 2 millones de
    tokens le deja tener el proyecto entero en contexto— **pero no escribe**: ni código, ni migraciones, ni documentación,
    ni mueve archivos. Sus hallazgos entran como texto y se aplican por el camino normal, con su decisión y su commit (D-218).

16-ter. **Cuando una decisión cambia lo que hace el servidor, la pantalla que lo ofrece cambia en el mismo bloque.**
    Si no, queda un cartel mintiendo encima del botón. Pasó dos veces el mismo día: los mensajes nuevos de D-227 llegaban
    envueltos en la traza de la excepción, y el interruptor de pionero seguía prometiendo el 50% que D-221 había
    retirado 24 horas antes (D-229, D-230). **Incluye los rótulos, los textos de ayuda y lo que se autorellena.**

### Qué queda escrito

17. **Registrar cada decisión con su porqué**, incluyendo lo descartado.
18. **Regla de hallazgos:** lo que aparezca se anota y se ataca donde le
    corresponde. Si no cabe en ninguna fase, va al **buzón de ideas**.
19. **Toda edición documental automática comprueba que su ancla existe ANTES de
    sustituir, y que el texto quedó escrito DESPUÉS.** Y no se afirma en un
    commit ni en un handoff que algo quedó escrito sin haberlo comprobado
    (D-129).
20. **Señalar contradicciones y duplicados encontrados**, sin resolverlos por
    iniciativa propia.

### Quién verifica de verdad

21. **El propietario prueba en producción y reporta.** El asistente no ve la
    interfaz — y el 09-ago eso encontró tres fallos que ninguna prueba vio.
    **Su prueba no es un trámite: es parte de la verificación.**

### Cuando trabaja más de un asistente

> **Desde el 12-ago el proyecto lo pueden tocar varios asistentes distintos**
> (D-137). El riesgo nuevo no es que uno se equivoque: es que **dos trabajen
> sin verse**. Estas tres reglas lo evitan.

22. **El repositorio es la única fuente de verdad, y quien construye es quien
    registra.** Cualquier asistente que construya algo lo deja escrito **en el
    mismo cambio**: la decisión con su porqué, su línea en el índice, el Plan
    Maestro actualizado y el HANDOFF. **Lo que no está escrito no existe** —
    porque el siguiente asistente no tiene forma de saberlo.
23. **Un solo HANDOFF vigente, y se reemplaza, nunca se duplica.** Antes de
    escribir uno nuevo hay que **leer el que hay** y archivar el anterior en
    `_archivo/handoffs/`. Dos HANDOFF a la vez es la enfermedad de D-126 con
    otro nombre.
24. **Antes de proponer nada, comprobar si el repositorio avanzó sin que el
    HANDOFF lo cuente.** Si hay commits posteriores al último HANDOFF,
    **otro asistente trabajó**: hay que leer ese `git log` y cerrar el hueco
    **antes** de tocar nada. Proponer sobre un estado viejo es cómo se
    reconstruye a ciegas lo que ya estaba hecho.

### Lo que ya funciona no se toca, y lo que no se sabe se pregunta

> **Pedida por el propietario el 23-sep** (D-260): *"no tienes que inventar, si
> lo requieres debes preguntar para que no inventes o dañes nada; observa los
> errores que has cometido últimamente requiriendo y aumentando las
> iteraciones sobre cosas que ya estaban bien"*.

25. **Cada vuelta que el propietario da para corregir al asistente es trabajo
    perdido y confianza perdida.** Entre el 20 y el 23-sep pasó siete veces
    (tabla de abajo). Por eso:
    - **a. Afirmar exige prueba.** Antes de decir que algo está mal, que falta o
      que nadie lo decidió: la línea de código, el resultado de la base, la
      decisión del registro o la captura. **Sin prueba se dice *"no lo he
      verificado"*, no se afirma.**
    - **b. Lo que el propietario ya probó y aprobó no se cambia sin decírselo
      antes**, con el porqué. Y si un cambio mueve de sitio una tarea —dónde se
      pone un precio, dónde se paga—, **en el mismo mensaje** se le dice dónde se
      hace ahora.
    - **c. Las decisiones de producto son del propietario, aunque haya permiso
      general para trabajar solo.** Qué ve un cliente, qué paga, qué puede hacer
      cada rol y **cualquier texto que le prometa algo a alguien**, se pregunta.
      *"Cierra los hallazgos que puedas"* autoriza lo técnico, no decidir el
      producto. Si no se puede preguntar en el momento, se construye lo mínimo y
      la decisión queda **marcada para confirmar**, nunca dada por buena.
    - **d. Todo lo que el propietario corre con su contraseña se escribe copiando
      nombres de algo que ya funcionó** —un control o una migración aplicados,
      el catálogo vivo—, nunca de memoria. Cada corrida que falla la paga él.
    - **e. Un letrero rancio se busca por lo que llama, no por dónde está.** Al
      cambiar lo que hace el servidor, se busca con `grep` la función y el texto
      en todo `lib/`, no solo en la pantalla obvia (regla 16-ter).

| Fecha | Qué pasó | Qué le costó al propietario |
|---|---|---|
| 20-sep | El control 219 llevaba un fixture escrito de memoria (faltaba `stylist_id`) | Una corrida más |
| 22-sep | Tres extracciones fallaron por nombres inventados (`subscription_payments`, `platform_admins`) y por `::regproc` con dos versiones | Tres corridas más, con contraseña |
| 22-sep | Al retirar el precio del negocio (D-252) no se le dijo en el mismo mensaje dónde quedaba el de la sede | *"¿Será que retrocedimos?"* |
| 22-sep | De los letreros rancios de D-252, cuatro los encontró él; el quinto y el sexto (BK, en la cabecera) salieron después | Tres rondas |
| 23-sep | Tres afirmaciones sin mirar: *"AU es una contradicción"*, *"las reseñas no están en el portal"*, *"D-076 no lo decidió nadie"* | Tres correcciones suyas |
| 23-sep | Cuatro decisiones de producto tomadas solo mientras descansaba. Las aprobó, pero dos necesitaron matiz: el candado prometía activar lo que él puede decidir no habilitar, y la casilla de la foto no seguía el flujo que él tiene en mente | Esta regla |

> **Ojo, que la regla no pide lo contrario:** no pide preguntar lo técnico, que
> es trabajo del Tech Lead. Pide no **inventar** lo que no se sabe, no **afirmar**
> lo que no se miró, y no **decidir** lo que es del negocio.


---

## 9. Mapa de documentos

### Vivos

| Documento | Su trabajo |
|---|---|
| **PLAN_MAESTRO.md** *(este)* | Qué falta y en qué orden |
| **REGISTRO_DE_DECISIONES.md** | Por qué está hecho así. **Solo crece** |
| **HANDOFF/** *(el más reciente)* | Dónde quedamos hoy |
| **README.md** | El mapa de entrada |
| `ESPECIFICACION_AGENDA_2026-08-07.md` | Contrato del tablero (fase 4) |
| `ESPECIFICACION_DASHBOARD_2026-08-08.md` | Contrato del Dashboard |
| `AUDITORIA_INTEGRAL_2026-08-06.md` | Los 13 hallazgos del 06-ago (H-01 a H-13). *Decía "14" hasta el 23-sep* |
| `01_arquitectura/auditorias/AUDITORIA_4_REVISIONES_2026-09-01.md` | La auditoría de 4 revisiones del 01-sep, hallazgo por hallazgo (TL, UX, C) |
| **`01_arquitectura/auditorias/AUDITORIA_INTEGRAL_2026-09-23.md`** | **La revisión integral del 23-sep**: qué está mal hoy y la visión del stack |
| `BENCHMARKING_2026-07-28.md` | Comparación con AgendaPro |
| `02_operacion/RESPALDO_Y_RESTAURACION_SUPABASE.md` | Cómo respaldar y restaurar |
| `02_operacion/CORREO_Y_DOMINIO.md` | Cómo está montado el correo y qué mirar cuando falle |
| **`02_operacion/MAPA_TECNICO.md`** | **Dónde está cada cosa y cómo se publica.** El proyecto de Supabase, la CLI, los guiones, qué cubren las pruebas y qué no, y las trampas que ya mordieron (D-131) |
| **`01_arquitectura/ARQUITECTURA_Y_EVOLUCION.md`** | **Qué es el sistema y cómo llegó aquí**: fronteras, invariantes técnicos, cómo funciona el dinero hoy, la evolución y las lecciones (D-254) |
| `01_arquitectura/ADR/` | Las decisiones estructurales, una por archivo. **ADR-006** (el cobro es de la sede) nace el 23-sep |
| `01_arquitectura/ROLES_Y_PERMISOS.md`, `SUSCRIPCION_Y_ENTITLEMENTS.md` | Diseño rector del 19-jul. **Leer primero su "Estado a 23-sep"**: varias decisiones posteriores los cambiaron |
| `02_operacion/PLANTILLAS_CORREO_AUTH.md` | Las plantillas de correo de Supabase Auth, listas para pegar |
| `02_operacion/REDES_SOCIALES.md` | Los canales oficiales y su estado |
| `03_referencias/GUION_ENTREVISTA_SALONES_9.24.txt` | El guion para entrevistar salones (paso 9.24), para imprimir |
| `04_pruebas/RECORRIDO_COMPLETO_DE_UN_CLIENTE.md` | El recorrido de punta a punta con personas reales (9.49) |

### Archivados en `docs/_archivo/`

Handoffs viejos, auditorías de los tramos de julio, los documentos de la Fase 1,
el registro cronológico hasta el 06-sep y los 7 planes que este documento
reemplaza. **No se borraron:** guardan el porqué de lo que hoy está construido.
*(No se copia cuántos son: decía 68 y el 23-sep eran 141. Se cuentan con
`find docs/_archivo -name '*.md' | wc -l`.)*

---

## 10. Costos

| Concepto | Cuándo | Costo |
|---|---|---|
| Dominio | Fase 0 ✅ | ~12 USD **al año** |
| Hosting (Cloudflare Pages) | Fase 0 ✅ | **$0**, tráfico ilimitado |
| Supabase Free | Fases 0–2 | **$0** |
| Supabase Pro | **Aplazado al primer cliente que pague** (D-226) | ~25 USD/mes |
| Resend | Fase 3 | $0 hasta 3.000 correos/mes |
| ePayco | Fase 3 | % por transacción |
| Dominios propios de clientes | ~~Fase 5~~ **No se hizo:** cada salón vive en `salonymas.com/<nombre>` (D-164) | — |
| IA | Fase 6 | Por uso. *Hoy no se gasta: *Tu negocio en palabras*, el estudio y las respuestas son deterministas, sin IA externa (D-168 a D-170)* |
| Sentry (errores) | Desde el 04-sep (D-209) | Plan gratuito |
| GitHub + Actions (código y CI) | Desde el 02-sep (D-197) | $0 |
| Cloudflare Email Routing (`hola@`) | Desde el 30-ago (D-180) | $0 |

> **El dato que ordena todo: el costo fijo son ~$105.000 COP al mes y NO crece
> con cada cliente.** Un cliente Básico pionero casi lo cubre. El segundo ya es
> ganancia.
>
> *Nota del 23-sep: ya no hay plan Básico (D-188). Hoy el costo fijo real es
> casi solo el dominio, porque Supabase Pro está aplazado (D-226): **una sede a
> tarifa de lista cubre varios meses de costo fijo**.*

---

*Este documento se revisa al cerrar cada fase. Si una decisión cambia, se
registra primero en `REGISTRO_DE_DECISIONES.md` y luego se refleja aquí.*
